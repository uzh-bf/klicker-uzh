diff --git a/dist/esm/server/lib/router-utils/setup-dev-bundler.js b/dist/esm/server/lib/router-utils/setup-dev-bundler.js
index ddb7d83eab907d4b31fd686a7381c15ff27246a5..ff35b223c880caece9aa3daf47077c315b0a9d85 100644
--- a/dist/esm/server/lib/router-utils/setup-dev-bundler.js
+++ b/dist/esm/server/lib/router-utils/setup-dev-bundler.js
@@ -229,7 +229,7 @@ async function startWatcher(opts) {
         const routeTypesFilePath = path.join(distDir, 'types', 'routes.d.ts');
         const validatorFilePath = path.join(distDir, 'types', 'validator.ts');
         let initialWatchTime = performance.now() + performance.timeOrigin;
-        wp.on('aggregated', async ()=>{
+        const aggregate = async ()=>{
             var _serverFields_middleware, _serverFields_middleware1;
             let writeEnvDefinitions = false;
             let typescriptStatusFromLastAggregation = enabledTypeScript;
@@ -788,6 +788,19 @@ async function startWatcher(opts) {
                     Log.warn('Failed to reload dynamic routes:', e);
                 }
             }
+        };
+        // Async event listeners can overlap and publish an older route inventory last.
+        let aggregation = Promise.resolve();
+        wp.on('aggregated', ()=>{
+            aggregation = aggregation.then(aggregate).catch((e)=>{
+                if (!resolved) {
+                    reject(e);
+                    resolved = true;
+                } else {
+                    Log.warn('Failed to reload dynamic routes:', e);
+                }
+            });
+            return aggregation;
         });
         wp.watch({
             directories: [
diff --git a/dist/server/lib/router-utils/setup-dev-bundler.js b/dist/server/lib/router-utils/setup-dev-bundler.js
index 2924899412660e0dfefe1bcecf6213f2e19a5c07..2c1a44e9ae185bf8e2a95d1902ad93573874aabd 100644
--- a/dist/server/lib/router-utils/setup-dev-bundler.js
+++ b/dist/server/lib/router-utils/setup-dev-bundler.js
@@ -297,7 +297,7 @@ async function startWatcher(opts) {
         const routeTypesFilePath = _path.default.join(distDir, 'types', 'routes.d.ts');
         const validatorFilePath = _path.default.join(distDir, 'types', 'validator.ts');
         let initialWatchTime = performance.now() + performance.timeOrigin;
-        wp.on('aggregated', async ()=>{
+        const aggregate = async ()=>{
             var _serverFields_middleware, _serverFields_middleware1;
             let writeEnvDefinitions = false;
             let typescriptStatusFromLastAggregation = enabledTypeScript;
@@ -856,6 +856,19 @@ async function startWatcher(opts) {
                     _log.warn('Failed to reload dynamic routes:', e);
                 }
             }
+        };
+        // Async event listeners can overlap and publish an older route inventory last.
+        let aggregation = Promise.resolve();
+        wp.on('aggregated', ()=>{
+            aggregation = aggregation.then(aggregate).catch((e)=>{
+                if (!resolved) {
+                    reject(e);
+                    resolved = true;
+                } else {
+                    _log.warn('Failed to reload dynamic routes:', e);
+                }
+            });
+            return aggregation;
         });
         wp.watch({
             directories: [
