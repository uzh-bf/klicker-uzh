version: "3"
services:
  react:
    build:
      context: .
      args:
        API_URL: "http://localhost:4000/graphql"
        FINGERPRINTING: "true"
    environment:
      REDIS_URL: ":klicker-dev@144.76.118.94:6379"
      STATIC_PATH: "/"
    ports:
      - "3000:3000"
