mutation subscribeNativeDeviceToPush($courseId: String!, $token: String!) {
  subscribeNativeDeviceToPush(courseId: $courseId, token: $token) {
    id
    subscriptions {
      id
      token
    }
  }
}
