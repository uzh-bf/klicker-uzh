query GetCourseWeeklyActivity($courseId: String) {
  getCourseWeeklyActivity(courseId: $courseId) {
    totalParticipants
    isSuppressed
    weeklyActivity {
      date
      activeParticipants
    }
  }
}
