query ParticipantPerformance($courseId: String!) {
  participantPerformance(courseId: $courseId) {
    id
    firstErrorRate
    firstPerformance
    lastErrorRate
    lastPerformance
    totalErrorRate
    totalPerformance
  }
}
