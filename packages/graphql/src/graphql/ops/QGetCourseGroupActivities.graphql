query GetCourseGroupActivities($courseId: String!) {
  groupActivities(courseId: $courseId) {
    id
    displayName
    status
    description
    scheduledStartAt
    scheduledEndAt
    escapeRoomConfig {
      id
      timeLimit
      hintPenalty
      lockoutSeconds
      introText
    }
  }
}
