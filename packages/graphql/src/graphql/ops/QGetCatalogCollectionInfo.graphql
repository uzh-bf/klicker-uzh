query GetCatalogCollectionInfo($catalogCollectionId: String) {
  getCatalogCollectionInfo(catalogCollectionId: $catalogCollectionId) {
    id
    name
    access
    ownerShortname
    isRequested
    isManager
    isEditor
    isShared
  }
}
