query GetFeedbacks($quizId: String!) {
  feedbacks(quizId: $quizId) {
    id
    isPublished
    isPinned
    isResolved
    content
    votes
    resolvedAt
    createdAt
    responses {
      id
      content
      positiveReactions
      negativeReactions
      createdAt
    }
  }
}
