query GetCourseLearningAnalyticsControl($courseId: String!) {
  course(id: $courseId) {
    id
    isArchived
    isLearningAnalyticsEnabled
    analyticsStatus {
      areAnalyticsValid
      analyticsLastComputedAt
    }
  }
}
