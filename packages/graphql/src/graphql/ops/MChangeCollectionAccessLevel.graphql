mutation ChangeCollectionAccessLevel(
  $collectionId: Int!
  $permissionId: Int!
  $accessLevel: AccessLevel!
) {
  changeCollectionAccessLevel(
    collectionId: $collectionId
    permissionId: $permissionId
    accessLevel: $accessLevel
  ) {
    permissionId
    accessLevel
  }
}
