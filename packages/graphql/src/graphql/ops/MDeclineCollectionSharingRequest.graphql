mutation DeclineCollectionSharingRequest(
  $collectionId: Int!
  $userId: String!
) {
  declineCollectionSharingRequest(
    collectionId: $collectionId
    userId: $userId
  ) {
    collectionId
    userId
  }
}
