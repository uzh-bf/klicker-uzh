mutation ChangeCourseDates(
  $courseId: String!
  $startDate: Date
  $endDate: Date
) {
  changeCourseDates(
    courseId: $courseId
    startDate: $startDate
    endDate: $endDate
  ) {
    id
    startDate
    endDate
  }
}
