mutation RespondToFlashcardInstance(
  $id: Int!
  $courseId: String!
  $correctness: Int!
) {
  respondToFlashcardInstance(
    id: $id
    courseId: $courseId
    correctness: $correctness
  ) {
    id
  }
}
