mutation ImportAnswerCollection($collectionId: Int!) {
  importAnswerCollection(collectionId: $collectionId) {
    id
    name
    access
    description
    ownerShortname
    entries {
      id
      value
    }
  }
}
