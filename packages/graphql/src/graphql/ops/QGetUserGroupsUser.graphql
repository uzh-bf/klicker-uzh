query GetUserGroupsUser {
  getUserGroupsUser {
    id
    name
    members {
      id
      shortname
      email
    }
    admins {
      id
      shortname
      email
    }
    owner {
      id
      shortname
      email
    }
    numOfMembers
    isMember
    isAdmin
    isOwner
  }
}
