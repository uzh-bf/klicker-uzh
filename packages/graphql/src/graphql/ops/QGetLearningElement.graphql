#import "./FQuestionData.graphql"

query GetLearningElement($id: ID!) {
  learningElement(id: $id) {
    id
    displayName
    description
    pointsMultiplier
    resetTimeDays
    orderType
    course {
      id
      displayName
      color
    }
    instances {
      id
      pointsMultiplier
      ...QuestionData
      evaluation {
        feedbacks {
          ix
          feedback
          correct
          value
        }
        choices
        score
        pointsAwarded
        percentile
        newPointsFrom
      }
    }
  }
}
