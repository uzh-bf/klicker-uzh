#import "./FQuestionData.graphql"

query GetLearningElement($id: ID!) {
  learningElement(id: $id) {
    id
    displayName
    course {
      id
      displayName
      color
    }
    instances {
      id
      ...QuestionData
      evaluation {
        feedbacks {
          ix
          feedback
          correct
          value
        }
        choices
      }
    }
  }
}
