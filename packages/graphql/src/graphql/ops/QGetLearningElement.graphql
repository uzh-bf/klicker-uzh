#import "./FQuestionData.graphql"

query GetLearningElement($id: ID!) {
  learningElement(id: $id) {
    id
    displayName
    pointsMultiplier
    course {
      id
      displayName
      color
    }
    instances {
      id
      ...QuestionData
      evaluation {
        feedbacks {
          ix
          feedback
          correct
          value
        }
        choices
        score
        pointsAwarded
        percentile
        newPointsFrom
      }
    }
  }
}
