#import "./FQuestionData.graphql"

query GetLearningElement($id: ID!) {
  learningElement(id: $id) {
    id
    course {
      id
    }
    instances {
      id
      ...QuestionData
      evaluation {
        feedbacks {
          ix
          feedback
          correct
          value
        }
        choices
      }
    }
  }
}
