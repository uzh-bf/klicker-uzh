#import "./FQuestionData.graphql"

query GetLearningElement($id: ID!) {
  learningElement(id: $id) {
    id
    course {
      id
      displayName
    }
    instances {
      id
      ...QuestionData
      evaluation {
        feedbacks {
          ix
          feedback
          correct
          value
        }
        choices
      }
    }
  }
}
