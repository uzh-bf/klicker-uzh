query GetBookmarkedQuestions($courseId: String!) {
  getBookmarkedQuestions(courseId: $courseId) {
    id
    elements {
      id
      questionInstance {
        id
        questionData {
          id
          name
          type
        }
      }
    }
  }
}
