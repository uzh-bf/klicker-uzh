query GetAnswerCollectionCatalogInfo(
  $collectionId: Int!
  $catalogCollectionId: String
) {
  getAnswerCollectionCatalogInfo(
    collectionId: $collectionId
    catalogCollectionId: $catalogCollectionId
  ) {
    id
    name
    description
    entries {
      id
      value
    }
  }
}
