mutation CreateParticipantGroup($courseId: String!, $name: String!) {
  createParticipantGroup(courseId: $courseId, name: $name) {
    id
    name
    code
    participants {
      id
      username
    }
  }
}
