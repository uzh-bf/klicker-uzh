#import "./FQuestionData.graphql"

query GetMicroSession($id: String!) {
  microSession(id: $id) {
    id
    displayName
    description
    course {
      id
      displayName
      color
    }
    instances {
      id
      ...QuestionData
      evaluation {
        feedbacks {
          ix
          feedback
          correct
          value
        }
        choices
        score
        pointsAwarded
        percentile
        newPointsFrom
      }
    }
  }
}
