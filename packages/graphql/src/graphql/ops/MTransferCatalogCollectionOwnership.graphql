mutation TransferCatalogCollectionOwnership(
  $catalogCollectionId: String!
  $usernameOrEmail: String!
) {
  transferCatalogCollectionOwnership(
    catalogCollectionId: $catalogCollectionId
    usernameOrEmail: $usernameOrEmail
  ) {
    ...PermissionInfoData
  }
}
