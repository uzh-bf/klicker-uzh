fragment StudentLiveQuizBlocksData on LiveQuiz {
  blocks {
    id
    status
    expiresAt
    timeLimit
    randomSelection
    execution
    escapeRoomTotalInstances
    escapeRoomClearedInstances
    escapeRoomConfig {
      timeLimit
      hintPenalty
      introText
    }
    escapeRoomAttempts {
      id
      startedAt
      timeLimit
      penaltySeconds
      hintsUsed
      lockoutUntil
      status
      completedAt
    }
    elements {
      id
      type
      elementType
      revealedHint
      options {
        hasHint
      }
      ...ElementDataWithoutSolutions
    }
  }

  activeBlock {
    id
    status
    expiresAt
    timeLimit
    randomSelection
    execution
    escapeRoomTotalInstances
    escapeRoomClearedInstances
    escapeRoomConfig {
      timeLimit
      hintPenalty
      introText
    }
    escapeRoomAttempts {
      id
      startedAt
      timeLimit
      penaltySeconds
      hintsUsed
      lockoutUntil
      status
      completedAt
    }
    elements {
      id
      type
      elementType
      revealedHint
      options {
        hasHint
      }
      correlationKey
      ...ElementDataWithoutSolutions
    }
  }
}
