mutation ImportElementPackage(
  $importToken: String!
  $selectedElementRefs: [String!]!
) {
  importElementPackage(
    importToken: $importToken
    selectedElementRefs: $selectedElementRefs
  ) {
    importedElements
    warnings
  }
}
