mutation RequestAnswerCollection($collectionId: Int!) {
  requestAnswerCollection(collectionId: $collectionId) {
    id
    uuid
    name
    objectType
    access
    ownerShortname
    isRequested
    isShared
    isOwner
  }
}
