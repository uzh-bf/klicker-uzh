query GetUserQuestions {
  userQuestions {
    id
    name
    type
    content

    pointsMultiplier
    version

    isArchived
    isDeleted
    createdAt
    updatedAt

    options

    tags {
      id
      name
      order
    }
  }
}
