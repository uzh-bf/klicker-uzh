mutation SetResearchConsentWithRevision(
  $consent: Boolean!
  $expectedRevision: Int!
  $disclosureVersion: String!
) {
  setResearchConsent(
    consent: $consent
    expectedRevision: $expectedRevision
    disclosureVersion: $disclosureVersion
  ) {
    ...ParticipantDataUseData
  }
}
