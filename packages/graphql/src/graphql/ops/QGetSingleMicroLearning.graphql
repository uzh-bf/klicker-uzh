query GetSingleMicroLearning(
  $id: String!
  $withEscapeRoomHints: Boolean! = false
) {
  getSingleMicroLearning(id: $id) {
    ...MicroLearningData
  }
  escapeRoomHints(microLearningId: $id) @include(if: $withEscapeRoomHints) {
    instanceId
    hint
  }
}
