mutation ManualRandomGroupAssignments($courseId: String!) {
  manualRandomGroupAssignments(courseId: $courseId) {
    id
    name
    code
    averageMemberScore
    groupActivityScore
    participants {
      id
      username
      score
      email
      avatar
    }
  }
}
