subscription FeedbackAdded($sessionId: String!) {
  feedbackAdded(sessionId: $sessionId) {
    id
    isPublished
    isPinned
    isResolved
    content
    votes
    resolvedAt
    createdAt
    responses {
      id
      content
      positiveReactions
      negativeReactions
    }
  }
}
