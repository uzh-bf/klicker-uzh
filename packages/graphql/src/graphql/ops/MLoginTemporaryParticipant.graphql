mutation LoginTemporaryParticipant(
  $liveQuizId: String!
  $pseudonym: String!
  $avatar: String
) {
  loginTemporaryParticipant(
    liveQuizId: $liveQuizId
    pseudonym: $pseudonym
    avatar: $avatar
  )
}
