mutation LoginTemporaryParticipant(
  $liveQuizId: String!
  $pseudonym: String!
  $locale: LocaleType
) {
  loginTemporaryParticipant(
    liveQuizId: $liveQuizId
    pseudonym: $pseudonym
    locale: $locale
  )
}
