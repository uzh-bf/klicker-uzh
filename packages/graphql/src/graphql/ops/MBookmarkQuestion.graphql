mutation BookmarkQuestion(
  $instanceId: Int!
  $courseId: String!
  $bookmarked: Boolean!
) {
  bookmarkQuestion(
    instanceId: $instanceId
    courseId: $courseId
    bookmarked: $bookmarked
  ) {
    id
    isActive
    bookmarkedQuestions {
      id
    }
  }
}
