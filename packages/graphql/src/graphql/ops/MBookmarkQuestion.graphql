mutation BookmarkQuestion(
  $stackId: Int!
  $courseId: String!
  $bookmarked: Boolean!
) {
  bookmarkQuestion(
    stackId: $stackId
    courseId: $courseId
    bookmarked: $bookmarked
  ) {
    id
    isActive
    bookmarkedStacks {
      id
    }
  }
}
