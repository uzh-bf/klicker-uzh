#import "./FQuestionData.graphql"

mutation ResponseToQuestionInstance($id: ID!, $response: ResponseInput!) {
  respondToQuestionInstance(id: $id, response: $response) {
    id
    ...QuestionData
    evaluation {
      feedbacks {
        ix
        feedback
        correct
        value
      }
      choices
    }
  }
}
