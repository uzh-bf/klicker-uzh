#import "./FQuestionData.graphql"

mutation ResponseToQuestionInstance(
  $courseId: ID!
  $id: ID!
  $response: ResponseInput!
) {
  respondToQuestionInstance(courseId: $courseId, id: $id, response: $response) {
    id
    ...QuestionData
    evaluation {
      feedbacks {
        ix
        feedback
        correct
        value
      }
      choices
    }
  }
}
