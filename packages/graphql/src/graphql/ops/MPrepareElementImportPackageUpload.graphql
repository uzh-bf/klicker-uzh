mutation PrepareElementImportPackageUpload($filename: String!, $bytes: Int!) {
  prepareElementImportPackageUpload(filename: $filename, bytes: $bytes) {
    uploadURL
    uploadCapability
    artifactId
  }
}
