mutation LeaveParticipantGroup($groupId: String!, $courseId: String!) {
  leaveParticipantGroup(groupId: $groupId, courseId: $courseId) {
    id
    name
    code
    participants {
      id
      username
    }
  }
}
