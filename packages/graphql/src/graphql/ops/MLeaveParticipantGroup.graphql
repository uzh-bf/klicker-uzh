mutation LeaveParticipantGroup($groupId: ID!, $courseId: ID!) {
  leaveParticipantGroup(groupId: $groupId, courseId: $courseId) {
    id
    name
    code
    participants {
      id
      username
    }
    course {
      id
      name
      displayName
    }
  }
}
