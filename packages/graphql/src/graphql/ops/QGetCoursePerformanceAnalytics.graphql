query GetCoursePerformanceAnalytics($courseId: String!) {
  getCoursePerformanceAnalytics(courseId: $courseId) {
    name
    totalParticipants
    activityProgresses {
      activityName
      activityType
      startedCount
      completedCount
      repeatedCount
    }
  }
}
