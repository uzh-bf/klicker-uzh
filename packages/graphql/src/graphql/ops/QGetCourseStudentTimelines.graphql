query GetCourseStudentTimelines {
  getCourseStudentTimelines {
    courseId
    courseName
    courseStart
    courseEnd
    timelineEntries {
      timestamp
      collectedPoints
      collectedXp
      totalPoints
      totalXp
    }
  }
}
