query GetSessionLeaderboard($sessionId: String!) {
  sessionLeaderboard(sessionId: $sessionId) {
    id
    participantId
    rank
    username
    avatar
    score
    isSelf
    lastBlockOrder
  }
}
