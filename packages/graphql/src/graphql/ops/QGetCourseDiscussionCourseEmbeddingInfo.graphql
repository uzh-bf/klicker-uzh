query GetCourseDiscussionCourseEmbeddingInfo(
  $courseId: String!
  $allowAnonymous: Boolean
  $expiresInHours: Int
) {
  getCourseDiscussionEmbeddingInfo(
    courseId: $courseId
    allowAnonymous: $allowAnonymous
    expiresInHours: $expiresInHours
  ) {
    courseId
    scopeKey
    scopeLabel
    allowAnonymous
    expiresAt
    embedToken
    embedUrl
  }
}
