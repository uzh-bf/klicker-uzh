mutation AddConfusionTimestep(
  $quizId: String!
  $difficulty: Int!
  $speed: Int!
) {
  addConfusionTimestep(
    quizId: $quizId
    difficulty: $difficulty
    speed: $speed
  ) {
    difficulty
    speed
  }
}
