mutation AddConfusionTimestep(
  $sessionId: String!
  $difficulty: Int!
  $speed: Int!
) {
  addConfusionTimestep(
    sessionId: $sessionId
    difficulty: $difficulty
    speed: $speed
  ) {
    difficulty
    speed
  }
}
