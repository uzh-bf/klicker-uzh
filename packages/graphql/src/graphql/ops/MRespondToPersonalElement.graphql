mutation MRespondToPersonalElement(
  $id: String!
  $response: FlashcardCorrectnessType!
  $expectedVersion: Int!
) {
  respondToPersonalElement(
    id: $id
    response: $response
    expectedVersion: $expectedVersion
  ) {
    ...FPersonalElement
  }
}
