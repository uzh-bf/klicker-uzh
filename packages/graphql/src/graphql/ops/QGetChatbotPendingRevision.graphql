query QGetChatbotPendingRevision($id: String!, $expectedRevisionVersion: Int) {
  getChatbotPendingRevision(
    id: $id
    expectedRevisionVersion: $expectedRevisionVersion
  ) {
    ...ChatbotAuthoringRevisionData
  }
}
