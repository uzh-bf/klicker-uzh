mutation CreateCourseDiscussionThread(
  $input: CreateCourseDiscussionThreadInput!
) {
  createCourseDiscussionThread(input: $input) {
    thread {
      ...DiscussionThreadData
    }
    failureCode
  }
}
