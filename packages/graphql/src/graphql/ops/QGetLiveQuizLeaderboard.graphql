query GetLiveQuizLeaderboard($quizId: String!) {
  liveQuizLeaderboard(quizId: $quizId) {
    id
    participantId
    rank
    username
    avatar
    score
    level
    isSelf
    isTemporary
    lastBlockOrder
  }
}
