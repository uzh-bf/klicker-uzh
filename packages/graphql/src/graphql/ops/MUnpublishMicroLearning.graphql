mutation UnpublishMicroLearning($id: String!, $deleteResponses: Boolean!) {
  unpublishMicroLearning(id: $id, deleteResponses: $deleteResponses) {
    id
    name
    displayName
    status
    numOfStacks
  }
}
