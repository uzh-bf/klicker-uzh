query GetSingleAnswerCollection($id: Int!) {
  getSingleAnswerCollection(id: $id) {
    id
    name
    description
    ownerShortname
    numSharedUsers
    permissionLevel

    isOwner
    isManager
    isEditor
    isShared

    entries {
      id
      value
      numSolutionUsages
    }
  }
}
