mutation RequestChatbotPublication(
  $id: String!
  $useCase: String!
  $expectedStudentCount: Int!
) {
  requestChatbotPublication(
    id: $id
    useCase: $useCase
    expectedStudentCount: $expectedStudentCount
  ) {
    id
  }
}
