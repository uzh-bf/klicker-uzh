query QAdaptiveAssessmentItemPoolPreview($assessmentId: String!) {
  adaptiveAssessmentItemPoolPreview(assessmentId: $assessmentId) {
    competenceName
    subCompetenceName
    levelLabel
    count
  }
}
