subscription FeedbackCreated($quizId: String!) {
  feedbackCreated(quizId: $quizId) {
    id
    isPublished
    isPinned
    isResolved
    content
    votes
    resolvedAt
    createdAt
    responses {
      id
      content
      positiveReactions
      negativeReactions
    }
  }
}
