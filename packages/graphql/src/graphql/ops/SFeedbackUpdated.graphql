subscription FeedbackUpdated($quizId: String!) {
  feedbackUpdated(quizId: $quizId) {
    id
    isPublished
    isPinned
    isResolved
    content
    votes
    resolvedAt
    createdAt
    responses {
      id
      content
      positiveReactions
      negativeReactions
    }
  }
}
