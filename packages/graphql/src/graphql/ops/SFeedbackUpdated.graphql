subscription FeedbackUpdated($sessionId: String!) {
  feedbackUpdated(sessionId: $sessionId) {
    id
    isPublished
    isPinned
    isResolved
    content
    votes
    resolvedAt
    createdAt
    responses {
      id
      content
      positiveReactions
      negativeReactions
    }
  }
}
