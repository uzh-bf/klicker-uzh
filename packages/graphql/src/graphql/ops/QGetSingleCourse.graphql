query GetSingleCourse($courseId: String!) {
  course(id: $courseId) {
    id
    isArchived
    isGamificationEnabled
    pinCode
    name
    displayName
    description
    notificationEmail
    color
    numOfParticipants
    numOfParticipantGroups
    averageScore
    startDate
    endDate

    isGroupCreationEnabled
    groupDeadlineDate
    maxGroupSize
    preferredGroupSize
    randomAssignmentFinalized

    derivedAccess
    numSharedUsers
    permissionLevel
    isOwner
    isManager
    isEditor
    isShared
    isRemovable

    # TODO: update here to use more efficient query method as well -> details no longer needed
    liveQuizzesInfo {
      ...ActivityInfoData
    }
    practiceQuizzesInfo {
      ...ActivityInfoData
    }
    microLearningsInfo {
      ...ActivityInfoData
    }
    groupActivitiesInfo {
      ...ActivityInfoData
    }
  }
}
