mutation JoinParticipantGroup($courseId: String!, $code: Int!) {
  joinParticipantGroup(courseId: $courseId, code: $code) {
    id
    name
    code
    participants {
      id
      username
    }
  }
}
