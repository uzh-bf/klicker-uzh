mutation RespondToPracticeQuizStack(
  $stackId: Int!
  $responses: [StackResponseInput!]!
) {
  respondToPracticeQuizStack(stackId: $stackId, responses: $responses) {
    id
    status
    score
  }
}
