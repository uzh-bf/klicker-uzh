mutation RespondToPracticeQuizStack(
  $stackId: Int!
  $courseId: String!
  $responses: [StackResponseInput!]!
) {
  respondToPracticeQuizStack(
    stackId: $stackId
    courseId: $courseId
    responses: $responses
  ) {
    id
    status
    score
  }
}
