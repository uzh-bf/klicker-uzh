mutation UnsubscribeFromPush(
  $courseId: String!
  $endpoint: String
  $token: String
  $isNativePlatform: Boolean!
) {
  unsubscribeFromPush(
    courseId: $courseId
    endpoint: $endpoint
    token: $token
    isNativePlatform: $isNativePlatform
  )
}
