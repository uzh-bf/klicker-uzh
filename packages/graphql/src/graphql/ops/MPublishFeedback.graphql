mutation PublishFeedback(
  $id: Int!
  $isPublished: Boolean!
  $liveQuizId: String!
) {
  publishFeedback(id: $id, isPublished: $isPublished, liveQuizId: $liveQuizId) {
    id
    isPublished
  }
}
