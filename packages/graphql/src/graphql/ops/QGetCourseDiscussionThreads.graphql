query GetCourseDiscussionThreads(
  $courseId: String!
  $scopeKey: String
  $sort: DiscussionSort
  $limit: Int
  $cursor: String
  $includeLinkedLiveQuizSpaces: Boolean
  $embedToken: String
) {
  courseDiscussionThreads(
    courseId: $courseId
    scopeKey: $scopeKey
    sort: $sort
    limit: $limit
    cursor: $cursor
    includeLinkedLiveQuizSpaces: $includeLinkedLiveQuizSpaces
    embedToken: $embedToken
  ) {
    threads {
      ...DiscussionThreadData
    }
    nextCursor
    hasMore
  }
}
