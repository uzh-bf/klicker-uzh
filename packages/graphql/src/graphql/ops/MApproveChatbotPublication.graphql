mutation ApproveChatbotPublication($id: String!, $expectedRevisionVersion: Int) {
  approveChatbotPublication(
    id: $id
    expectedRevisionVersion: $expectedRevisionVersion
  ) {
    id
    status
    publishedAt
  }
}
