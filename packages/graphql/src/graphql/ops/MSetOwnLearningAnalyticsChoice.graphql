mutation SetOwnLearningAnalyticsChoice(
  $courseId: String!
  $status: LearningAnalyticsChoice!
) {
  setOwnLearningAnalyticsChoice(courseId: $courseId, status: $status) {
    courseId
    status
    isCurrent
  }
}
