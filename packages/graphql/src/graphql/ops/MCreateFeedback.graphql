mutation CreateFeedback(
  $sessionId: ID!
  $content: String!
  $isPublished: Boolean!
) {
  createFeedback(
    sessionId: $sessionId
    content: $content
    isPublished: $isPublished
  ) {
    id
    isPublished
    isPinned
    isResolved
    content
    votes
  }
}
