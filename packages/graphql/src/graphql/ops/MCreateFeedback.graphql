mutation CreateFeedback(
  $sessionId: ID!
  $content: String!
) {
  createFeedback(
    sessionId: $sessionId
    content: $content
  ) {
    id
    isPublished
    isPinned
    isResolved
    content
    votes
  }
}
