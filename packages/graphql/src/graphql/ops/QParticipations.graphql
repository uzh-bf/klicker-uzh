query Participations($endpoint: String) {
  participations(endpoint: $endpoint) {
    id
    points
    subscriptions {
      id
      endpoint
    }
    course {
      id
      displayName
      microSessions {
        id
        displayName
      }
      sessions {
        id
        displayName
      }
    }
  }
}
