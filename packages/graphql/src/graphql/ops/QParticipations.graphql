query Participations($endpoint: String) {
  participations(endpoint: $endpoint) {
    id
    completedMicroSessions
    subscriptions {
      id
      endpoint
    }
    course {
      id
      displayName
      microSessions {
        id
        displayName
        scheduledStartAt
        scheduledEndAt
      }
      sessions {
        id
        displayName
        linkTo
      }
    }
  }
}
