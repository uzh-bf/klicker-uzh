query Self {
  self {
    id
    username
    avatar
    avatarSettings
    xp
    level
    achievements {
      id
      achievedAt
      achievedCount
      achievement {
        id
        name
        description
        icon
        iconColor
      }
    }
  }
}
