query Self {
  self {
    id
    username
    avatar
    avatarSettings
    achievements {
      id
      achievedAt
      achievedCount
      achievement {
        id
        name
        description
        icon
        iconColor
      }
    }
  }
}
