query Self {
  self {
    id
    email
    username
    avatar
    avatarSettings
    isProfilePublic
    xp
    level
    levelData {
      id
      index
      name
      avatar
      requiredXp
      nextLevel {
        id
        index
        requiredXp
        avatar
        name
      }
    }
  }
}
