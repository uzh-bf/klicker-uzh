query Self {
  self {
    id
    email
    username
    avatar
    avatarSettings
    isActive
    isProfilePublic
    isSSOAccount
    isEmailValid
    xp
    level
    levelData {
      id
      index
      name
      avatar
      requiredXp
      nextLevel {
        id
        index
        requiredXp
        avatar
        name
      }
    }
  }
}
