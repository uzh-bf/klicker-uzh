query GetAssessmentParticipantInvitations($courseId: String!) {
  assessmentParticipantInvitations(courseId: $courseId) {
    id
    email
    matriculationNumber
    status
    invitedAt
    acceptedAt
  }
}
