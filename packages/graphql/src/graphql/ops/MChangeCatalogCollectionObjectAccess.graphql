mutation ChangeCatalogCollectionObjectAccess(
  $catalogCollectionId: String!
  $access: ObjectAccess!
) {
  changeCatalogCollectionObjectAccess(
    catalogCollectionId: $catalogCollectionId
    access: $access
  )
}
