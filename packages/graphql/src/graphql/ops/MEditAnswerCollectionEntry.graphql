mutation EditAnswerCollectionEntry(
  $id: Int!
  $value: String!
  $collectionId: Int!
) {
  editAnswerCollectionEntry(
    id: $id
    value: $value
    collectionId: $collectionId
  ) {
    id
    value
  }
}
