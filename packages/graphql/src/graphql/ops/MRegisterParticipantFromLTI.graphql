mutation RegisterParticipantFromLTI(
  $courseId: String!
  $participantId: String!
  $email: String!
) {
  registerParticipantFromLTI(
    courseId: $courseId
    participantId: $participantId
    email: $email
  ) {
    id
    participantToken
    participant {
      id
      isActive
      avatar
      username
    }
    participation {
      id
      isActive
    }
    course {
      id
      name
      displayName
    }
  }
}
