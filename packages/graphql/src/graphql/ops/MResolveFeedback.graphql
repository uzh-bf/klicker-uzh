mutation ResolveFeedback(
  $id: Int!
  $isResolved: Boolean!
  $liveQuizId: String!
) {
  resolveFeedback(id: $id, isResolved: $isResolved, liveQuizId: $liveQuizId) {
    id
    isPublished
    isPinned
    isResolved
    content
    votes
  }
}
