mutation ToggleIsArchived($elementIds: [Int!]!, $isArchived: Boolean!) {
  toggleIsArchived(elementIds: $elementIds, isArchived: $isArchived) {
    success
    partialSuccess
    failure
    elements {
      __typename
      id
      isArchived
    }
  }
}
