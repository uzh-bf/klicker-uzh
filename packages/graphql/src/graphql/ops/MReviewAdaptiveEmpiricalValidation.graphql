mutation ReviewAdaptiveEmpiricalValidation(
  $validationId: String!
  $decision: AdaptiveReviewDecision!
) {
  reviewAdaptiveEmpiricalValidation(
    validationId: $validationId
    decision: $decision
  ) {
    id
    status
  }
}
