query GetCourseActivityAnalyticsV2($courseId: String!) {
  getCourseActivityAnalyticsV2(courseId: $courseId) {
    isSuppressed
    effectiveN
    weeklyActivity {
      periodIndex
      effectiveN
    }
  }
}
