query GetBasicCourseInformation($courseId: String!) {
  basicCourseInformation(courseId: $courseId) {
    id
    displayName
    description
    color
    isCourseQAEnabled
    isCourseQAAnonymousEnabled
    owner {
      shortname
    }
  }
}
