mutation ModifyAnswerCollection(
  $id: Int!
  $name: String
  $access: ObjectAccess
  $description: String
  $catalogCollectionId: String
) {
  modifyAnswerCollection(
    id: $id
    name: $name
    access: $access
    description: $description
    catalogCollectionId: $catalogCollectionId
  ) {
    id
    name
    access
    description
    numSharedUsers
    entries {
      id
      value
    }
  }
}
