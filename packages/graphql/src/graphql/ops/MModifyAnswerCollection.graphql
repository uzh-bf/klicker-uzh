mutation ModifyAnswerCollection(
  $id: Int!
  $name: String
  $description: String
) {
  modifyAnswerCollection(id: $id, name: $name, description: $description) {
    id
    name
    description
    numSharedUsers
    entries {
      id
      value
    }
  }
}
