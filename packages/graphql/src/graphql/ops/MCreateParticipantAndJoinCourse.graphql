mutation CreateParticipantAndJoinCourse(
  $username: String!
  $password: String!
  $courseId: String!
  $pin: Int!
) {
  createParticipantAndJoinCourse(
    username: $username
    password: $password
    courseId: $courseId
    pin: $pin
  ) {
    id
    username
    avatar
  }
}
