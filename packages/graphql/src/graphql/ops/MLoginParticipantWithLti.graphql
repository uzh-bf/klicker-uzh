mutation LoginParticipantWithLti($signedLtiData: String!, $courseId: String) {
  loginParticipantWithLti(signedLtiData: $signedLtiData, courseId: $courseId) {
    participant {
      id
    }
    participantToken
  }
}
