mutation RequestCatalogCollection($catalogCollectionId: String!) {
  requestCatalogCollection(catalogCollectionId: $catalogCollectionId) {
    id
    name
    access
    ownerShortname
    isRequested
    isShared
    isOwner
    isOwnerOrAdmin
  }
}
