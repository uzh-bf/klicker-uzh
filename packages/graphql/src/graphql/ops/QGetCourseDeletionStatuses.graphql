query GetCourseDeletionStatuses($ids: [String!]!) {
  courseDeletionStatuses(ids: $ids) {
    id
    status
    courseId
    courseName
    errorType
    errorMessage
    createdAt
    updatedAt
  }
}
