query GetCourseDeletionStatuses($ids: [String!]!) {
  courseDeletionStatuses(ids: $ids) {
    id
    status
    isQueued
    courseId
    courseName
    errorType
    errorMessage
    createdAt
    updatedAt
  }
}
