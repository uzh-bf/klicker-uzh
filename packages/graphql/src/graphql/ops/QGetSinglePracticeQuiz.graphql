query GetSinglePracticeQuiz(
  $id: String!
  $withEscapeRoomHints: Boolean! = false
) {
  getSinglePracticeQuiz(id: $id) {
    ...PracticeQuizData
  }
  escapeRoomHints(practiceQuizId: $id) @include(if: $withEscapeRoomHints) {
    instanceId
    hint
  }
}
