query GetCourseOverviewData($courseId: ID!) {
  getCourseOverviewData(courseId: $courseId) {
    id
    participant {
      id
      avatar
      username
    }
    participation {
      id
      isActive
      points
    }
    course {
      id
      name
      displayName
      color
    }
  }
}
