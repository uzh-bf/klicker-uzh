query GetCourseOverviewData($courseId: ID!) {
  getCourseOverviewData(courseId: $courseId) {
    id
    participant {
      id
      avatar
      pseudonym
    }
    participation {
      id
      isActive
      points
    }
    course {
      id
      name
      displayName
    }
  }
}
