query QGetVerifiableCredential($token: String!) {
  assessmentReportVerification(token: $token) {
    status
    issuedAt
    snapshot {
      version
      subject {
        email
        source
      }
      course {
        name
        displayName
      }
      results {
        basePoints
        availableBasePoints
        correctnessPoints
        availableCorrectnessPoints
        bonusPoints
        availableBonusPoints
        totalPoints
        availableTotalPoints
      }
      comparison {
        cohortSize
        percentile
        histogram {
          binStart
          binEnd
          count
        }
      }
    }
  }
}
