query QGetVerifiableCredential($token: String!) {
  verifiableCredential(token: $token) {
    id
    token
    type
    metadata
    isRevoked
    issuedAt
    expiresAt
    course {
      id
      name
      displayName
    }
  }
}
