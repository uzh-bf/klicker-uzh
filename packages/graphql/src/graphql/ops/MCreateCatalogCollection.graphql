mutation CreateCatalogCollection($name: String!, $access: ObjectAccess!) {
  createCatalogCollection(name: $name, access: $access) {
    id
    name
    access
    ownerShortname
    isOwner
    isManager
    isEditor
    isRequested
    isShared
  }
}
