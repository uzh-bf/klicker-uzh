mutation CreateAnswerCollection(
  $name: String!
  $description: String!
  $answers: [String!]!
) {
  createAnswerCollection(
    name: $name
    description: $description
    answers: $answers
  ) {
    ...AnswerCollectionData
  }
}
