mutation CreateKbUrlResource(
  $kbId: ID!
  $url: String!
  $title: String!
  $materialType: KBResourceMaterialType
) {
  createKbUrlResource(
    kbId: $kbId
    url: $url
    title: $title
    materialType: $materialType
  ) {
    id
  }
}
