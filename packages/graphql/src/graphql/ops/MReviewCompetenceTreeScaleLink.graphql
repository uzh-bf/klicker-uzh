mutation ReviewCompetenceTreeScaleLink(
  $scaleLinkId: String!
  $decision: AdaptiveReviewDecision!
) {
  reviewCompetenceTreeScaleLink(
    scaleLinkId: $scaleLinkId
    decision: $decision
  ) {
    id
    status
  }
}
