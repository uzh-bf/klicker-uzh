query GetCourseActivityAnalytics($courseId: String!) {
  getCourseActivityAnalytics(courseId: $courseId) {
    name
    totalParticipants
    isSuppressed
    dailyActivity {
      date
      activeParticipants
    }
    weeklyActivity {
      date
      activeParticipants
    }
  }
}
