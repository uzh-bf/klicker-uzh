mutation UpdateQuestionInstances($questionId: Int!) {
  updateQuestionInstances(questionId: $questionId) {
    id
    questionData {
      id
      name
      type
      content
      pointsMultiplier
      type
    }
  }
}
