query GetAnswerCollectionsElements($templateId: String) {
  getAnswerCollectionsElements(templateId: $templateId) {
    id
    name
    isShared
    isEditor

    entries {
      id
      value
    }
  }
}
