query GetUserSessions {
  userSessions {
    id
    name
    displayName
    accessMode
    status
    createdAt
    blocks {
      id
      instances {
        id
        questionData {
          id
          name
          type
        }
      }
    }
    course {
      id
      name
      displayName
    }
  }
}
