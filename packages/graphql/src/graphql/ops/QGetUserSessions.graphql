query GetUserSessions {
  userSessions {
    id
    name
    displayName
    accessMode
    status
    createdAt
    blocks {
      id
      instances {
        id
        questionData {
          id
          name
        }
      }
    }
    course {
      name
      displayName
    }
  }
}
