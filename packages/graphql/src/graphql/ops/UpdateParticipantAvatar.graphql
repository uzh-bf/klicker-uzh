mutation UpdateParticipantAvatar(
  $avatar: String!
  $avatarSettings: AvatarSettingsInput!
) {
  updateParticipantAvatar(avatar: $avatar, avatarSettings: $avatarSettings) {
    avatar
    avatarSettings
  }
}
