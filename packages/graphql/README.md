# packages/graphql

## Development

To run all tests locally:
`pnpm test:local`

To run tests locally for a specific file:
`pnpm test:local access.test.ts`

To run tests locally in watch mode:
`pnpm test:local --watch access.test.ts`
