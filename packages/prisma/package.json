{
  "name": "@klicker-uzh/prisma",
  "version": "3.0.0-alpha.26",
  "license": "AGPL-3.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "analyze": "size-limit --why",
    "prebuild": "npm run generate",
    "build": "npm-run-all build:ts build:copy",
    "build:ts": "NODE_ENV=production tsup",
    "build:copy": "node src/copy.js",
    "build:watch": "NODE_ENV=development tsup --watch",
    "format": "prettier --write .",
    "generate": "prisma generate --schema=src/prisma/schema.prisma",
    "prisma:migrate": "doppler run --config dev -- prisma migrate dev --schema=src/prisma/schema.prisma",
    "prisma:deploy:raw": "prisma migrate deploy --schema=src/prisma/schema.prisma",
    "prisma:deploy": "doppler run --config dev -- npm run prisma:deploy:raw",
    "prisma:deploy:qa": "doppler run --config stg -- npm run prisma:deploy:raw",
    "prisma:deploy:prod": "doppler run --config prd -- npm run prisma:deploy:raw",
    "prisma:push:raw": "prisma db push --schema=src/prisma/schema.prisma",
    "prisma:push": "doppler run --config dev -- npm run prisma:push:raw",
    "prisma:push:qa": "doppler run --config stg -- npm run prisma:push:raw",
    "prisma:reset:raw": "prisma migrate reset --schema=src/prisma/schema.prisma --skip-seed",
    "prisma:reset": "doppler run --config dev -- npm run prisma:reset:raw",
    "prisma:reset:seed": "doppler run --config dev -- prisma migrate reset --schema=src/prisma/schema.prisma",
    "prisma:reset:qa": "doppler run --config stg -- npm run prisma:reset:raw",
    "prisma:studio:raw": "prisma studio --schema=src/prisma/schema.prisma",
    "prisma:studio": "doppler run --config dev -- npm run prisma:studio:raw",
    "prisma:studio:qa": "doppler run --config stg -- npm run prisma:studio:raw",
    "prisma:studio:prod": "doppler run --config prd -- npm run prisma:studio:raw",
    "preseed": "npm run build",
    "seed": "ENV=development doppler run --config dev -- npm-run-all seed:test",
    "seed:test": "ENV=development ts-node -r dotenv/config --esm --experimental-specifier-resolution=node src/data/seedTEST.ts",
    "seed:achievements": "ENV=development ts-node -r dotenv/config --esm --experimental-specifier-resolution=node src/seed/seedAchievements.ts",
    "seed:qa": "ENV=development doppler run --config stg -- npm-run-all seed:qa:test",
    "seed:qa:test": "ts-node --esm --experimental-specifier-resolution=node src/data/seedTEST.ts",
    "seed:prod": "ENV=production doppler run --config prd -- npm-run-all seed:prod:bf1 seed:prod:ami",
    "seed:prod:bf1": "npm run seed:qa:bf1",
    "seed:prod:ami": "npm run seed:qa:ami",
    "seed:prod:achievements": "ENV=production ts-node --esm --experimental-specifier-resolution=node src/seed/seedAchievements.ts",
    "size": "size-limit"
  },
  "prisma": {
    "seed": "npm run seed"
  },
  "engines": {
    "node": ">=18"
  },
  "size-limit": [
    {
      "path": "dist/index.js",
      "limit": "10 KB"
    }
  ],
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "9.3.0",
    "@pothos/plugin-prisma": "3.41.0",
    "@size-limit/preset-small-lib": "8.2.4",
    "@tsconfig/node18": "^1.0.1",
    "@tsconfig/recommended": "^1.0.2",
    "@types/bcryptjs": "^2.4.2",
    "@types/ramda": "^0.28.15",
    "bcryptjs": "2.4.3",
    "dotenv": "16.0.3",
    "dotenv-cli": "7.0.0",
    "npm-run-all": "4.1.5",
    "prisma": "4.8.1",
    "prisma-erd-generator": "1.2.5",
    "ramda": "0.28.0",
    "size-limit": "8.2.4",
    "ts-node": "10.9.1",
    "tsup": "6.6.2",
    "typescript": "4.9.5"
  },
  "dependencies": {
    "@prisma/client": "4.8.1"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
