{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "format": {},
    "format:check": {
      "outputMode": "errors-only"
    },
    "lint": {
      "outputMode": "errors-only"
    },
    "check": {
      "outputMode": "errors-only"
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "build:test": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false,
      "outputs": ["dist/**", ".next/**"],
      "dependsOn": ["@klicker-uzh/prisma#build", "@klicker-uzh/markdown#build"],
      "persistent": true
    },
    "publish": {
      "cache": false,
      "dependsOn": ["build"]
    },
    "start": {
      "dependsOn": ["^build", "build"],
      "outputs": ["dist/**", ".next/**"],
      "persistent": true
    },
    "start:test": {
      "dependsOn": ["^build:test", "build:test"],
      "outputs": ["dist/**", ".next/**"],
      "persistent": true
    }
  }
}
