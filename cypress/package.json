{
  "private": true,
  "name": "@klicker-uzh/cypress",
  "devDependencies": {
    "@cypress/code-coverage": "~3.13.11",
    "@klicker-uzh/prisma": "workspace:*",
    "@testing-library/cypress": "~10.0.3",
    "@types/node": "^20.16.1",
    "@types/uuid": "^10.0.0",
    "cypress": "~14.0.2",
    "cypress-real-events": "~1.14.0",
    "jose": "~5.9.4",
    "localforage": "~1.10.0",
    "typescript": "~5.6.3",
    "uuid": "~10.0.0"
  },
  "scripts": {
    "test": "doppler run --config=dev_cypress -- cypress open",
    "test:run": "doppler run --config=dev_cypress -- cypress run",
    "test:run:one": "doppler run --config=dev_cypress -- cypress run --spec",
    "test:watch": "doppler run --config=dev_cypress -- cypress open --watch"
  },
  "engines": {
    "node": "=20"
  },
  "volta": {
    "extends": "../package.json"
  }
}
