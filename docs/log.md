# Log

## 2026-07-22

- **Fix**: Import/export rollout now invalidates active element fingerprints once before historical media classification without changing authored versions or modification timestamps, binds resume state to a privacy-safe database identity, verifies the invariant from one database snapshot, indexes both stale-version and active-null repair predicates, and makes export honor the persisted media SHA-256 classification fail-closed.
- **Fix**: Repeated media-dependent fingerprint invalidations now use a transaction-unique 64-hex dirty token and null didactic version, preventing an ABA race from satisfying an in-flight optimistic fingerprint write without changing authored element metadata.
- **Fix**: Authored elements now lock and reject unresolved first-party media before fingerprint persistence; a one-shot pending marker lets normal direct-upload finalization avoid an element scan, while unmarked historical transitions and verified import-staging repairs retain targeted fingerprint-only invalidation and synchronous refresh.
- **Fix**: Supported development, staging, Playwright-CI, and production-flashcard seeds now mark directly updated resources stale and finish with a target-inheriting GraphQL repair check under the rollout advisory lock. The repair is capped at ten passes/four minutes and fails closed to the guarded rollout backfill if unrelated historical backlog prevents the active-resource invariant from closing.
- **Fix**: Direct media upload now uses an explicit finalization protocol while retaining legacy upload and `UserProfile` persisted operations for dark rolling deployments. Enabled servers reject stale upload clients before side effects; finalization enforces the shared 256 MiB absolute limit from Azure metadata, retains zero/unknown/oversized attempts for bounded one-hour cleanup, and permits current/null 5–256 MiB media only as deterministic export omissions.
- **Fix**: Abandoned direct uploads are hidden from library/authoring/fingerprint/export paths and hourly cleanup claims each exact owner-bound Azure target before blob/row deletion, retaining durable retry state on indeterminate storage failures.
- **Fix**: The published `20260716085603_import_export_fingerprint_repair_indexes` migration remains byte-for-byte immutable; active/null-fingerprint partial indexes now ship in registered follow-up migration `20260722100000_import_export_null_fingerprint_repair_indexes` with exact-definition validation and production checksum/runbook coverage.
- **Update**: [import-export-packages](./import-export-packages.md), [import-export-production-runbook](./import-export-production-runbook.md), [domain-model](./domain-model.md), [data-and-migrations](./data-and-migrations.md), [graphql-api-layer](./graphql-api-layer.md), [async-and-workers](./async-and-workers.md), [ci-and-deployment](./ci-and-deployment.md), and [testing](./testing.md) now document the didactic-v2/media-v1 split, synchronous active-resource fingerprint invariant, pinned-session rollout mutex, single-`DATABASE_URL` operations boundary, guarded local media → didactic → verify runner, protected resume and recovery context, full active-row rollout rescan, seed bootstrap, explicit readiness gate, old-image drain requirement, and deliberately separate follow-up database constraint.
- **Update**: [ci-and-deployment](./ci-and-deployment.md) and the [import-export production runbook](./import-export-production-runbook.md) now require backend-first two-phase deployment for the finalization-aware Manage upload protocol and previous-Manage-first rollback; a single Helm upgrade changing both images is an explicit stop condition.

## 2026-07-17

- **Fix**: Direct-media upload now declares the accepted PNG, JPEG, and GIF MIME types correctly and gives localized feedback for unsupported files before any upload target is requested.
- **Fix**: The import commit phase now guards native unload and Pages Router history-pop navigation in addition to hiding modal close controls, preventing refresh, tab close, or browser Back from silently abandoning a mutation with an unknown durable outcome.
- **Fix**: Direct-media finalization now recovers when rollout backfill classified a not-yet-visible SAS upload as a version-1 omission; current/null rows are rechecked, the real hash is installed with an exact compare-and-set, and omission-based element fingerprints are invalidated once without crossing owner or future-version boundaries.
- **Fix**: Direct media upload now retries fingerprint finalization with the same media ID under a bounded three-attempt policy, and feature-enabled media-library reads hide unresolved rows so a reload cannot insert pending media into authored content; focused frontend, GraphQL, and Playwright regressions cover recovery and exhaustion.
- **Fix**: Import/export durable-state migration correction now rejects `READY` package artifacts without final bytes or SHA-256 and `COMPLETE` receipts without a retention deadline before sealing the exact CHECK expressions.
- **Fix**: Export preview now observes its renewable Redis lease throughout snapshot and bounded media-metadata work, so lost ownership cannot return a successful preview or bypass configured live-work isolation; the metadata pool stops dequeuing on failure and settles already-started reads before releasing that lease.
- **Fix**: Direct media uploads now stay within Azure's 256 MiB single-request boundary for create-only SAS tokens and reject oversized images locally before requesting upload credentials or finalization.

## 2026-07-16

- **Fix**: Import/export browser errors no longer expose raw GraphQL messages or extensions, answer-collection review panels retain their pointer-accessible layout, every bounded preview/list scroller is keyboard-accessible, and cancellation/viewport Playwright cases track their real request and scroll boundaries; Playwright CI now enables the feature during both build and execution.
- **Refactor**: The generic file dropzone now has a neutral common owner, import preview conversion derives its model from the generated operation instead of a React component, and answer-collection pagination clamps safely when replacement data shrinks under a reused collection reference.
- **Fix**: Import commit remains non-dismissible, while its best-effort post-commit list refresh can now be closed safely; generation guards suppress every late dispatch, toast, and close continuation, and successful validation focuses the copyright/psychometric disclosures before the selectable rows.
- **Fix**: Direct-media fingerprint finalization atomically marks only the owner's affected null-fingerprint elements with a constraint-valid null marker, increments their versions to defeat concurrent pre-finalization writes, and skips the bump on idempotent retries; element authoring and batch writes now enqueue best-effort refresh without reporting committed edits as failed.
- **Test**: The import/export Playwright groups now have independent spec entry points with per-spec cleanup, upload leases are included in isolated teardown, and runner-sensitive backend resource measurements are evidence rather than hard CI gates.
- **Fix**: Element export now fails closed when its package preview is missing, cancels link/blob work when the modal closes, discloses excluded psychometric history in English and German, and keeps warning/retry/pagination state accessible and scoped to the current preview; Playwright covers these boundaries and records large-review timing as evidence instead of CI-load-sensitive gates.
- **Fix**: Import/export fingerprint refresh, scheduled repair, and cleanup now declare explicit Hatchet execution timeouts and shorter cooperative work budgets; they use durable markers/backlog plus the next continuation or cron instead of immediate retries, while single-flight repair/cleanup policies reject duplicate cron runs and cancellation remains distinct from a successful budget stop.
- **Fix**: Export-snapshot SQL now schema-qualifies every application relation as `public`, independent of the connection's `search_path`.
- **Refactor**: Import/export production database operations now separate runtime connection/locking, static schema contracts, catalog inspection, and recovery-only counting instead of combining them in one 1,000-line module.
- **Fix**: Package uploads now acquire renewable one-per-user/four-global concurrency leases before claim, body consumption, and storage writes; exact-length reads use one bounded allocation instead of retaining all chunks and concatenating a second full package buffer.
- **Fix**: Production inspection rejects active failed/duplicate migration attempts, feature raw SQL targets the public schema explicitly, and rollback retains the maintenance-capable worker until record-scoped cleanup is complete.
- **Fix**: Import/export preserves the current free-text Unicode grading semantics, rejects oversized case-study options before canonicalization, and rejects zero-byte media consistently across manifest, parser, staging, and export boundaries.
- **Fix**: Linked-element fingerprint invalidation and refresh now use an explicit answer-collection/deletion/ID index; the executable database inspection, migration checksum, large-target concurrent pre-create contract, analytics schema mirror, and production runbook include the complete repair/invalidation index set.
- **Fix**: Import/export production inspection now fails closed on incompatible column/index/constraint shapes, missing or disabled ownership and immutability triggers, and invalid migration step counts; the follow-up index migration NULL-safely revalidates all seven concurrently pre-creatable indexes and independently verifies each canonical CHECK expression before sealing it, without coupling the seal to `NOT VALID` state.
- **Fix**: GraphQL and Playwright CI wait for the general Hatchet worker's registered `/ready` contract before starting import/export-dependent tests.
- **Fix**: Name, status, and tag-only edits preserve omitted legacy shared fields without revalidating them; newly supplied shared fields, creations, and explicit option edits retain strict canonical validation.
- **Refactor**: Package element discrimination now survives execution planning and GraphQL preview resolution, including typed case-study ref-to-ID mapping; normal element writes consume one typed mutation-preparation plan instead of repeating canonicalization and relation assembly.
- **Refactor**: Media upload SAS creation and server-side upload finalization now share one feature-agnostic GraphQL schema owner; import/export error translation remains local to feature components rather than the global Apollo link.
- **Refactor**: Element import/export now owns its Pothos types, root registrations, and boundary guards in one feature schema module instead of scattering them through the legacy element/query/mutation files.
- **Refactor**: Signed package capabilities and import tokens now share one strict, purpose-separated canonical-payload codec while retaining byte-compatible wire formats and caller-specific policy.
- **Refactor**: The media-reference facade now delegates CommonMark rewriting, typed element traversal, aggregate work budgets, and shared contracts to focused modules without changing its public API.
- **Refactor**: Foreground direct-media finalization now lives in the neutral fingerprint service rather than the scheduled maintenance module.
- **Refactor**: The import/export REST adapter now passes only its required Prisma, Redis, and authenticated-user capabilities into package services, and validates binary upload chunks without unsafe casts.
- **Fix**: Import/export now configures only its dedicated upload-body and storage-operation deadlines; feature configuration no longer changes the global Node HTTP server timeouts for unrelated backend requests.
- **Update**: [import-export-packages](./import-export-packages.md) and [frontend-conventions](./frontend-conventions.md) now document the import preview's non-submitting native-form boundary, accessible focus/state behavior, 44-pixel selection targets, and durable media-finalization success with best-effort library refresh.
- **Update**: [import-export-packages](./import-export-packages.md), [graphql-api-layer](./graphql-api-layer.md), [async-and-workers](./async-and-workers.md), [data-and-migrations](./data-and-migrations.md), and [import-export-production-runbook](./import-export-production-runbook.md) now document authenticated create-only direct-upload finalization with href-source storage dispatch, rollout null/current fingerprint revisits versus version-only scheduled repair, fresh backlog rechecks, and the repair-index migration's checksum and large-target concurrent pre-create verification.
- **Update**: [import-export-packages](./import-export-packages.md), [graphql-api-layer](./graphql-api-layer.md), [import-export-error-contract](./import-export-error-contract.md), [async-and-workers](./async-and-workers.md), [ci-and-deployment](./ci-and-deployment.md), and [import-export-production-runbook](./import-export-production-runbook.md) now point to the split schema/tests and distinguish immediate fingerprint refresh, bounded 15-minute repair, and rollout backfill, including required worker allowlisting, processed-unfingerprintable null/version semantics, and privacy-safe count/backlog observability.

## 2026-07-15

- **Update**: [import-export-packages](./import-export-packages.md), [import-export-production-runbook](./import-export-production-runbook.md), and [testing](./testing.md) now document renewable PostgreSQL receipt fencing, independent import concurrency (one per user and four globally by default), completed-replay bypass, and the focused persistence/snapshot/export/database regression suites.
- **Update**: [import-export-packages](./import-export-packages.md), [frontend-conventions](./frontend-conventions.md), and [import-export-error-contract](./import-export-error-contract.md) now attribute import transport, cancellation, error translation, commit, and post-commit refresh to `useElementImportWorkflow`; `UploadModal` remains the modal shell and phase renderer.
- **Update**: [import-export-packages](./import-export-packages.md) now documents the dedicated per-user and global validation concurrency limits, the fail-closed Redis lease applied to package validation, and compatibility behavior for partial edits of non-portable legacy elements.

## 2026-07-14

- **Creation**: [import-export-packages](./import-export-packages.md), [import-export-error-contract](./import-export-error-contract.md), and [import-export-production-runbook](./import-export-production-runbook.md), indexed from [index](./index.md), document the final version-3 package boundary, server-owned error and telemetry contract, and operator-controlled dark migration, backfill, canary, rotation, and rollback sequence. Tags and psychometric history remain excluded; target owners, protected evidence, and soak decisions remain external release blockers.
- **Update**: [architecture-overview](./architecture-overview.md), [domain-model](./domain-model.md), [graphql-api-layer](./graphql-api-layer.md), [frontend-conventions](./frontend-conventions.md), [async-and-workers](./async-and-workers.md), [data-and-migrations](./data-and-migrations.md), [testing](./testing.md), and [ci-and-deployment](./ci-and-deployment.md) now reflect shared all-nine element canonicalization, durable artifact/receipt/media staging, private package storage, CLI-only historical backfills, deployed refresh/cleanup maintenance, fail-closed runtime gates, current Playwright and app-unit CI ownership, and reviewed v3 deployment requirements.

## 2026-07-07

- **Update**: migration-in-flight banners added to [graphql-api-layer](./graphql-api-layer.md), [architecture-overview](./architecture-overview.md) (GraphQL→tRPC, PR #5132), and [chat-platform](./chat-platform.md) (AI-SDK→Mastra, PRs #5126/#5129) — pages stay authoritative until those PRs merge.

- **Update**: [testing](./testing.md) and [index](./index.md) reframed for the Cypress→Playwright switch — Playwright is the primary suite for new specs, Cypress is a frozen legacy suite pending removal (both still run in CI). Matching routing updates in the `klicker-testing-verification` and `klicker-cypress-e2e` skills. Migration roadmap: `project/plans_future/2026-07-07-wiki-skills-migration-roadmap.md`.

- **Update**: [index](./index.md) skill-routing section expanded with the seven new `klicker-*` skills.

- **Creation**: frontend + ops pages — [frontend-conventions](./frontend-conventions.md), [chat-platform](./chat-platform.md), [testing](./testing.md), [ci-and-deployment](./ci-and-deployment.md), [developing-a-feature](./developing-a-feature.md). Absorbed the remaining `project/CODEBASE_NOTES.md` sections; Playwright authoring/CI gotchas moved to the `klicker-playwright-e2e` skill.

- **Creation**: backend pages — [domain-model](./domain-model.md), [graphql-api-layer](./graphql-api-layer.md), [data-and-migrations](./data-and-migrations.md), [async-and-workers](./async-and-workers.md), [auth-model](./auth-model.md). Absorbed the GraphQL/data, export-package, and LTI entries from `project/CODEBASE_NOTES.md`.

- **Creation**: initial bundle — [index](./index.md), [getting-started](./getting-started.md), [architecture-overview](./architecture-overview.md). Evidence base: `project/docs/WIKI_BOOTSTRAP_BRINGUP.md` (executed bring-up) and `project/docs/WIKI_BOOTSTRAP_INVENTORY.md` (repo archaeology).
