# Assessment participant invitation screenshots

These screenshots were captured from the real local lecturer interface. Seeded
participant email addresses and matriculation numbers were replaced in the
browser DOM with clearly synthetic placeholders before each image was saved, so
no participant identifiers are published in the repository.

The CSV guardrail update is shown in:

- `desktop-csv-guardrails.png` — English warning, template action, partial
  import result, and deletable pending invitation.
- `mobile-csv-guardrails-de.png` — German warning, template action, and
  inconsistent-row validation at a mobile viewport.
