query QuestionList {
  questions: allQuestions {
    id
    title
    type
    instances {
      id
      version
      createdAt
      session
    }
    tags {
      id
      name
    }
    versions {
      id
      description
      createdAt
    }
    createdAt
    updatedAt
  }
}
