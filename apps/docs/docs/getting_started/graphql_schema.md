# GraphQL Schema

The public schema definition for the KlickerUZH API is generated code-first using Pothos in the `@klicker-uzh/graphql` package.

You can download or view the raw schema file at:

- [schema.graphql](/schema.graphql)
