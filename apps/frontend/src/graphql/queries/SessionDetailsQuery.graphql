query SessionDetails($id: ID!) {
  session(id: $id) {
    id
    name
    blocks {
      id
      randomSelection
      instances {
        version
        question {
          id
          title
          type
        }
      }
    }
  }
}
