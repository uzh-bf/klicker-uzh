// TODO: join page should show the list of available running sessions of an account and link to them
// TODO: if only one session is active, redirect to the session directly
// TODO: if no session is active, show a message that no session is active
// TODO: use smart regeneration or caching, server-side redirect

function Join() {

}

export default Join
