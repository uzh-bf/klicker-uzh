generator client {
  provider             = "prisma-client-py"
  interface            = "sync"
  recursive_type_depth = 5
  previewFeatures      = ["fullTextSearch", "fullTextIndex", "postgresqlExtensions", "prismaSchemaFolder"]
}
