function CatalogSeparatorTitle({ title }: { title: string }) {
  return (
    <div className="mt-3 rounded-t border-b border-slate-200 bg-slate-100 px-3 py-1 text-xs font-semibold uppercase text-slate-500">
      {title}
    </div>
  )
}

export default CatalogSeparatorTitle
