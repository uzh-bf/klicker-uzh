import React from 'react'

interface HistogramProps {
  data: any
}

const defaultValues = {}

function Histogram({ data }: HistogramProps): React.ReactElement {
  return <div>Histogram</div>
}

export default Histogram
