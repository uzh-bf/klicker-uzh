export const MAX_BONUS_POINTS = 45 // maximum 45 bonus points for fastest answer
export const TIME_TO_ZERO_BONUS = 20 // seconds until the bonus points are zero
export const DEFAULT_POINTS = 10 // points a participant gets for participating in a poll
export const DEFAULT_CORRECT_POINTS = 5 // points a participant gets for answering correctly (independent of time)
