/**
 * Client-safe tool-name constants shared between MCP service modules and
 * client components. Keep this module free of server-only imports so the
 * client bundle never reaches the server logger graph.
 */
export const STUDENT_PRACTICE_QUIZ_TOOL_NAME = 'start_student_practice_quiz'
