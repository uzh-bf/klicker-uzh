query SessionDetails($id: ID!) {
  session(id: $id) {
    id
    name
    blocks {
      id
      instances {
        version
        question {
          id
          title
          type
        }
      }
    }
  }
}
