subscription FeedbackAdded($sessionId: ID!) {
  feedbackAdded(sessionId: $sessionId) {
    id
    content
    votes
    pinned
    resolved
    createdAt
    resolvedAt
    responses {
      id
      content
      createdAt
    }
    createdAt
  }
}
