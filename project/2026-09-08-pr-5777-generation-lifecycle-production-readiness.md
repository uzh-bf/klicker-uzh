# Production readiness — PR 5777 (generation lifecycle contracts)

Scope: pull request [uzh-bf/klicker-uzh#5777](https://github.com/uzh-bf/klicker-uzh/pull/5777), branch `rs/generation-lifecycle-contracts` → `v3-ai`. Head `1c8ff98696924f1f53a3fa9177e16076c66d744a`, merge base `df1ea25580136bf1dc70b05dc8e18a633374b371`. 25 files, +2685/−424. Draft, `mergeStateStatus: UNSTABLE`.

Audit performed under `$rs-production-readiness`: one frontier orchestrator plus eight read-only dimension workers at xhigh effort, then one verifier per candidate blocker. Nothing in this audit authorises merging, leaving draft, force-pushing, deploying, or provider activation.

## Assessment

**Refactor acceptance: supported with concerns. Production deployment readiness: not established.**

Static analysis supports that `completeElementGeneration` and `elementGenerationLease` are behaviour-preserving extractions of the two deleted inline blocks — identical predicates and write sequences, the same 15-second constant, and the same acquire predicate at all five call sites — with no migration, schema, SDL, or new configuration change. This is code-refactor acceptance evidence only. It does not establish hosted deployment readiness, CI gating, live runtime health, or deployment rollback completeness. A source revert is an ordinary code rollback path; deployment rollback still depends on the release system, image selection, migrations, and runtime health.

The audit findings below describe the original head. Subsequent owner verification now passes all nine focused browser cases, including generation editing, analytics allow/deny, the loading status before profile failure, and closed AI routes. The dynamic-route 404s cleared after the existing guarded Manage cache repair and canonical restart; no product-route or authorization change was needed. Fixed-label synchronization logs and the analytics loading status are implemented. Two preload tests, 31 lifecycle tests, all 47 type/lint tasks, and the repository policy checks pass. Hosted execution and independent review of the corrective extension remain pending.

### Remaining readiness items

- **Hosted qualification:** execute the corrected lecturer-MCP setup and smoke steps, and the affected Playwright jobs, on the published corrective head. Local passes do not replace hosted evidence.
- **Lease timing and failure handling:** the inherited concerns remain outside this behavior-preserving refactor. Manifest size metadata and synthetic benchmarks cannot establish production timing or settle lease expiry and takeover.
- **CI policy:** required-check configuration remains a separate repository-policy decision. Historical branch API responses do not establish the effective gate without accounting for rulesets.
- **Release qualification:** environment-specific deployment, migration, rollback and live-health evidence is still required before production activation. This report authorizes none of those actions.

## Prior gates

| gate | artifact | status |
| --- | --- | --- |
| `final-reviewer` (integrated outcome) | `project/_local/reviews/2026-09-08-generation-integrated-final.md` | **present** — reviewed `df1ea25580..e9641fbc900cbfdd76b051ba6df0c7245c760f96`, pass with no findings, all 25 changed paths, no exclusions. The single commit since (`1c8ff98696`) edits only the plan's Progress section, so the reviewed code is identical to head. |
| `slice-reviewer` (retained fixture) | `project/_local/reviews/2026-09-08-retained-fixture-slice-review.md` | present — `a4c8333acc..96bbe231be`, DONE_WITH_CONCERNS, no correctness/ownership/transaction/authentication defect. |
| `slice-reviewer` (target integration) | `project/_local/reviews/2026-09-07-target-integration-slice-review.md` | present — `7563dc45b0..2ddf4a380f`, all 97 paths, DONE_WITH_CONCERNS, no merge-resolution defect. |
| `slice-reviewer` (local MCP fixture) | `project/_local/reviews/2026-09-07-local-mcp-fixture-slice-review.md` | present |
| `slice-reviewer` (v3 integration) | `project/_local/reviews/2026-09-07-v3-integration-slice-review.md` | present |
| `simplifier` (retained fixture, local MCP fixture, v3 integration) | three artifacts dated 2026-09-07/08 | present — all DONE, no justified net simplification. |
| `$code-review` | — | missing. Not a stop; the `final-reviewer` pass covers standards and frozen-plan compliance for the integrated outcome. |
| `$security-review` | — | not applicable. No authentication, authorisation, trust-boundary, secret-handling or new-data-flow change: the diff adds four `process.env` reads of pre-existing names, and `gitleaks detect --log-opts df1ea2558..HEAD` over 18 commits reports no leaks. The one adjacent item (raw fixture `INSERT`s without the `requireDisposableDatabase` marker) is recorded as a handoff. |
| `$thermo-nuclear-code-quality-review` | — | not applicable. Behaviour-preserving extraction; maintainability items are recorded as handoffs. |

## Findings

The table below preserves the historical audit snapshot at the recorded head. Post-audit owner updates do not convert static analysis into reproduced deployment evidence or re-score these findings.

No finding in this audit is a blocker. Severity is the orchestrator's, not the worker's, and every row below is inherited from the merge base unless stated otherwise.

| severity | dimension | finding | evidence | proposed action | verification |
| --- | --- | --- | --- | --- | --- |
| inherited risk | Failure modes | **A stale synchronization request may replace a terminal status.** | The audit found a status-free polling claim and catch-side writes fenced by lease ownership. This was a static trace, not a reproduced incident. | Preserve this refactor's contract; evaluate terminal-state protection in a separately approved change with the generation owner. | Reachability and user impact remain unverified. Existing drafts and artifacts are not shown to be deleted. |
| inherited risk | Failure classification | **Some infrastructure errors may be recorded as non-retryable artifact failures.** | The synchronization catch-all maps unclassified errors to ARTIFACT_INVALID. The audit traced this mapping statically; it did not reproduce an infrastructure incident. | Treat retry classification and accounting consequences as a separate generation-owner decision; changing them exceeds this behavior-preserving extraction. | No leaked reservation was established. Spend settlement occurs earlier, so claims about refunds or recovery require the accounting contract. |
| inherited risk | Failure modes and performance | **Artifact verification may outlast the fixed lease.** | The audit found a 15-second lease and potentially substantial artifact verification before completion. It did not measure representative wall-clock duration or establish sustained contention. | Preserve the frozen lease contract here. The generation owner may evaluate duration and bounded lease hardening separately. | A manifest size or synthetic benchmark alone cannot confirm or retire the risk; network, parsing, scheduling and concurrent behavior matter. |
| major | Failure modes / Observability | **The browser poller is the only thing that advances a build; no server-side sweeper exists.** No Hatchet task calls `completeElementGeneration` or the lease primitives, and no cron touches generation. A closed tab leaves a build non-terminal indefinitely. This is repository-inconsistent: the same documentation page describes server-side reconciliation for every comparable lifecycle. | `packages/hatchet/src` and `apps/hatchet-worker-general/src` contain exactly three `elementGenerationBuild` references, all in `kbMaintenance.ts:806,876,923` (artifact-prefix purge). No cron in `packages/hatchet/src/index.ts:363-470` references it, while `monitor-kb-graph-builds` at `:438-449` runs `onCrons: ['* * * * *']`. Contrast `sweep-stale-course-duplications` at `:515-516`, added specifically "so recovery no longer depends on a user polling" (`docs/async-and-workers.md:50`). `findOwnedBuild` filters by `ctx.user.sub`, so there is no operator read path either. The index already exists: `knowledge.prisma:417` `@@index([status, syncLeaseUntil])`. | Roadmap item: a bounded stale-build sweep mirroring `sweep-stale-course-duplications`, selecting non-terminal builds with an expired lease and no recent progress, reconciling against the provider run before declaring failure. | **confirmed by convergence** — three workers established it independently by exhaustive search of the Hatchet task registry and cron catalogue. The associated claim that each stranded build holds a `RESERVED` spend row with no release path is **refuted** (see the refuted row below). |
| major | Observability | **Terminal synchronization errors lack a dedicated diagnostic event at the audited head.** | The two synchronization catch blocks persist a failure after applicable early returns. The audit traced these paths statically; it did not inspect all production log collection. | Add one fixed event label per catch. No raw error, message, stack, content, URL, identifier or token. | Candidate adds both labels; no live production observability claim. |
| follow-up | Observability | **Lease contention has limited diagnostics.** | Acquire and release outcomes are largely control flow. A false release does not identify the cause or prove takeover. | Consider a bounded future telemetry design; do not add misleading takeover warnings or change lease behavior in this refactor. | Static observation only; contention frequency is unmeasured. |
| minor | Data safety / Future design | **The idempotent completion contract preserves a pre-existing draft with divergent content when its unique key matches.** `createMany({ skipDuplicates: true })` retains the existing row, and the cardinality gate counts rows rather than comparing content. The audit did not reproduce a supported path that creates this state and does not claim that the state is unreachable. The current test records the intended idempotent behavior frozen by the plan; this is a future design question rather than a justified fix for this refactor. | `elementGenerationCompletion.ts:181-194`; unique key `@@unique([buildId, sourceElementId, duplicationIndex])` at `knowledge.prisma:509` and `migration.sql:176-177`. The fixture builds the divergent case at `elementGenerationCompletion.integration.test.ts:198-205` (`name: 'Previously edited'`, `revision: 3`) and `:433-450` asserts that the existing row survives and the build completes. The completion and parser guards limit the supported re-entry paths, but do not establish that the state is impossible in every future workflow. | Preserve `skipDuplicates: true` and the current test contract for this behavior-preserving refactor. Future design work may decide whether completion should reconcile content as well as cardinality, but that decision is outside this report. | Static analysis only; no end-to-end reproduction of the divergent-draft state. |
| major | Deploy & rollback | **`test-mcp-lecturer.yml` was left behind by the disposable-database guard rollout, so its smoke tests have not run since 2026-09-07.** Root cause is one commit: `7c73ed231c` (#5799) added the guard plus provisioning to `test-graphql.yml` and `test-unit.yml` and missed this workflow. Whether the job runs is decided by its path filter, not the event type. | `.github/workflows/test-mcp-lecturer.yml:88` `DATABASE_URL: postgresql://klicker:klicker@localhost:5432/klicker-prod`, rejected by `disposableDatabase.ts:30` (`decodeURIComponent(url.username) !== 'klicker_test'`). Last success 2026-09-07T14:49Z (run `34135045990`), first failure 16:54Z (run `34145352308`), guard landed 15:52Z. Across a 40-run window the job is only ever skipped (20), failure (5), cancelled (2), or success from before the guard (12) — so server boot, `smoke:local` and `smoke:negative` have not executed at all. The unit-test step at `:81` still runs because it precedes database setup. | Two parts, both required: add a provisioning step before "Setup database" mirroring `test-graphql.yml:137-140`, and change all three `DATABASE_URL` values (lines 88, 93, 140) to the `klicker_test` login. Line 140 matters as much as the others — after provisioning, `klicker-prod` still exists but stays unmigrated. The fixing PR self-verifies, because the workflow path is inside its own line-25 filter. | unverified. Not verified: whether the smoke tests pass once the database is provisioned correctly — there may be a second failure behind the first. |
| major | Deploy & rollback | **The historical audit did not establish a required status-check gate for either target branch.** The branch-protection responses showed no required contexts in the queried endpoint, but the effective gate cannot be concluded without a complete ruleset audit. | Historical queries: `gh api repos/uzh-bf/klicker-uzh/branches/v3-ai/protection/required_status_checks` → `404 Branch not protected`; the same call for `v3` → `[]` (protected, zero required contexts). The recorded ruleset query was partial and is insufficient to determine effective enforcement. `test-mcp-lecturer.yml:145-146` says the job can be used as a required check in branch protection. | Resolve required-check policy with its separate repository-policy owner. This report does not change policy or assert whether CI is advisory. | **Historically confirmed only for the queried branch-protection endpoint.** Effective ruleset behavior and intent were not fully audited. |
| qualification | End-to-end coverage | **Hosted generation-editor coverage failed at the audited head and target.** | The failure occurred at unavailable account access before the editor, not at completion persistence. The repaired fixture has a passing focused local editor run. | Finish hosted qualification after scoped repairs and review. | Local editor pass does not replace current-head hosted CI or full provider-backed lifecycle proof. |
| minor | UX | **The analytics route's loading state lost its accessible name and its visible text — the audit's one genuinely change-introduced defect.** While feature flags are settling, the route now shows an unlabelled spinner with no text, where it previously showed the localised wait string beside the spinner. Screen-reader users get nothing announced; sighted users get no statement of what is loading. | `LearningAnalyticsRouteGuard.tsx:24-30` renders only `<div className="flex h-full w-full items-center justify-center"><Loader /></div>`, and `packages/shared-components/src/Loader.tsx:12-19` is a single `FontAwesomeIcon` with `animate-spin` in a plain `div` — no `role="status"`, no `aria-label`, no text node. The replaced `AnalyticsLoadingView.tsx:17-19` rendered the translated `manage.analytics.analyticsLoadingWait` as a sibling of the loader; that file is unchanged by this PR, so the text still exists but this route no longer reaches it. Observed: a request to `/analytics` on the running instance returned HTTP 200 whose markup contains the guard wrapper class and `animate-spin`, zero occurrences of `data-cy="navigation"`, and no `<title>` element — so the first painted state is exactly this unlabelled spinner. | Add `role="status"` and the localised wait text (or an `aria-label`) to the guard's own wrapper div. Do not put it on the shared `Loader`, and do not re-mount `Layout` — avoiding `Layout` is the stated point of the change. | unverified. Confidence 100, and **change-introduced** rather than inherited. Judged against `$web-design-guidelines`, which delegates to the Vercel Web Interface Guidelines; the worker fetched those and applied the async-update `aria-live` and loading-state rules verbatim. |
| minor | UX | **The chrome-less window before the analytics shell is longer than before, and the content jumps when it ends.** The pre-shell period now also covers feature-flag settling, not just profile loading. | `flagsReady` requires `initializationSettled` (`packages/feature-flags/src/react.tsx:113-119`), gated by `growthbook.init({ timeout: config.timeoutMs ?? 2000 })` (`browserClient.ts:44`) — an upper bound of about 2 seconds beyond the profile query. `Layout.tsx` is unchanged and its pre-profile branch (`:56-62`) renders the identical bare `Loader`, so the very first paint was already chrome-less; what is new is that the chrome-less state persists through flag settling. Observed hydrated end state at 1280x900 as `lecturer`: header present, `document.title` `Learning Analytics`, body text "Learning analytics are not available for your account yet." A DOM probe measured the guard wrapper at 1280x790 at y=110 against a 900px viewport, so `h-full` resolves and the spinner is genuinely centred — the shift is viewport-centred to header-offset, not a broken box. | Accept as an intentional trade-off, or reserve the shell by rendering the wait text in the guard — the same edit as the row above. | unverified. Bounded and self-healing; no failure state. The stated motive ("Resolve profile availability before mounting Layout's login redirect") is the code comment's claim and was not independently verified. |
| minor | Failure modes / Data safety | **Draft-count scope diverges between the two completion variants.** Questions count `{ buildId, duplicationIndex: 0 }`; flashcards count every draft for the build. Deliberate and currently unreachable, but the flashcard path would raise a spurious non-retryable `DRAFT_INVALID` and roll back a legitimate completion if duplication and re-completion ever overlapped. | `elementGenerationCompletion.ts:123-126` versus `:159`. Plan line 101 preserves the asymmetry intentionally. The PR's own test demonstrates it at `elementGenerationCompletion.integration.test.ts:452-476`: the question variant completes with two drafts against `generatedElementCount: 1` while the flashcard variant rejects. | Align the flashcard predicate to `{ buildId, duplicationIndex: 0 }`, or add one comment stating that flashcards intentionally forbid duplicates and therefore count all rows. | unverified. |
| follow-up | Observability | **Release results do not establish why ownership was lost.** | The lease helper reports whether its fenced update matched. | Preserve release behavior; design diagnostics separately if needed. | No live frequency or cause measurement. |
| minor | Config & secrets / Performance | **The lease's safety margin, the transaction timeouts and the pool ceiling are all implicit.** The invariant that the completion transaction cannot outlive its 15-second lease holds only because Prisma's undeclared default `timeout` is 5000 ms. Nothing states that relationship at either end, so an ordinary future change — a global `transactionOptions`, or a per-call `{ timeout: 20000 }` added for a slow batch — would silently invert it. | `elementGenerationLease.ts:4`; `elementGenerationCompletion.ts:57` (no options argument); `packages/prisma/src/index.ts:41-48` (no `transactionOptions`, no pool `max`; still carries the pre-existing `// TODO: figure out whether using Pool with pg is a good idea for us` at line 12); effective defaults `maxWait ?= 2000`, `timeout ?= 5000` and `pg-pool` `max` 10. Adjacent constant sharing the literal but not the meaning: `questionGeneration.ts:67` `REVIEW_DISPATCH_RECOVERY_MILLISECONDS = 15_000`. | Pass an explicit `{ timeout }` below `ELEMENT_GENERATION_LEASE_MILLISECONDS` at the completion call, or add one comment stating the required ordering. Set pool `max` and `transactionOptions` explicitly as a follow-up. Do **not** add an environment knob for the lease — it is an internal optimistic-sync window with separate durable fences. | unverified. Transaction duration under realistic draft counts was not measured. |
| follow-up | Observability | **The context has no dedicated generation logger.** | This does not by itself prove that production has no logs or tracking. The two fixed-label catch events follow existing console conventions. | No new logging abstraction is needed for this refactor. | Source observation only; production collection was not checked. |
| minor | Observability | **`/healthz` is an unconditional liveness stub** and cannot report that generation is degraded. Pre-existing and untouched. | `apps/backend-docker/src/app.ts:214-216`; probed live against the running instance — HTTP 200, body literally `OK`. No Postgres, Redis or Hatchet check. | None. Listed because the charter asks about health checks. | unverified. Live-probed. |
| minor | Observability | **Whether takeover is rare or routine is unmeasured.** Held as a hypothesis rather than stated as structural. | Lease 15 s (`elementGenerationLease.ts:4`), client polls every 5 s, and synchronisation downloads blobs and verifies digests while holding it (`questionGeneration.ts:639`, `:748-750`). Apollo suppresses a scheduled poll while the previous fetch is in flight, so a single tab probably cannot contend with itself; that two components poll the same build concurrently was not established. | Capture one duration measurement the next time a build runs end to end. If synchronisation routinely approaches 15 s, the logging above stops being a nice-to-have. | unverified. Confidence 50 — the same unmeasured precondition as the livelock row. |
| minor | Data safety | **The new seed-repair `INSERT`s sit behind a runtime-boundary guard with no database-identity check.** What keeps `local-mcp-seed.mjs` off a non-disposable database is a container-and-URL-shape guard, not the repository's `klicker_test` naming convention, and the new statements raise the consequence of a boundary miss from rewriting one fixture row to creating `Course` and `Chatbot` rows. | `local-mcp-seed.mjs:64-77` issues `INSERT INTO "Course"` and `INSERT INTO "Chatbot"` directly. The only gate is `local-mcp-bootstrap.mjs:35-55` (cwd `realpath` equals `/workspaces/klicker-uzh`, protocol, hostname in `postgres|localhost|127.0.0.1|[::1]`, empty query string, `DEVROUTER_PROCESS_HELPER` present, two subprocesses). Both subprocesses were read: `util/dev-runtime.sh:146-154` only compares a marker token, `util/profile-resolver.sh:49-71` only inspects profile components. Neither reads `database.pathname`. Contrast `scripts/disposable-prisma.ts`, which rejects by database name. | Add `database.pathname` to the boundary check, matching the `klicker_test` naming already enforced by `scripts/disposable-prisma.ts`. | unverified. The guard does fail closed on every branch including a `spawnSync` status of `null`, and the fixture-identity gate at `local-mcp-auth.mjs:83-129` is strict, so escape requires deliberate misuse. |
| minor | Data safety | **The new seed test harness deletes from bare table names and is safe only through `search_path`.** Correct as committed; one edit away from reaching `public`. | `test-local-mcp-seed.mjs:524` `SET search_path TO pg_temp`, guarded at `:506-509` by `LOCAL_MCP_SEED_TEST === '1'` plus `validateRuntimeContext()`. Both gates fail closed, and the file is wired into no runner. | Schema-qualify the destructive statements as `pg_temp."User"`, or assert `current_schemas(false)` immediately after the `SET` so a lost `search_path` aborts instead of deleting. | unverified. |
| minor | Data safety | **The restored fixture course drifts from the seeded course it replaces**, so a repaired runtime is not equivalent to a freshly seeded one. | `local-mcp-seed.mjs:64-73` inserts `'Synthetic local runtime fixture'` with `pinCode` `934671825`, while `seedTEST.ts` seeds the same id as "Testkurs" with a different pin. `course.prisma:24` is `pinCode Int? @unique`; the new value collides with none of the seeded pins. | Import `USER_ID_TEST` / `COURSE_ID_TEST` from `@klicker-uzh/prisma-data` instead of hardcoding literals, and reuse the seeded name and pin so repair converges on the seeded state. | unverified. Fixture drift, not data safety. |
| minor | Deploy & rollback | **Rolling back re-breaks the local MCP fixture bootstrap, but only to an already-broken baseline.** New fixture state is rejected by the old guard — and the old guard already rejected the old seed, so a revert restores a known-broken state rather than creating a new one. | `local-mcp-seed.mjs:96` writes `passChatbotId = false` and `:80` inserts restored configs with `isEnabled` false; the merge-base guard rejected both (the diff of `local-mcp-auth.mjs` removes `!server.passChatbotId ||` and `!config.isEnabled ||`). `seedMCPServers.ts` is unchanged by this PR and already seeds `authType: 'scope_token'`, `passChatbotId: false` at the merge base. | None required for merge. If a rollback is exercised, reset and reseed the local database rather than debugging the bootstrap. Worth recording: the bootstrap's catch calls `stopOwnedProcesses()` over `['klicker-dev', 'klicker-local-mcp']` (`local-mcp-bootstrap.mjs:16`, `:101-107`), so a rejected repair takes down the whole dev stack. | unverified. Dev-only; no production, staging or data impact. |
| minor | Docs & operability | **`async-and-workers.md` documents the new mechanism but gives no recovery model**, on a page that supplies one for every comparable subsystem. An on-call engineer learns the invariant but cannot answer "my generation is stuck." | `docs/async-and-workers.md:89-102` is the whole new text and names no lease duration, although `elementGenerationLease.ts` fixes it at 15 s. The same page gives exact TTLs at `:106`, a literal operator recipe at `:110`, and the KB-graph grace and hold at `:64-65`; its frontmatter at `:4` promises "what silently breaks without workers". | Add two sentences: the 15-second window with no renewal, and one line stating that a build left non-terminal is not swept and is only re-evaluated when the owner reopens it. Do not add a rollback section — the mechanism is internal and has no feature flag. | unverified. |
| minor | Docs & operability | **No operational answer anywhere for an `ElementGenerationBuild` stranded non-terminal.** The documented new error codes do not strand — both `DRAFT_INVALID` and `CONCURRENT_MODIFICATION` reach the terminal `FAILED` write with the code recorded. The residual case is process death. | The only scheduled sweep is `packages/hatchet/src/index.ts:515-516`. `kbMaintenance.ts:806-814` never selects or writes `status`. Nothing under `docs/` or `docs/solutions/` addresses a stuck build. | One short paragraph naming the identifying query (non-terminal `status` with `syncLeaseUntil` in the past) and the intended remedy. Naming it an accepted gap is a valid outcome and cheaper than the sweep this PR explicitly excluded (plan line 65). | unverified. |
| minor | Docs & operability | **The in-diff plan carried a stale devrouter-pin Progress entry that, at audit time, no later entry superseded.** The entry describes a `package.json` / `pnpm-lock.yaml` / `pnpm-workspace.yaml` pin at 0.0.55 that no longer exists at head, so a reader checking plan compliance against the current tree finds every clause false. *(Corrected during the audit — see the verification column. The original finding claimed the described work had never been done; that was wrong.)* | `project/2026-09-04-pr-5777-generation-lifecycle-contracts-plan.md:333` claims the pin, `:335` records user approval for it, `:337` claims all three files retain it (audit-time lines 309/311/313, shifted by +24 when the owning session's addendum landed above them). At head none holds: `grep -i devrouter package.json` exits 1, `grep -c devrouter pnpm-lock.yaml` returns 0, and `git diff --stat <base>..HEAD -- package.json pnpm-workspace.yaml` is empty. | Append one Progress entry recording the upstream supersession, so a future agent resuming this plan does not try to restore the pin. Do not edit the historical entries — they were accurate when written. **Done by the owning session** at plan line 219, which names both commits and states "Do not restore the old package pin to satisfy that historical Progress entry." | **corrected by the orchestrator against primary sources.** The pin genuinely existed and the approval genuinely happened: `c38e7f9be8` ("build(devrouter): upgrade local dev routing CLI to 0.0.55", #5805, 2026-09-06 20:01) introduced it. It was then deliberately removed upstream by `fddc5b38b9` ("fix(devrouter): use explicit host and CI tool installations", #5814, 2026-09-07 11:46), whose diff drops `"@devrouter/cli": "0.0.55"` from `package.json`, the release-age exception from `pnpm-workspace.yaml`, and 110 lockfile lines, replacing the dependency with `.github/scripts/install-devrouter.sh` plus `util/devrouter-cli.mjs`. `git merge-base --is-ancestor` confirms **both commits are ancestors of the merge base** `df1ea25580` and neither is in the PR range, so this PR neither made nor unmade the pin. The wave-one worker's observations were all accurate; its interpretation was not. It concluded the plan asserted a deliverable that was never produced, and warned that a future agent might "restore the lockfile churn" — the correct guidance is the exact opposite: **do not restore the pin**, because upstream removed it on purpose and current validation uses the host-tool contract. The remaining lockfile diff in this PR is unrelated Next.js peer normalisation, as the worker correctly established. |
| minor | Docs & operability | **The new focused-test solution page contradicts its own example** — the verification sentence describes a five-file, 57-test run while the documented incantation names one file. | `docs/solutions/test-failure/graphql-focused-test-selection.md:29-30` versus `:42` ("direct-filename invocation selected the intended five files and passed all 57"). | List the five filenames, or reword `:42` to describe the single-file run the example performs. The central mechanism is correct — reproduced read-only: `exec vitest list --filesOnly <file>` selects 1 file, and adding the extra `--` separator selects 75. | unverified. |
| minor | Docs & operability | **The openrouter page names the disabled-binding trap, rules out the obvious remedy, and gives no enable path.** | `docs/solutions/integration/openrouter-local-chat-runtime.md:98-99` and `:103-106` state that a disabled binding stays disabled after credential rotation and that both restored bindings remain disabled, but never name how to enable one. The gate is `seedMCPServers.ts:293-299` (`kBChatbot` with `isEnabled: true`) applied at `:318` and `:340-342`; `ChatbotDetails.tsx` is touched by an import reorder only, so no UI remedy is introduced either. | One sentence naming the gate: enablement follows an `isEnabled: true` `KBChatbot` row for the chatbot, so enabling the KB binding for the fixture chatbot and rerunning canonical repair activates the tool. | unverified. Scoped to the local Chat/MCP dev loop. |
| minor | Docs & operability | **An identical `errorMessage` asserts lease loss for a condition that also covers a status mismatch**, at two raise sites at opposite ends of the transaction. | `elementGenerationCompletion.ts:60-81` guards on `id`, `elementType`, `status` and `syncLeaseOwner`, yet any miss yields `'Question-generation build completion lost its lease'` — the same string the post-write check at `:210-215` produces. Both callers persist `errorMessage: normalized.message`. | Differentiate the two messages (for example "no build matched the completion guard" versus "conditional completion affected 0 rows"). | unverified. Diagnosability only; both paths roll back the same transaction, so there is no state divergence. |
| minor | Docs & operability | **`KB_GRAPH_BLOB_HOST_PORT` is documented as optional where a parallel worktree requires it.** Pre-existing, not change-introduced. | `.devcontainer/README.md:201` says it "can override" the host port, while `.devcontainer/docker-compose.devrouter.yml:18` binds `- '127.0.0.1:${KB_GRAPH_BLOB_HOST_PORT:-10003}:10000'`. This audit's own startup required `KB_GRAPH_BLOB_HOST_PORT=10053` for exactly this reason. | State that each concurrent worktree must set a unique value. | unverified. |
| minor | Performance | **`GeneratedElementDraft` rows have no retention policy** and `duplicationIndex` has no ceiling. Blob artifacts are reclaimed; relational rows are not. | Repository-wide search for `generatedElementDraft.delete{,Many}` and `elementGenerationBuild.delete{,Many}` across the GraphQL, Hatchet and Prisma packages returns only generated-client doc comments. `kbMaintenance.ts:915-928` purges blobs but deliberately keeps the row ("The ledger row survives the purge (ADR 0013 keeps its cost evidence)"), and `docs/adr/0013-…` never mentions drafts, so draft retention is unconsidered rather than accepted. `elementGeneration.ts:668` increments `duplicationIndex` with no cap; `questionGeneration.ts:79-107` loads every draft for a build with no `take`. | Add a duplication ceiling in `duplicateGeneratedElementDraft` and file a retention decision alongside the ADR 0013 ledger-row retention. No change needed in this PR. | unverified. Growth is slow because each build is capped at 20 elements. |
| **refuted** | Observability | **Claimed:** each build stranded non-terminal holds a `RESERVED` spend row with no release path, so a lecturer's quota stays reserved while they are away. | **Refuting evidence:** `settleElementGenerationSpend` runs on the *dispatch* path at `elementGenerationDispatch.ts:123-125` (and `:129`, `:132` for the recovered-run branch), so by the time a build reaches `FINALIZING` its `costStatus` is already `SETTLED` and `reservedMinorUnits` has moved into `settledMinorUnits`. A release is then structurally impossible, not merely omitted: `elementGenerationAccounting.ts:431-435` returns `false` unless `costStatus === RESERVED`. `releaseUnclaimedElementGenerationSpend` releases only `UNCLAIMED` (pre-dispatch) spend, and `releaseStaleClaimedElementGenerationSpend`'s single production caller is guarded by `dispatchClaimedAt !== null` plus a 15-minute grace and exists for a dispatch never correlated to a run. Nothing leaks and no quota is stranded. | — | **refuted.** The no-refund *outcome* is real but is a design property shared with `WORKFLOW_FAILED`, `REJECTED` and every other post-dispatch failure: the provider work was performed and paid for. Recorded so the next audit does not re-litigate it. |

## Not checked

Every coverage gap the workers declared, plus the orchestrator's own.

- **The analytics route's transient spinner was never seen rendered.** The UX dimension was covered on a second attempt after the first worker exhausted its turn budget (`SendMessage` is disabled for this session, so it could not be resumed). The replacement observed the `Header` rendered and measured, and observed the analytics guard's hydrated end state plus its first-paint markup in the server HTML — but never caught the styled transient spinner itself: locally the flag window is too short, the browser tool offers no request-delay option, and the one attempt to freeze the paint by aborting Next.js chunks produced an unstyled page the worker correctly declined to cite. The two loading-state findings therefore rest on the diff, the server HTML and a geometry probe rather than on a screenshot of the real transient appearance.
- **The header was not observed with a pending catalog-sharing-request badge or an element-generation menu entry present.** Both widen the left group (`Header.tsx:89-90`, `:165-166`, `:189-194`) and are presumably what motivated the wrap change, but the worker could not reproduce narrow-desktop overflow with the seeded lecturer — at 1024px there was roughly 300px of slack — and creating that data was outside its authority. So `flex-wrap` was verified to work correctly at the widths where it engages (below ~676px in English, ~700px in German), not at the width the change was made for.
- **No element-generation build flow was driven end to end in a browser** — explicitly out of scope for the narrowed UX pass, and unreachable for the other workers without an exclusive runtime window. The primary-task and error-path flows of the refactored surface were therefore never exercised interactively by any worker. Together with the red `Y-question-generation-review` spec, this means the completion refactor's user-visible behaviour rests entirely on static equivalence analysis and the inherited UI acceptance evidence recorded in the plan.
- **The production ingress timeout is unknown, and it is decisive.** Whether a *single* tab can livelock a build depends on it: Apollo serialises one tab correctly (`@apollo/client@3.13.8` skips its own poll while its own request is in flight, and dedups at the link level on printed query plus canonical variables, with no `queryDeduplication` override anywhere in `apps` or `packages`), so one tab cannot steal its own lease. But nothing propagates a client disconnect to the server — `.download()` takes no signal, and the only `abortSignal` in the GraphQL package is unrelated (`knowledgeSourceGateway.ts:121`) — so if the gateway cuts a request before verification finishes, the orphaned work keeps running, Apollo reschedules immediately, and the successor steals the expired lease. `deploy/env-uzh-prd/values.yaml` uses `className: haproxy` with only cert-manager annotations and **no timeout annotation**, so the effective default lives in the controller's own configmap, outside this repository.
- **Real `FINALIZING` wall-clock duration against a production-scale knowledge graph.** The decisive unmeasured precondition for the livelock row and for whether takeover is rare or routine. The shared runtime has no real knowledge-base graph, no worker drove a live build, and load testing is outside worker authority. One timed pass would move that finding to confirmed or to nothing.
- **Live reachability of the inherited terminal-status concern.** The audit used static source tracing and did not reproduce the behavior or establish incident frequency.
- **Whether Apollo's link-level deduplication merges the 2500 ms and 5000 ms pollers in practice.** No `queryDeduplication` override exists, so the default is in force, but no worker observed request traffic to prove the two pollers never overlap.
- **Live database identity and applied-migration state.** `devrouter exec` returned `Error: Lifecycle transition is blocked.` for several workers — the shared runtime was mid-transition under a concurrent worker — and the audit forbade restarting it or starting a second one. HTTP routes stayed at 200 throughout, so browser verification was unaffected. Whether `20260826140000_kb_graph_generation_bundle` is applied in the environments `v3-ai` deploys to, and whether staging carries the `KB` fixture server, are outside read access. The schema and migration SQL are authoritative primary evidence, so this gap is not load-bearing.
- **`/tmp/dev.log`** went unread for the same reason — the one place a stray runtime log line could have contradicted the draft-count-mismatch finding.
- **Infisical was never inspected**, by instruction. The audit can therefore state only that this diff requires no addition to it, not what any environment contains. No secret value was read anywhere; configuration was inspected by key name and presence only.
- **No test suite was re-run.** Workers read recorded test source as evidence, per the brief.
- **Post-fix behaviour of `test-mcp-lecturer`** — the corrected workflow was not run, so there may be a second failure behind the first.
- **Whether the recovered Manage feature-access cases verify flag gating** rather than passing on defaults.
- **Whether the absent branch protection is deliberate.**
- **`git fetch --prune` failed inside one worker's sandbox** (credential helper blocked), so that worker used local refs. The orchestrator refreshed remote refs successfully and confirmed head `1c8ff98696924f1f53a3fa9177e16076c66d744a` unchanged, base `v3-ai`, still draft, `UNSTABLE`.

## Handoffs

Findings outside this gate's readiness territory, with the gate that owns each.

| finding | owner |
| --- | --- |
| The fixture seed performs raw `INSERT`s without the repository's first-class `requireDisposableDatabase` marker check (the `klicker-disposable-test-v1` `shobj_description` gate), relying on fixture-shape recognition instead. Note for accuracy: `assertLocalSeedOwnership` is not a database-identity discriminator — `seedTEST.ts:383` calls `seedMCPServers`, and `seed:qa` runs `seedTEST.ts` against staging, so the fixture shape recognises a staging database as readily as a dev one. The runtime boundary in the bootstrap is the layer doing the real work, and it holds. | `$security-review` |
| `docs/async-and-workers.md:84` states "The callers retain their status predicates", which is imprecise — the two polling entry points pass none. | `$code-review` (docs accuracy) |
| The primitive's `syncLeaseUntil` is never consulted by any business write, so the name suggests time-based mutual exclusion it does not provide. | `$thermo-nuclear-code-quality-review` |
| `retryable` is accepted by `questionGenerationServiceError` but never passed by either new error path, so it is dead as an input for `DRAFT_INVALID` and `CONCURRENT_MODIFICATION`. Whether the completion guard's conjunction should be split into distinct error codes is an API-contract question. | `$thermo-nuclear-code-quality-review` |
| `packages/shared-components/src/Loader.tsx:18` has no reduced-motion variant for `animate-spin`, so `prefers-reduced-motion` is not honoured. Pre-existing shared-component issue, not change-introduced. | `$code-review` (accessibility) |
| `justify-between` on the header row is now redundant with `ml-auto` on the right `Navigation` (`Header.tsx:377`, `:409`). The `ml-auto` is the load-bearing one — verified at 640, 540 and 430px, where `justify-between` alone would left-align the wrapped right group. | `$code-review` |
| Below about 560px the manage page still scrolls horizontally (`scrollWidth` 543 at 540px, 542 at 430px) because the left header group does not itself wrap. Pre-existing and **improved** by this PR — the single-row intrinsic width dropped from about 676px to about 542px — so the overflow threshold moved down rather than being eliminated. | `$code-review` |
| `apps/chat/scripts/local-mcp-auth.mjs` — `typeof server.passChatbotId !== 'boolean'` is vacuous: `chat.prisma:296` declares `passChatbotId Boolean @default(false)` (non-nullable). The sibling `chatbotIdHeader String?` at `:297` keeps its `!== null` check meaningful. | `$code-review` |
| `'https://growthbook.test'` is now triplicated across `util/_with_local_test_origins.sh:32`, `playwright/util/fixtures/manage.ts:53` and `playwright/tests/B-feature-access.spec.ts:209`. Correct today by construction. | `$thermo-nuclear-code-quality-review` |
| `local-mcp-seed.mjs` hardcodes UUID literals instead of importing the shared `USER_ID_TEST` / `COURSE_ID_TEST` constants. | `$thermo-nuclear-code-quality-review` |
| The 267-line `test-local-mcp-seed.mjs` harness is wired into no automated runner (no reference in `apps/chat/package.json`, `post-start.sh` or CI; only a manual command in a project document). | `$code-review` (test portfolio) |
| Root `CLAUDE.md` still presents the local Benibot `KLICKER_LOCAL_MCP_OK` `doc_query` check as a working procedure, while this PR's own solution page records that both restored bindings remain disabled and the chatbot remains a draft. The live probe was under profile `manage,mcp`, not the documented `chat,ai,mcp`, so runtime activation on the `ai`/KB-graph path was not ruled out. | `$code-review` (docs accuracy) |
| `kbMaintenance.ts:800-935` purges generation artifact prefixes 30 days after KB deletion without checking dependent build status. The outcome is a terminal `FAILED` rather than silent stranding, so no worker raised it as a finding. | `$code-review` |

## Audit metadata

Wave one: 8 dimension workers dispatched in parallel at xhigh effort, read-only. Seven returned on the first attempt; the UX dimension required a second, narrowly scoped worker after the first exhausted its turn budget, so all eight dimensions are covered. Wave two: 3 verifiers on the three highest-convergence candidate blockers — the stale-poller overwrite (**confirmed**), the transient-error spend loss (**confirmed outcome, mechanism partly refuted**), and the lease livelock (**mechanism confirmed, precondition unverifiable**). Total dispatched: 12 against a hard maximum of 16.

No worker marked any finding a blocker, and the orchestrator did not promote one. All three verified mechanisms are inherited and unchanged by this PR, so under the skill's verdict rule no confirmed or unverifiable **blocker** exists; the unverifiable item is a major whose settling measurement is named in condition 7.

Each verifier improved on the claim it was given rather than merely ratifying it: one found that recovery is worse than reported but the damage losslessly repairable, one refuted the dangling-reservation mechanism while confirming the outcome, and one refuted the stated cause of the livelock with a direct benchmark while confirming the mechanism under a corrected precondition. Two wave-one claims were withdrawn or corrected on this basis, and the refuted reservation claim is retained in the findings table so it is not re-litigated.

The orchestrator independently verified, against primary sources rather than worker report: both branch-protection API results, the two conflicting CI run identities, the full base-versus-head Playwright shard attribution, the prior-gate reviewed commits, and that the PR head has not moved.

Convergence across independent workers is treated as evidence quality throughout. Three workers separately confirmed the refactor is semantics-preserving; three converged on the lease livelock; two on the transient-error loss; three on the absent server-side sweeper.

## Post-audit developments

This section records owner updates made **after** the audit concluded; it does not overwrite the historical audit snapshot. The audit itself was read-only and made no repository change other than this report.

As of 2026-09-08 15:42 CEST, the owning session is acting under its own separately recorded user approval (plan section "Approved hosted qualification repairs — 2026-09-08", which covers the inherited browser failures, lecturer-MCP CI setup, and sanitised synchronisation logging). The following status is owner-reported and does not add audit verification:

- **Condition 3 is being implemented.** The concurrent dirty diff contains fixed-label synchronisation logging in each `synchronizeLeasedBuild` catch block (`questionGeneration.ts`, `flashcardGeneration.ts`), before the fenced write. This report does not verify the edit. The intended scope is fixed event labels only, with no raw error, message, stack, content, URL, identifier or token; a false release result is not proof of takeover.
- **Condition 4 satisfied**, and this report corrected as a result (see the devrouter row).
- **Condition 5 is scoped to an executor** with an explicit contract: reuse the existing disposable-PostgreSQL provisioner, include it in path selection, provision before reset, and use the restricted marked database consistently through reset, seed and both smoke checks. Hosted acceptance remains pending and must show execution rather than a skip.
- **Condition 1 accepted as owned work** for the three failing specs, with the fixture-versus-product diagnosis explicitly required first and "never globally enable beta or mock application permission decisions to obtain a pass" as a standing constraint.
- **Conditions 6 and 7, and the lease/retry/terminal-state proposals, are dispositioned as out of scope** for this PR: required-check configuration is a separate repository-policy action, and lease renewal, retry classification and terminal-state changes would alter behavior frozen by this refactor's approval. Those findings remain inherited and roadmap-owned; neither this report nor any single review authorizes those changes.

- **Current correction update.** The owner reports the closed AI route passes after a canonical restart, with no product-route fix. The loading localized text and `role="status"` edit is present but not yet verified. The prior analytics failures exposed a split test controller and preload. The new controller requires the existing opt-in test runtime; ordinary development correctly returns 404. Qualification is still pending. Planner-approved test-fixture wiring is underway. These are status updates, not new hosted-pass claims.

Two points of independent convergence are worth recording, because they were reached without reference to this audit: the owning session's CI triage independently established the same base-versus-head Playwright attribution on all three shards, and independently found the lecturer-MCP failure occurs during database setup before the smoke tests, with the same rejection on the exact target — matching this report's Deploy finding, including its inherited status.

The source edits above are the owning session's work under its own approval. This audit did not make them, does not verify them, and they are not covered by any gate recorded in the Prior gates table.

### Runtime disposition

The original audit's route-uptime inference did not prove container replacement or ownership transfer. The owning session subsequently recorded a canonical stop with Stopped provider state and zero source-matched routes, then resumed the same checkout for these fixes. Current state and producing-run logs are recorded in the execution plan. No runtime deletion is authorized or implied by this report.

## Owner verification update — 2026-09-08

The corrective source remains uncommitted. Two preload tests, 31 lifecycle tests, affected typechecks and scoped formatting pass. The test controller passes live HTTP update, rejection and restoration checks. The final focused browser run in the existing dev:test/manage fixture mode has four passes and five failures: generation editing passes, but dynamic analytics and knowledge-base pages return 404 before guard evaluation. The loading correction is therefore not accepted from static inspection alone. Source delivery and production readiness remain pending; see the execution plan for producing logs and runtime shutdown. No hosted execution, merge or deployment is claimed.

### Subsequent verified recovery — 2026-09-08

The owner used the existing guarded Manage cache repair and canonical restart. Both source-present dynamic routes now return 200 without route or authorization changes. All nine focused browser cases pass in 39 seconds, superseding the local five-failure checkpoint above. This includes loading status before profile failure, analytics allow/deny, absent and closed feature gates, unauthenticated no-query behavior, and generation editing. Hosted CI and independent delivery reviews remain separate gates. Evidence and runtime disposition are maintained in the execution plan.
