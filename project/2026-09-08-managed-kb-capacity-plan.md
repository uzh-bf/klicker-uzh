# Managed knowledge-base capacity

## Approval summary

Support larger managed resource inventories with a global limit of 1,000 resources and a nullable administrator-controlled storage allowance on each KB. Preserve the 500 MiB default, individual 25 MiB source bound, ownership checks and reservation accounting. Aggregate byte metrics must serialize values above 2 GiB accurately in Manage.

The user approved these source changes and native review as part of the managed-resource migration. This slice delivers capacity only. It does not register legacy resources, change retrieval, enable new source types, grant lecturer quota-editing rights or modify a live environment. Source delivery remains a draft until human review and merge approval.

Completion requires focused quota and worker tests, generated schema equivalence, safe byte serialization, package checks and independent source review. A later adoption implementation must establish truthful provenance and lifecycle independently; increased limits do not establish migrated content.

## Execution details

### Contracts and ownership

Main owns the shared capacity policy, GraphQL/worker integration, migration generation, verification and delivery. A native executor supplied the nullable field and shared helper; main completed the connected behavior. The source target is v3-ai, using trees/kb-capacity on feat/kb-capacity; the earlier adoption worktree is historical.

KB.storageLimitMiB is nullable. Null uses 500 MiB. Overrides are positive signed-32-bit integers; conversion to bytes must remain a safe integer. The same resolver drives admission, metrics and worker measured-size reconciliation. Unknown sizes retain conservative reservations. Each source remains independently bounded to 25 MiB.

Five aggregate byte fields become GraphQL Float and reject invalid integer values. Counts and individual resource bytes remain Int. GraphQL clients and the public SDL are generated from the source schema.

Use one generated additive migration and keep the analytics schema mirror equivalent. The standard local create-only command encountered unrelated baseline drift; generate the exact change by comparing baseline and edited Prisma model folders. Never reset retained data or fold unrelated drift into this migration.

### Verification

Extend existing GraphQL integration coverage for scoped allowance, unchanged defaults, reservations above 2 GiB and denial after returning to the default. Check the generated scalar serialization above 2 GiB. Extend worker tests for admission under default and enlarged limits. Reuse existing concurrency and tombstone coverage. Do not pin copy or documentation text.

Run typechecks, schema generation, scoped formatting and repository pre-commit checks. Host-only runtime-policy tests execute on host Node24; container toolchains execute inside the exact disposable task runtime. Native simplification and data-integrity slice review follow the committed change; integrated final review belongs to the completed delivery package.

### Boundaries

Source commits and ordinary draft delivery are approved. Human merge, deployment, live quota changes and data migration remain separate actions. Private cohort manifests and experimental evidence stay outside public Git history. No new dependency or infrastructure ownership is introduced. This reversible capacity field does not require a separate ADR; changed resource provenance/lifecycle would require its own decision record.

## Progress

2026-09-08: native planning review approved the capacity contract and staged adoption investigation. Capacity implementation, generated migration and public SDL are complete locally. Prisma and shared types build; GraphQL and worker typechecks pass. GraphQL integration tests pass65; worker tests pass35. GraphQL tests emit missing-Redis warnings in the minimal profile, so this is contract evidence, not full runtime health. The migration applied only to marked local klicker_test. Host policy checks pass68 tests. The broad check passed 38 of 40 tasks; simultaneous Prisma generation failed on a missing generated model, and the SDL check detected the intended unstaged change. Serialized Prisma build and typechecks subsequently passed. Staging the generated SDL followed by GraphQL generation, schema equivalence and typecheck passed. Source formatting, lint, dependency consistency and Prisma mirror checks pass. Independent source review and visual verification remain pending. No staging or production writes, source publication or merge occurred.


2026-09-09 continuation: isolated capacity worktree is trees/kb-capacity on feat/kb-capacity, committed head 81d77b7511. Refreshed origin/v3-ai is 15 commits ahead of the branch baseline; branch has three capacity commits and no upstream or PR. Trusted Claude slice review covered all 15 paths and returned DONE_WITH_CONCERNS. Its material finding is missing measured-byte database aggregate coverage above 2 GiB; the existing test covered reservations only. Extended that integration test locally to measure 91 resources at the individual size bound, verify metrics, and admit another resource. This correction is uncommitted and unexecuted. The reviewer's claim about database SUM overflow is not accepted as a proven defect without reproducing the actual Prisma path. Invalid administrator storage overrides continue to fail closed; no silent fallback added.

Local verification is blocked: devrouter ensure for this exact worktree failed because Docker reported all predefined address pools have been fully subnetted. No network pruning or configuration changes were made. Exact devrouter stop succeeded and freed zero routes. Remaining delivery work: restore runtime capacity, execute the extended DB integration test and relevant Manage visual check, resolve any actual failures, obtain integrated final review, then commit and publish the coherent draft PR. Merge and deployment remain separately gated. Fresh STG retrieval answered the synthetic question correctly, but its source card is unnamed and lacks an opening URL; this is not complete citation acceptance.


2026-09-09 runtime recovery: user approved removal of the exact unused kb-adoption-preservation_default Docker network and Devrouter configuration reconciliation. Immediately before removal, zero attached containers and the expected Compose ownership label were verified. Only that network was removed; containers and volumes were preserved. Read upgrade guidance for 0.0.56 through 0.0.62 and changed .devrouter.yml version from 0.0.55 to installed 0.0.62. The same exact worktree startup now passes subnet allocation; Devsy reports feat-kb-capacity Running. Startup session 21148 is still executing its lifecycle hook, so neither application readiness nor test success is established. Preserve this running operation; do not launch another ensure.


Recovery verification: ensure completed successfully on Devrouter 0.0.62 with profile manage; auth returned HTTP 200 and Manage HTTP 307 as required. GraphQL compilation emitted type warnings, so build success is not a clean typecheck. The focused measured-byte integration test passed, then the full knowledge.test.ts file passed all 65 tests against the guarded disposable database. Real measured totals above 2 GiB were read and admission succeeded; no aggregate overflow reproduced. Biome check passed with only three informational pre-existing Node import notices. Citation planner returned REVISE: require deleted-KB checks, supported authentication transport, conditioned bounded blob reads and explicit managed identity through projection/grouping. Historical file retention is not necessary for a fail-closed stale-version contract. The serving producer must be resolved before projection changes. No new citation code, source publication, merge or deployment yet.


Cross-task alignment, 2026-09-09: user explicitly requested coordination with Klicker Doc Query, Klicker Chat Roadmap, and related KB work. Sent bounded ownership/contract requests to those existing tasks; responses remain pending. Capacity remains owned here in trees/kb-capacity, targeting v3-ai, with 65 passing KB tests and stopped runtime. No citation implementation has begun. Live PR https://github.com/uzh-bf/klicker-uzh/pull/5764 overlaps proposed citation work in normalizeSources.ts, owner-preview chat route, preview page, and message-parts.tsx. Preserve its response-example receipt/capture behavior and agree ownership before editing those paths. Current target integration PR https://github.com/uzh-bf/klicker-uzh/pull/5851 is open; do not merge it or chase target movement without a readiness reason. Existing Doc Query owner retains title/origin fixes and FineCo repair. This task must resolve the serving endpoint with that owner before selecting any producer projection patch; no duplicate speculative cross-repo fixes. Scope remains fresh synthetic KB acceptance and capacity draft delivery, not legacy migration or catalog portfolio proof.


Owner receipts received: Klicker Chat Roadmap confirms no active citation-opening writer. It owns lecturer editor/preview and synthetic student acceptance; reports student retrieval and reload persistence on this synthetic KB, but opaque URN title remains (owner-reported evidence, not this task's fresh browser proof). Preserve separate participant/owner auth, no credential-bearing citation URLs, no Participation.isActive authorization, temporary preview state, and current entitlement/deletion checks on each open. Klicker Doc Query owns upstream title/projection/provenance repair; its newer STG deployment receipt supersedes this task's earlier projection snapshot but requires owner confirmation. Do not patch that producer independently.

Klicker KB/KG/Generation confirms no current capacity schema/service/worker delta. It owns source-only local ingestion/retrieval launchers at 0e96bb4b4c on rs/local-kb-ingestion-retrieval in trees/rs/generation-lifecycle-contracts, with no PR reported. Shared paths under its ownership include normalizeSources.ts, services/docQueryResult.ts, services/mcpClients.ts, tool-fallback.tsx and the owner-preview chat route. Coordinate before edits. Preserve original source metadata separately from authenticated access URLs. Provider launcher 87f02c3 on rs/provider-local-launcher remains under that task's independent review.

Verified additional overlap: https://github.com/uzh-bf/klicker-uzh/pull/5803 changes citation-chip.tsx and other mobile Chat components, targets rs/mobile-pwa-polish, and remains open. https://github.com/uzh-bf/klicker-uzh/pull/5676 changes participant chat route, history and fetch utilities, targets v3, and remains open. Neither branch is an automatic merge dependency for this v3-ai package. Capacity can proceed independently; citation implementation awaits a shared identity/projection contract with its existing owner and the revised planner pass.


Verification continuation, 2026-09-09: refreshed remote refs; capacity branch remains three commits ahead and fifteen behind origin/v3-ai with no upstream. Host policy checks passed, including 68 Playwright host tests. The initial monolithic check incorrectly included host-only Devrouter tests in the container and failed. Separated host and container execution. Container checks then exposed missing installed package files, including Linux Rollup and Parcel binaries. Recreated only generated container dependencies using the unchanged frozen lockfile; installation succeeded. The subsequent container run passed all 40 check tasks, all lint tasks, dependency consistency, agent-document policy, removed-artifact policy and Prisma mirror checks. No tracked dependency or lockfile change occurred. Full KB integration evidence remains 65 passing tests, including measured totals above 2 GiB. Manage visual verification and integrated source review remain pending. No PR, merge or deployment is claimed.

Local UI continuation: after dependency repair, the exact Manage startup repeatedly timed out on the auth readiness contract. Browser readback returned Bad Gateway. A formatting attempt could not execute because Devrouter reported an earlier lifecycle worker may still be active and requested explicit stop. Started source-path-scoped devrouter stop to reconcile this unhealthy runtime; its terminal provider/route verification is recorded separately when available. UI acceptance, formatting, final review and draft delivery are still incomplete. Passing container checks do not establish UI readiness.

Runtime release verified: scoped stop succeeded and freed three routes. Devsy status for feat-kb-capacity is Stopped; fresh devrouter route inventory contains no feat-kb-capacity reference. Worktree, database and dependency volumes are preserved.


Target integration and runtime boundary, 2026-09-09: committed the previously verified measured-byte regression test as 616e565c81; identity and exact-commit gitleaks checks passed. Refreshed target was a4adca18a47e3d325f77f9ee48cf21ae894882b8. It contains the merged host/container dependency-isolation fix absent from this checkout, which directly addresses the observed missing Linux dependency files. Integrated that exact target with --no-commit; target merge was clean. Applying the preserved local changes created only a Devrouter version conflict, resolved to the already approved 0.0.62 while preserving the target dependency preparation command. Integration remains uncommitted pending affected verification. The preservation stash remains intact.

Fresh old-runtime startup again stalled at auth proxy compilation and failed its 90-second readiness contract. Scoped stop completed. The updated configuration cannot start because Devrouter rejects repair against changed managed configuration. Installed CLI assertRepairBaseline confirms this guard checks profile, configuration hashes and resource sets. No guard bypass, state-file edit or runtime deletion occurred. Provider status is Stopped and fresh route inventory contains no capacity workspace. Proposed next action requires explicit deletion approval: recreate only the runtime for trees/kb-capacity, preserving its Git checkout but potentially removing its local synthetic database and caches. Main owns runtime recovery and final proof; explore owns the bounded existing synthetic UI acceptance procedure. No citation producer changes or final review have occurred.


Approved recreation attempt, 2026-09-09: user explicitly approved exact runtime deletion and recreation. The named stop --delete command failed with "Stop requires one exact provider registration." Fresh provider inventory contains no feat-kb-capacity registration. Devrouter still records the exact Git worktree owner, absent provider, and zero routes. The following ensure attempt failed with "Lifecycle transition is blocked." Read-only workspace gc confirms this exact workspace is ineligible because its registered worktree is present. No broad cleanup, lifecycle-state edit, owner deletion or alternate runtime was attempted. The missing capability is supported source-path-scoped reconciliation of an absent provider with a retained blocked lifecycle record. Approval remains valid; do not request recreation approval again when that capability is restored.

The bounded explorer returned DONE_WITH_CONCERNS: use the existing Y-kb-management-ux.spec.ts route and synthetic lecturer. Capture real default 1,000-resource / 500 MiB metrics; separately label intercepted above-2-GiB rendering and quota-error states as synthetic frontend evidence, not persisted database proof. Existing database integration evidence covers measured capacity. No screenshots have yet been captured.


Reviewed recovery canary: Devrouter source CLI at reviewed 9a72f16 completed canonical non-destructive stop with exit 0, stopped=true and freedRoutes=0. Retained stopBaseline remains present; top-level stopProof is absent, so proof location was requested from the Devrouter owner. Source diff hash before and after stop remained e969da82cf29a44e880693865cf32eb5ae72222623fde5dd67bf6770f7a8fd25. Subsequent approved ensure with the same CLI and explicit Devsy selection failed creating default-fe-d0f0d_default because Docker address pools are fully subnetted. CLI retained a reset candidate and reported candidate ownership/complete service population not proved; recovery state was not replaced. No further runtime mutations or broad network cleanup were attempted. Devrouter owner received the exact failure for coordination with networking. UI verification remains blocked; runtime state is incomplete, not claimed stopped or healthy.


Independent continuation while networking is blocked: user requests progress without waiting for allocator recovery. Keep candidate/runtime custody unchanged. Refreshed source refs; HEAD has four task commits and no upstream, with 38 commits behind origin/v3-ai and 155 ahead/16 behind origin/v3. Hold the deliberate pending integration of a4adca18 rather than chasing target movement. No unresolved merge paths; staged and unstaged diff checks pass. Ran host-only dependency-mount and Playwright-host tests with host tooling access: 38 passed. The first sandboxed run could not discover pnpm workspaces; the authorized host run passed. This verifies integrated host tooling, not container or browser behavior. Requested the authoritative managed source identity/projection contract from Klicker Doc Query; do not duplicate its producer implementation. Continue source inspection and contract planning independently. Application checks, screenshots, final readiness review and completed draft delivery retain their outstanding runtime acceptance requirements.


2026-09-09 renewed goal and current evidence: remote refs refreshed; preserve pending integration of a4adca18 with HEAD 616e565c81 and no unresolved merge paths. Published Devrouter 0.0.65 doctor reports OrbStack allocation readiness unknown, managed network policy absent, and the exact manage runtime still in failed-transition/process-start with configuration drift. The release does not establish runtime recovery. Owner-directed canonical non-destructive stop on 0.0.65 failed with `Retained provider identity changed.` The retained baseline exists with seven containers and an empty sourceContainer. Provider identity reconciliation remains with the Devrouter owner; no deletion, policy change, raw Docker cleanup or startup retry followed. Container checks, screenshots and completed capacity delivery remain pending.

Citation source evidence: inspected mcp-doc-query c9feb93b5fe03518d4abc81e88de3375877e8c9c public documents construction at src/pipeline.py:3088-3142 and metadata allowlists at185-238. That path omits kb_id, external_resource_id and resource_version. The canonical ingestion identity is external_resource_id; earlier checks for resource_id alone were insufficient. Klicker Doc Query reports current STG ingestion retains accepted display_name and stamps the canonical identity tuple, but old artifacts are not backfilled. That owner retains producer repair; no repaired projection or authenticated opening is claimed.

Opening-contract inspection at the pending integrated source confirms separate participant getParticipantId/authedFetch and owner-preview authorization paths. withChatbotAuth is cookie-only and cannot substitute for scoped iframe header authentication. The KBChatbot binding has isEnabled; KB and resource each have deletion markers. KBResource distinguishes resourceVersion from activeResourceVersion and activeContentSha256. A revised opening plan must verify the cited version against its actual retained bytes, current enabled binding and current access; it must fail closed when exact bytes are unavailable. Existing machine ingestion gateway serves only queued/processing resources and is not a participant opening endpoint. No citation implementation started.


Opening planner construction completed (Lovelace, native planner, DONE_WITH_CONCERNS). Proposed separate participant and owner-preview POST endpoints sharing an exact-byte reader. Participant flow composes getParticipantId, published chatbot/course checks, embed scope and participation; owner flow uses withOwnerPreviewAuth plus course deletion. Both require current enabled KB binding, undeleted KB/resource and the complete canonical locator tuple. Preserve chip scroll/focus and safe public-link behavior; managed cards use authenticated fetch without putting credentials or storage URLs in navigation.

The proposed reader checks active version and digest against retained blob metadata, conditions a bounded download on ETag, verifies length and SHA-256 before releasing bytes, and rechecks access/resource state before returning a private no-store response. Missing exact bytes fail closed. This is a construction result, not an implementation-ready approval: pending decisions are storage ownership/access, existing shared consumer ownership, exact URL-resource behavior and browser presentation. Chat currently has no direct Azure Blob dependency; inspected Chat source only configures the account URL, not proof of server read credentials. Do not add storage authority implicitly. The existing GraphQL reconciliation predicate also requires current and active version equality; justify any opening-specific divergence against resource lifecycle before settling it.

Current runtime recovery is blocked on a confirmed unsupported generation mismatch: baseline providerId equals current workspace.id, context and normalized source path match, current UID differs (length16), all seven baseline container IDs are absent at the baseline-pinned endpoint, and the saved daemon ID matches. Devrouter owner retains source recovery; no supported command is available in0.0.65. No further runtime mutation. Current Manage UI shows the synthetic chatbot as Draft with participant access unavailable and owner-preview available. Unsaved disclaimer state prevented navigation; dismissal was attempted, then browser timed out, so no successful discard/save is claimed.

2026-09-10 runtime recovery succeeded (Devrouter 0.0.66 + stale Turbopack snapshot): installed CLI 0.0.66; bumped .devrouter.yml 0.0.62 to 0.0.66 (uncommitted, on top of the pending a4adca18 integration). Canonical stop returned stopped=true, freedRoutes=0, clearing the stuck failed-transition journal. Two ensure attempts then failed identically on the auth readiness contract: Next dev printed Compiling proxy and stalled with zero CPU for 9.5+ minutes (curl 28 at 30/60/90s) while frontend-manage in the same run compiled and served GET / 200 in ~1s. Root cause evidence: apps/auth/.next/dev/trace shows the new run restored the Turbopack persistent snapshot (2,041 tasks) left by the uncleanly terminated pre-reset run and stalled before any compile event; the previous healthy run's trace shows normal compile/serve; manage restored its snapshot (954 tasks) and proceeded. Fix: scoped stop, removed only apps/auth/.next (regenerated build cache; no source, data or volume touched), then ensure --profile manage. Result: auth readiness HTTP 200 in 7s, manage 307, full route set registered (API/Auth/PWA/Manage/Chat/LTI/Blob and others), duplicateHosts empty, host probes manage 200, auth 200, api 404 on / as expected with GraphQL under /api/graphql. Open owner-visible quirk: devrouter status still reports drifted with managed route state could not be inspected while ensure's final JSON reports ready; devrouter ls returns thousands of stale worktree route entries and warns 9 managed workspace owners missing. Reported to the Devrouter owner as an inspection/stale-route hygiene issue; no functional impact on the active workspace. Remaining delivery: commit the pending integration with the 0.0.66 bump after affected verification, Manage UI browser evidence via Y-kb-management-ux.spec.ts, integrated final review, draft PR to v3-ai; merge/deployment remain separately gated.

Integrated verification and delivery receipts, 2026-09-10: the pending integration of a4adca18 was verified on the integrated tree before commit: container check 40/40 successful, focused KB integration suite 65/65 passed (exit 0) against the guarded disposable database, container auxiliary checks syncpack, agents-md, git-identity, removed-doc-artifacts and prisma-sync all passed, and both host Playwright check scripts passed after a frozen-lockfile host node_modules refresh (install Done in 7m36s; lockfile unchanged). The first hook attempt reproduced the known simultaneous Prisma generation race on the fresh host tree (39/40, prisma#check failed in parallel); the serialized prisma check passed alone and the retry hook passed fully. Committed the integration as merge commit f6e252b88c with parents 616e565c81 and a4adca18, preserving devrouter 0.0.62 resolution and the target dependency preparation command; exact-commit-range gitleaks scan (23 commits) found no leaks. Follow-up commit stages the .devrouter.yml 0.0.66 bump and this progress entry. Remaining: Manage UI browser evidence via the Y-kb-management-ux route, integrated final review, then draft PR to v3-ai; merge and deployment remain separately gated.

Final review and draft delivery, 2026-09-10: the configured external final reviewer (Claude CLI) was rejected by the automatic approval reviewer as an unverified external destination without explicit export authorization; no same-outcome external fallback was attempted. The main session therefore performed a line-level integrated diff audit of the capacity-only paths (types resolver, GraphQL schema/services/SDL, ingestion admission, single migration, analytics mirror, i18n, docs) and reconfirmed verification receipts on the integrated tree. The branch was pushed to origin/feat/kb-capacity at 1909e1018b and the verified draft PR https://github.com/uzh-bf/klicker-uzh/pull/5876 was created targeting v3-ai with summary, verification, and known-pending sections; read-back confirms draft state and head SHA. Manage UI browser evidence remains pending on the exact managed runtime: it sits in failed-transition full profile with degraded configuration baseline; the canonical stop/ensure cycle cannot switch to the manage profile because the recorded profile/configuration baseline differs, and the supported recovery is the previously user-approved exact runtime stop --delete (auto-reviewer rejected re-execution pending fresh explicit authorization for this exact command and effect). No merge, deployment, or unapproved runtime action occurred.


2026-09-10 UI evidence and runtime closure: Y-kb-management-ux.spec.ts passed (1 test, 15.6s) against the exact manage runtime at c4cea96f4565a7da7f2df1979570162e878671ec. Inspected English and German 1440x900 screenshots show the 1,000-resource default and 500 MiB storage allowance; keyboard/modal interactions passed. Upload/replacement responses are synthetic; this does not establish live ingestion. Both images are attached inline to draft PR #5876; CLI read-back verifies their URLs. Local manifest and byte-hash upload receipts are under /private/tmp/kb-capacity-evidence. Forge rendering inspection remains pending.

Runner recovery required the supported KLICKER_DEVROUTER_BIN override to the Volta 0.0.69 executable: Homebrew 0.0.66 rejected newer reliability-record fields. No records were edited. After verification, canonical stop succeeded (freedRoutes=3); devsy workspace describe and status confirm Stopped for /Users/rschlae/Git/klicker/klicker-uzh/trees/kb-capacity, identity feat-kb-capacity/default-fe-81be0, and devrouter ls confirms zero matching routes. Runtime data is preserved.

CI disposition: codebase check failed because the CI installer pinned 0.0.59 below the branch requirement 0.0.66. Aligned that single installer pin to 0.0.66; shell syntax and diff checks passed, and all 15 profile-runtime tests passed against installed 0.0.66 without starting a runtime. Translation CI fails before execution because its requested intl-production-smoke.mjs is missing from its checkout. Remaining work: publish this CI correction, inspect forge screenshot rendering, disposition remaining CI/review feedback, and confirm the final draft state. No merge or deployment.

2026-09-10 review disposition and target refresh: published 00789ec49e after 40/40 hook checks and the cached 26/26 pre-push build. GitHub screenshot table rendering was visually confirmed. GitGuardian incident 1509424 points to a synthetic password assertion for the disposable PostgreSQL CI service in hosted-pnpm-cache.test.cjs from integration commit f6e252b88c; no production credential was identified, and no incident dismissal or history rewrite was performed. Added an explicit public-schema compatibility note for KBMetrics Int-to-Float widening; retaining Int32 would break the approved capacity range, while the existing safe-integer validation bounds exact numeric representation.

The PR was marked ready externally; preserve that human action. A second target integration is justified by the missing translation CI script, not target drift alone: merge 352f47fd74 from origin/v3-ai applied without conflicts and is pending integrated verification and commit. The prior runtime remains stopped. Final CI/review acceptance remains pending.


2026-09-10 final correction and review: target integration was committed and pushed as 93ab544238. Cached source retries were then proven to bypass current quota (1 failing case, 36 passing). Correction e9cf8311b9 routes prepared and cached sources through the same locked admission, rechecks current attempt state and returns directly after denial. Worker tests41/41, container typecheck, repository hooks40/40 and pre-push builds26/26 pass. Independent simplification and data-integrity slice reviews returned DONE with no required correction. Review receipts are in project/_local/reviews. The exact runtime was recovered after its owner corrected malformed global capacity policy; final canonical stop freed3 routes, provider state is Stopped and route references are0.

Configured Claude integrated final review completed successfully and its structured output validated against the canonical schema. Source correctness passed with three low findings. Corrected stale Devrouter prerequisite/current-pin documentation while preserving historical capability evidence. The stale quota text in .agents/skills/klicker-data-model/SKILL.md remains explicitly deferred because global instructions make non-rs skills read-only; current quota guidance in docs/domain-model.md and docs/graphql-api-layer.md is correct. Optional database CHECK hardening is deferred: administrator configuration deliberately fails closed, and this package retains its single generated additive migration without expanding the agreed schema contract. No silent fallback to default quota was added. Worker unit mocks do not prove actual database locking or rollback; the review does not upgrade that evidence.

Published PR https://github.com/uzh-bf/klicker-uzh/pull/5876 remains against v3-ai and was marked ready externally; this task preserved that state. User authorized the configured final review and PR-description update; the latter was published and read back. Current-head unit CI passed. All eight Playwright shards in run34491290379 failed before tests because the trusted controller still installs Devrouter0.0.59 while the candidate requires0.0.66. Candidate installer correction alone cannot update that controller. GitGuardian retains the previously identified synthetic CI-service assertion finding. Overall CI acceptance remains incomplete. Existing EN/DE Manage screenshots and interactions retain their c4cea96 provenance and mocked ingestion boundary. No merge, deployment, live quota change or data migration occurred.
