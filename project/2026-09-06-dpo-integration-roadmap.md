# DPO integration roadmap

## Approval summary

The user reprioritized delivery on 12 September 2026. The first release, needed for the start of term on Monday 14 September, contains the updated privacy policy, normal account-creation disclaimer, new assessment-entry disclaimer, completion or renewal for existing accounts in both applications, and persisted independent participant research and learning-analytics choices. The first release must be independently deliverable without research or assessment exports, learning-analytics configuration or computation, or knowledge-base confirmations.

Knowledge-base upload confirmations are the second milestone. The remaining export, learning-analytics processing, and retained-points packages stay on the roadmap for subsequent delivery. This replaces the earlier requirement to finish all four packages before the first delivery; it does not discard their source or change their settled product contracts.

The first milestone uses the ordinary signup, assessment and profile workflows. Keep the concise notices and collapsible sections. Assessment adds the ordinary data categories plus assessment identity, detailed answers and results, audit logs, authorized lecturer access, and assessment account/data retention restrictions. The supplied 8 September privacy-policy revision and the user's subsequent assessment clarification govern the policy update and corresponding English translation. Existing fonts, colors, components, and navigation conventions remain.

The [completed app draft](./2026-09-05-dpo-app-draft-plan.md) provides reviewed presentation and wording. It does not satisfy this roadmap: completion requires real persistence, server enforcement, actual synthetic downloads/uploads, and browser verification through ordinary routes. The [execution plan](./2026-09-06-pr-5819-dpo-integration-execution-plan.md) owns implementation details.

The user authorized planning, this dedicated roadmap, and execution with a goal on 2026-09-06. Local edits, synthetic verification, configured reviews, and commits are authorized. The user settled branch placement: core changes target `v3`; AI-specific changes target `v3-ai`. Do not assemble those branches or pending consent branches into one verification branch. The user subsequently authorized task-branch pushes and full draft PR delivery. Changes to other owners' branches, target merges, deployment, real-data processing, and legal approval remain excluded.

The immediate next step is to isolate the account-and-policy release from the existing broad DPO changes, preserving both task branches and their deferred work. Capturing preferences does not activate analytics collection or processing. Notices must describe availability truthfully rather than promise an already available dashboard or export. Analytics processing and export readiness are gates for their later milestones, not for saving preferences or acknowledging the policy. The execution plan owns the focused release's acceptance checks and packaging reconciliation.

## Current delivery order — 12 September 2026

| Milestone | Required outcome | Completion boundary |
| --- | --- | --- |
| First: term-start account and policy release | Updated German policy and English translation; normal signup disclaimer; assessment-entry disclaimer; missing-choice and stale-policy completion in both applications; atomic participant choice persistence and saved-choice profile controls | Synthetic normal/assessment journeys, durable reload and session behavior, server enforcement, migration verification, DE/EN browser evidence, reviewed focused draft and exact-head checks |
| Second: KB upload confirmations | Rights and personal-data confirmations before transfer, using the updated KB implementation | Real synthetic transfer/ingestion, persisted binding and direct-call rejection; independently reviewed draft |
| Later: remaining DPO workflows | Research and assessment exports; analytics configuration, collection, deletion/recomputation and reports; retained points and all-member group averages | Existing package-specific acceptance; preserve the separate v3 and v3-ai targets |

An explicit `false` is a recorded choice, not an incomplete account. New learning-analytics choice starts unanswered; research is initially allowed. Existing database defaults without recorded choice metadata are not evidence of a submitted choice. Renewing policy acknowledgement preserves saved purpose choices. Choosing neither optional purpose must still permit normal and assessment access after the required acknowledgement.

## Stack restructuring — 13 September 2026

The user stopped full-roadmap execution and directed stacked delivery against `v3`. GitHub stacks are enabled for this repository. The existing broad branches stay frozen as safety references until the replacement stack validates and lands: `rs/dpo-core-integration` (PR #5819, head 0868e6fd87) and `rs/dpo-ai-integration` (PR #5825, head 8444fcf207). Term starts Monday 14 September; layer 1 is the only release-critical unit.

| Layer | Branch | Content | State |
| --- | --- | --- | --- |
| 1 — term-start disclaimers | `rs/dpo-release-1-disclaimers` | Database acceptance layer (participant data-use fields, acknowledgement/revision events, onboarding migration, consent indexes), normal/edu-ID/assessment signup disclaimers, existing-account completion and renewal gate in PWA and assessment journeys, saved-choice profile controls, reviewed data-use guide assets, updated German privacy policy and English translation | extraction in progress |
| 2 — profile and leaderboard boundary | `rs/dpo-release-2-profile-points` | Learning-analytics withdrawal owner and read gating, retained private points, all-member group averages, join/leave and deletion confirmations, leaderboard retention | mapped, not extracted |
| 3 — exports | `rs/dpo-release-3-exports` | Attested assessment result downloads and course research exports (JSON v1, receipts, limits), Manage export UI, REST mounting, Playwright report updates | mapped, not extracted |
| AI line — existing branch | `rs/dpo-ai-integration` | KB upload confirmations, chat policy adapters, LA provenance capture (all nine families), LA research-export class, empty-scope retirement (open) | existing draft PR #5825; reconciles after `v3` merges into `v3-ai` |

Layer rules: shared files with mixed content (`packages/i18n` messages, GraphQL registrations, `test/helpers.ts`, Playwright specs) are hunk-staged so a layer carries only its own strings and registrations; generated artifacts are regenerated at each layer tip; every layer is verified at its own head before publication. The source-to-stack coverage ledger lives in `project/_local/2026-09-13-dpo-stack-migration-ledger.md` and must reach source-PR equivalence before any layer opens for review.

## Working context and evidence

Audience: the user deciding the remaining boundaries, then an agent executing without conversation context. Main owns decisions, integration, roadmap updates, and final proof.

Preserve the draft and planning worktree `/Users/rschlae/Git/klicker/klicker-uzh/trees/consent-disclaimers`, branch `rs/consent-disclaimers`, at `8cea28ef339d3042d8ac928fe80a8b0ec8b1fe2a`. Its upstream is `origin/v3`; refreshed state is six ahead and 48 behind as of the latest refresh. Preserve the three pre-existing deletions under `packages/transactional/out/` and all unrelated work. No runtime is currently running for this task.

| Existing source               | Evidence and consequence                                                                                                                                                                                                                                                  |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| App draft                     | Nine isolated surfaces, 17 browser cases, nine isolation checks, 54 screenshots, and integrated review passed; functional integration remains undone                                                                                                                      |
| Consent schema, API, settings | Open [schema PR #5569](https://github.com/uzh-bf/klicker-uzh/pull/5569), [API PR #5590](https://github.com/uzh-bf/klicker-uzh/pull/5590), [settings PR #5595](https://github.com/uzh-bf/klicker-uzh/pull/5595); reuse their model and services rather than duplicate them |
| Assessment signup             | PWA `src/pages/createAccount.tsx:103` redirects assessment users away; a post-authentication completion route is required                                                                                                                                                 |
| Assessment export             | Manage `AssessmentStudentResultsTable.tsx` enables direct CSV download; course results require ADMIN permission in GraphQL                                                                                                                                                |
| Research export               | `packages/export` is an operator-only live-quiz CLI, not an authenticated research-export service                                                                                                                                                                         |
| Leaderboard                   | `services/courses.ts` initializes join score to zero and clears timeline points on leaving, contradicting the retained-points policy                                                                                                                                      |
| Knowledge base                | Actual resource upload/replacement/import components are on `origin/v3-ai`; they are absent from the draft baseline                                                                                                                                                       |

The authoritative copy is the primary checkout's `project/_local/2026-09-03-dpo-package-v2/`, including generated `CHOICES_HTML` and normal edu-ID markup. Preserve its guide, three attachment links, and archive checksum recorded in the draft plan. The task-worktree package with the same name is older and is not authoritative.

## Primitive impact and binding decisions

| Product primitive         | Disposition                                         | Contract                                                                                                                                                                         |
| ------------------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Account usability         | Extend                                              | Current acknowledgement and complete independent choices are required; acknowledgement does not mean LA participation                                                            |
| Global optional data use  | Extend existing owner                               | Research displays allowed initially; LA starts unanswered. Persist explicit completion and separate purpose history. Renewed terms preserve saved choices                        |
| Leaderboard participation | Extend                                              | Publication is independent of course access and ordinary personal point accrual. Retained points count immediately on first join/rejoin; historical rank awards are not replayed |
| Export release            | Compose authorization, eligibility, and attestation | A checkbox cannot grant permission. Research eligibility is checked at release. Only selected supported classes are delivered                                                    |
| Material transfer         | Extend existing upload/import owner                 | Separate unchecked rights and personal-data attestations precede transfer, including replacement and import                                                                      |

These user decisions are binding: no visual redesign; independent research/LA; no LA default answer; research allowed initially; saved choices survive renewed acknowledgement; ordinary author names and bibliographic credits are permitted; no reconstruction of already-erased points is implied. Assessment completion asks no ordinary account fields.

## Work packages

| Package                                          | Work and priority                                                                                                                                                                                  | Dependencies                                                                                                           | Check and terminal                                                                                                                                                  |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| W1 — account and profile integration             | P1. Extend the existing data-use owner with acknowledgement, atomic audit/history, server usability checks, normal/edu-ID/assessment onboarding, existing-account gate, and saved profile controls | A1 — separate branch placement; compatibility with existing data-use and LA withdrawal owners                          | Reloaded DB-backed choices, stale sessions, direct API denial, safe LTI return, failure/cancel/renewal, synthetic withdrawal proof; locally committed and reviewed  |
| W2 — retained points and leaderboard integration | P1. Persist ordinary points while inactive, hide public entries on leave, expose retained totals on join/rejoin, preserve rank-award history                                                       | A1 — separate branch placement; account gate contract where applicable                                                 | DB-backed accrual, leave/rejoin, idempotent scoring and concurrent updates; ordinary course UI verified; locally committed and reviewed                             |
| W3 — course export integration                   | P1. Assessment and research actions on Manage course details; replace direct assessment CSV bypasses; persist attestations and release actual authorized artifacts                                 | W1 — account and profile integration; A2 — research delivery contract; compatible LA owner for selected LA derivatives | Actual synthetic downloads, scope/identity/numeric integrity, request validation, permission and eligibility races, audit failures; locally committed and reviewed  |
| W4 — knowledge-base confirmation integration     | P1. Gate initial/additional/replacement/import transfers in existing resource workflow and backend tickets                                                                                         | A1 — separate branch placement; shared attestation contract                                                            | Synthetic file transfer and import through real code paths; direct ticket/confirmation bypass denied; reset/cancel/changed material; locally committed and reviewed |

The four packages describe the overall backlog. The current delivery order above determines which work blocks each release. Execute with one writer per worktree. Split cross-branch packages into core implementation targeting `v3` and AI adapters targeting `v3-ai`. Define shared contracts once; record unavailable prerequisite commits as dependencies rather than importing entire development branches. Review and verify each diff against its own target. Preserve the existing PRs while reconciling the focused first-release package; no published stack is created by this roadmap update.

## Decisions and external dependencies

| Decision                        | Recommendation and consequence                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Gate                                                                                                                        |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| A1 — separate branch placement  | Settled by the user: implement core signup, assessment completion, profile, leaderboard, and core assessment/research export workflows in task branches targeting `v3`. Implement KB confirmations and AI-only chat/analytics export or enforcement adapters in task branches targeting `v3-ai`. Preserve existing consent owners as explicit dependencies; do not merge their entire branches or duplicate their model. The anticipated later merge of `v3` into `v3-ai` is outside this task; compatibility in that direction is required | Separate drafts and target-specific verification; verify future `v3` into `v3-ai` compatibility without a shared-branch merge            |
| A2 — research delivery contract | Locked: course ADMINs may directly download selected research data, including free text and transcripts, after the required attestation. There is no separate human review or approval workflow. Use single-course JSON v1 with a manifest, typed selected classes, export-local random participant keys, and explicit rejection above 50,000 records or 25 MiB. Free text remains personal data; no automatic anonymization claim                                                          | W3 — course export integration; no silent removal of requested classes                                                      |
| Analytics compatibility         | Existing engine/coordinator/group-report owners must support global eligibility, prospective re-enablement, withdrawal deletion, and protected lecturer payloads. This task implements the account boundary and necessary withdrawal handoff/tests, not a new private dashboard or analytics platform                                                                                                                                                                             | Required backend guarantees cannot be claimed until verified; incompatible dependency keeps the affected package incomplete |
| Production policy               | DPO/copyright wording approval, retention/audit durations, disclosure checks, and reconciliation of ADRs 0023/0024 with the newer package are release decisions. Synthetic configuration is test evidence only                                                                                                                                                                                                                                                                    | Production activation, not permission to implement with synthetic data                                                      |

Normal edu-ID implementation reuses authenticated identity and existing account-linking rules; it must not collect assessment-specific identity for normal use or add new profile inputs absent from the package. If existing username provisioning cannot satisfy account constraints, report that concrete product decision before choosing a new identity mechanism. Legitimate anonymous/temporary flows remain available under their current contracts; a gated registered identity cannot bypass the gate by falling back to anonymous processing.

## Verification and review

Use the execution plan's behavioral portfolio. Reuse unchanged presentation evidence but obtain new proof for every real integration. Tests assert behavior and protocols, not prose, translation strings, or specimen contents. Use only synthetic data and local services. A mock proves a contract, not storage transfer or OAuth callback persistence.

Each substantive committed slice receives simplification review; authorization, data integrity, and cross-system slices receive risk review. Final review covers the complete committed integration and the roadmap's required outcomes. Complete runtime shutdown and exact provider/route verification before final review. No required package is parked without a user ruling.

## Progress

2026-09-06: User requested a dedicated DPO integration roadmap linked to the completed draft and authorized execution with a goal. Native planner approved the roadmap and execution plan after one corrective round. Parent retained the requested workflow integrations; separate analytics dashboards remain dependency-owned. Source assembly and the research delivery decision are pending. No functional integration, source merge, deployment, or real-data processing has occurred.

2026-09-06: User rejected combined source assembly and settled separate branch placement. This supersedes the earlier source-assembly proposal and its approval blocker. The earlier planner approval covers the functional scope; the revised topology needs a bounded consistency check. Research delivery remains a separate pending decision. No source merges have occurred.

2026-09-06 delivery: User authorized commits, push to draft PRs, and a device handoff. This supersedes earlier publication exclusions only for this checkpoint. Portable reviewed source material is in `project/dpo-reference-package/`; review receipts are in `project/dpo-review-evidence/`. Future functional work still targets separate branches, with no shared merge or deployment authority.

2026-09-07 continuation: canonical consent prerequisites are incorporated into both existing task branches. Core draft PR #5819 targets `v3`; AI draft PR #5825 targets `v3-ai`. User requested full completion through these drafts with an active goal and explicitly requires compatibility for the later `v3` into `v3-ai` merge. Earlier publication and prerequisite-incorporation exclusions are superseded. All four required packages remain in scope; current evidence and unfinished boundaries are maintained in the execution plan.

2026-09-13 delivery state before restructuring: all four packages are implemented, verified with synthetic evidence, committed, and pushed. Core PR #5819 head 0868e6fd87 (draft): first-milestone disclaimers and completion flows, profile controls, withdrawal, retained points and all-member group averages, join/leave/deletion confirmations, attested assessment and research exports; hosted build and GitGuardian passed with Playwright shards pending. AI PR #5825 head 8444fcf207 (draft): the same core work merged plus KB material confirmations (durable binding, narrowed-audience renewal, bypass rejection), chat policy adapters, and LA provenance — schema/migration `20260913170128_dpo_la_research_contributions`, capture in all nine analytics families, atomic withdrawal deletion, and the LEARNING_ANALYTICS research-export class (commits e5583175c0, 2e77c52813, 6f301e1380, 8444fcf207). The 8 September policy documents govern the shipped wording; DE/EN copy, collapsible structure, and assessment-specific retention wording were aligned and browser-verified.

2026-09-13 verification record: graphql suite on the fresh disposable database — export, data-use, and withdrawal tests green; three residual failures are classified non-regressions: activitySharing audit-entry contamination across runs on one database (test cleanup omits audit rows), assessmentRestrictions redis_assessment absence in the ai-profile container (reset catch returns null), both pre-existing on the target. `check` (codegen, schema snapshot, tsc) and Biome pass. AI GitGuardian failure is the known false positive on four synthetic constants, disclosed in the PR body. Detailed evidence: `project/_local/reviews/2026-09-13-document-export-continuation.md`.

2026-09-13 stack directive: user ordered stacked delivery; layer 1 is only the database acceptance layer, the create-account disclaimers, the existing-user completion/renewal surface, and the updated privacy policy, because the term starts 14 September. Exports, LA computation changes, KB upload disclaimer, and further features move to later layers or the AI line. Open items for later finalization: empty-scope retirement across the nine LA families (writers early-return on empty data; retirement design: delete by family plus deterministic scopeKey, which excludes participantId, then re-retain present participants), chat-transcript export content ruling (full content versus no-image-bytes versus text-only; asked three times, unanswered), slice-2b scope-identity threading, final reviews and exact-head CI on the stack and AI heads, AI runtime `rs-dpo-ai-integration` still running (profile ai, KB_GRAPH_BLOB_HOST_PORT 10090) and must be stopped at finalization, core runtime stop remains guard-blocked.
