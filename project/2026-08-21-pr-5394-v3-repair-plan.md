# PR #5394 current-v3 repair plan

## Goal

Make the assessment participant-invitation branch safe to review against the
current `v3` head. Preserve accepted admission records and leaderboard
consent, make verified-account resolution deterministic, remove the retired
invitation logs, and leave the backend code smaller where the behavior is
unchanged.

## Scope and authority

- Repository: `klicker-uzh/klicker-uzh`
- PR: #5394, target `v3`
- Local worktree: `trees/pr-5394`
- Repair branch: `rs/pr-5394-backend-repair`
- PR head at start: `8515e3b07cae4d7824328e601d3f9d1970cb55b0`
- Current `origin/v3`: `f58986faa8cfa4ff78d20a1ebeb1666473343d38`
- The local merge, edits, tests, and read-only reviews are authorized.
- Push to the PR source branch is authorized after the final review and
  verification. Merge, deployment, production or staging access, real
  invitation data, and worktree deletion remain out of scope.

## Current execution update (2026-08-21)

The authoritative `v3` ref is `f58986faa8cfa4ff78d20a1ebeb1666473343d38`.
It is already an ancestor through merge `62ea43185`; no additional base merge
is required. The current local head is `dc506c396`.

The concurrent regression now uses two concurrent service calls through one
shared Prisma query extension. A bounded five-second barrier forces both
initial creates to overlap, and `finally` releases the barrier and awaits both
operations before fixture cleanup. The focused database suite passes 21/21.

The additive migration is clean in the disposable database. Earlier bounded
local query evidence recorded 250 synthetic rows, a backward scan on the
`courseId, invitedAt, id` index with no explicit sort, 0.206 ms planning,
0.069 ms execution, and 68 ms non-concurrent local DDL. These measurements do
not establish production lock duration or query plans.

GraphQL generation is clean on two consecutive runs, GraphQL and Prisma
checks pass, the repository build completes with 23 successful tasks and exit
code 0, and the exact runtime is stopped with zero routes. `check:all` remains
blocked because the analytics image lacks a C compiler while building `pandas`.

## Decisions

Accepted invitations are immutable. A duplicate `PENDING` invitation may
receive a new matriculation number; an `ACCEPTED` invitation returns the
ordinary duplicate result without changing its admission metadata.

Auto-acceptance requires exactly one distinct active participant behind the
verified eligible account records. A missing or ambiguous identity stays
pending. Creating a participation uses the model default (`false`), and an
existing participation is never updated by invitation import.

Assessment invitation imports are bounded at 200 rows before processing. The
Manage CSV flow rejects files above 1 MiB and parsed inputs above 200 rows.
The Manage history uses an additive, stable paginated GraphQL field with a
maximum page size of 50 and keeps the old field and persisted operation intact
for rolling compatibility. Retiring the legacy unbounded field is a separate
follow-up after deployment evidence shows that old clients have drained.

## Slices and acceptance checks

### S1 — Reconcile current `v3`

Merge current `origin/v3`, retain current wiki guidance plus the invitation
contract, and remove both invitation entries under `docs/log/`. Do not restore
the retired log system or the current-v3-deleted wiki indexes.

Acceptance: `origin/v3` is an ancestor of the repair branch, no conflict
markers remain, and the named logs are absent.

### S2 — Repair backend invitation integrity

Update the shared invitation service, its production-capable import script,
database-backed tests, and the domain/API guidance. Reuse one deterministic
verified-account resolver; handle only unique-invitation races as duplicate
results; rethrow unexpected database failures instead of returning raw row
messages; and preserve `Participation.isActive`.

Acceptance: GraphQL package check passes; tests cover new and existing
participation state, duplicate verified accounts, ambiguous and inactive
identities, accepted metadata immutability, unexpected database errors, and
deletion-winning recreation without a stale duplicate result.

### S3 — Bound the Manage invitation surface

Enforce the import and CSV limits, add the additive paginated field, switch
Manage to finite page sizes, regenerate GraphQL artifacts, and document the
rolling compatibility boundary.

Acceptance: requests above 200 rows are rejected before writes; the browser
rejects files above 1 MiB and 200 data rows; page totals and stable ordering
are correct; page transitions retain their selected page; and the old
operation/hash remains unchanged.

### S4 — Simplification and reviews

Commit S2 and S3, then run the simplifier and data-integrity slice reviewer in
parallel over the immutable capacity range. Apply only verified,
behavior-preserving corrections and rerun the focused checks.

### S5 — Integrated verification and final review

Regenerate GraphQL outputs only if the final schema changes, run focused tests,
repository checks, and the applicable build/static checks in the task-scoped
runtime. Stop and verify that runtime by its exact source path. Run one native
Sol final reviewer over the complete committed `origin/v3..HEAD` range,
including the service, import script, schema/resolvers, tests, auth acceptance
comparison, and documentation.

### S6 — Reconcile the advancing `v3` base

Normally merge the verified current `origin/v3` head into this branch without
rewriting history. Resolve only the real documentation and generated-artifact
overlaps, retaining both the invitation and element-batch contracts. Regenerate
GraphQL outputs twice and verify that the second run is clean.

Acceptance: current `v3` is an ancestor, both operation families remain in the
generated artifacts, no conflict markers remain, and `git diff --check` passes.

### S7 — Prove the remaining local invitation release evidence

Add one database-backed concurrency regression using two concurrent service
calls through a shared Prisma extension and `Promise.all` for one verified
participant with an existing inactive participation. Assert one auto-accepted invitation, one duplicate result, one
participation, unchanged `Participation.isActive`, and no errors. This is
shared-service concurrency evidence, not proof from independently launched CSV
processes; do not add a child-process harness.

Against the disposable PostgreSQL database, apply and verify the invitation
history migration, inspect the named composite index, seed bounded synthetic
history, run `ANALYZE`, and capture `EXPLAIN (ANALYZE, BUFFERS)` for the
course-scoped page query. Record the observed row count and bounded local index
DDL timing without extrapolating to production.

Acceptance: the concurrent test and focused invitation suite pass; migration
status is clean; the indexed page query uses the composite index without an
explicit sort; and the evidence states the local-only lock/timing limitation.

### S8 — Integrated release-gate verification

Run generation twice, focused GraphQL and Prisma checks, repository checks,
build, generated-artifact cleanliness, and fresh PR readback. Stop and verify
the exact task runtime. Commit only observed evidence updates, run one native
Sol final review over the complete current-base range, and publish the reviewed
head under the existing branch push authority. Do not merge or deploy.

Terminal: the PR is conflict-free and locally/CI verified for this backend
release-evidence scope. Production migration timing, rollout/rollback,
frontend accessibility/localization, telemetry, operator recovery, and legacy
operation drain remain delivery-pending.

## Test portfolio

The existing lifecycle and authorization tests remain. New coverage protects
leaderboard consent, identity ambiguity, accepted metadata, failure
propagation, the 200-row server boundary, the paginated query, and the
shared-service concurrent import outcome. The
browser flow has been checked for finite page sizes, page-two retention, and
the 1 MiB/200-row guards; a production-sized 51-record Playwright fixture is
now part of the suite, although the disposable runtime lacked Playwright's
headless shell for executing it locally. A second GraphQL generation must be
clean when generation is run.

## Progress

- S1 completed at `d2ca218f2`: current `origin/v3` is an ancestor, the three
  documentation conflicts are resolved, and both invitation logs are absent.
- S2 completed through `8e557c17f`: accepted-invitation metadata and
  leaderboard consent are protected; identity matching requires one distinct
  active participant; participation state is preserved; unexpected database
  errors propagate; and deletion-winning invitation recreation is covered.
- S2 simplification completed with `555392af7` and `8e557c17f`: retry handling is one bounded
  helper, importer metadata is deduplicated once before both mutation paths,
  and repeated Prisma-shaped test setup is centralized. The native Gemini
  route rejected its configured effort; the trusted Luna simplifier and
  slice-reviewer fallbacks found no material concern.
- S2 integrated verification completed at `8e557c17f`: the native Node 24 GraphQL check passed, the
  focused invitation suite passed 18/18 with only Redis connection-refused
  warnings, GraphQL generation passed with unchanged generated artifacts, and
  `git diff --check` passed. Repository commit hooks passed gitleaks, format,
  typecheck, lint, syncpack, AGENTS, and Prisma-sync checks on host Node 26
  with the repository's Node 24 engine warning.
- The full `check:all` attempt remains blocked only by the analytics image's
  missing C compiler while building pandas 2.2.2; no analytics files changed.
  Build, browser, cross-process race, and new-head CI evidence remain
  unverified.
- S3 completed at `5ac9c5136`: assessment imports reject more than 200 rows,
  the browser rejects files over 1 MiB or 200 rows, and the Manage surface
  uses a stable paginated field with finite 10/20/50 page sizes. The legacy
  field and persisted operation remain unchanged for rolling compatibility.
- Current `origin/v3` was merged at `114a6cd90`; it is an ancestor of the
  repair branch at `4e5f1d368ef579aac44027ea73f36b21754397f1`.
- Browser verification covered the seeded Manage flow in English and German,
  including page two at ten entries per page; screenshots are in `/tmp`.
- The focused Playwright capacity test was added at `75e4b60f1`; package
  typecheck and the repository commit checks pass. The disposable runtime
  lacked the Playwright headless shell, so the browser test itself remains a
  CI/runtime follow-up rather than a local pass.
- Fresh simplifier and data-integrity slice reviews found no material concern;
  the data-integrity reviewer noted that a production-sized Playwright
  regression fixture is still a coverage improvement rather than a runtime
  defect.
- The course-scoped invitation history index was added at `73c89d36f`, mirrored
  into analytics, and applied to the disposable database; Prisma validation,
  migration status, package checks, and the focused 20-test invitation suite
  pass. The test fixture was made deterministic at `f32495158`.
- The final current-head build passed with 23 successful tasks in 2m53.857s.
  Fresh native Sol review over `origin/v3..b4d92c8cc` passed with no backend
  invitation findings.
- The reviewed code head `b4d92c8cc` and the subsequent readiness-only
  publication update were pushed to `feat/assessment-participant-invitations`;
  new-head CI is queued or running, with no failed conclusion in the initial
  readback.
- The exact runtime was stopped after the final runtime-dependent checks:
  `devpod status --output json pr-5394` reports `Stopped`, and the exact-path
  workspace readback reports `routeCount: 0` with no hosts.
- Fresh forge readback shows that `v3` advanced to `3f3af8295`, one commit
  beyond the reviewed branch base, and GitHub reports the PR as dirty and
  conflicting. A native Sol planning pass accepted S6–S8 above, with the
  same-process concurrency test explicitly distinguished from cross-process
  evidence. The plan is now executing against the verified current base.

- The current `v3` ref `f58986faa` is an ancestor through merge `62ea43185`.
  The final bounded concurrency test is committed at `dc506c396`; its focused
  suite passes 21/21 after a clean disposable reset. The final simplifier and
  slice re-review, native Sol final review, push, and exact-head CI readback
  remain the next S8 actions.

## Terminal conditions

The local branch is clean, current-v3-based, and verified at `dc506c396`; the
task-scoped runtime is stopped and has no exact routes. Final Sol review,
publication to `feat/assessment-participant-invitations`, and exact-head CI
readback remain. Do not merge or deploy in this task.
