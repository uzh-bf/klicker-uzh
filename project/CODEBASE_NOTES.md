# Codebase Notes

This file has been superseded by the **engineering wiki** at [docs/index.md](../docs/index.md). All previous entries were migrated into the wiki pages (facts) or the matching skill in [.agents/skills/](../.agents/skills/) (procedures, e.g. Playwright authoring gotchas → `klicker-playwright-e2e`).

Do not add new entries here. When you discover a non-obvious pattern during a task, add it to the matching wiki page (grep `docs/` for the symbol or area) in the same change set, per the `klicker-wiki-maintenance` skill.
