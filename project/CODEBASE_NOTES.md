# Codebase Notes

This file has been superseded by the **engineering wiki** in [docs/](../docs/). All previous entries were migrated into the concept pages (facts) or the matching skill in [.agents/skills/](../.agents/skills/) (procedures, e.g. Playwright authoring gotchas → `klicker-playwright-e2e`).

Do not add new entries here. When you discover a durable, non-obvious pattern during a task, add it to the matching concept page (grep `docs/` for the symbol or area) in the same change set, per the `klicker-wiki-maintenance` skill.
