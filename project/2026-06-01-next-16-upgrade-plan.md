# Next 16 Upgrade Plan

## Goal

Upgrade KlickerUZH from Next.js 15.5.x to Next.js 16 as the first major dependency-upgrade slice after the runtime/security baseline PR.

## Non-Goals

- Do not upgrade to TypeScript 6 in this PR.
- Do not migrate from webpack to Turbopack in this PR.
- Do not replace the PWA implementation with Serwist/native service workers in this PR unless the current plugin cannot build.
- Do not upgrade packages published less than 14 days before 2026-06-01 unless the exact version is manually reviewed and pinned.

## Research

### Official Next.js 16 upgrade guide

- Evidence: Next.js 16 upgrade guide, last updated 2026-03-31.
- Decision: use `next@16.2.6` and `eslint-config-next@16.2.6`; both were published 2026-05-07 and pass the 14-day policy.
- Decision: rename real Next entrypoints from `middleware` to `proxy`.
- Decision: replace `next lint` scripts with direct ESLint CLI scripts.
- Decision: opt out of Turbopack with webpack flags for this PR because the repo has custom webpack config and the PWA plugin uses Workbox webpack plugins.
- Decision: add explicit image config for `qualities`; remove deprecated `images.domains` after confirming equivalent `remotePatterns`.
- Risk: App Router async request APIs, metadata image params, and sitemap params are breaking in Next 16. Local scan must prove whether the repo has affected files.
- Source: https://nextjs.org/docs/app/guides/upgrading/version-16

### Package registry metadata

- `next@16.2.6`: published 2026-05-07T19:01:54.751Z; passes 14-day policy.
- `eslint-config-next@16.2.6`: published 2026-05-07T19:01:14.158Z; passes 14-day policy.
- `react@19.2.6`: published 2026-05-06T16:16:47.653Z; passes 14-day policy.
- `react-dom@19.2.6`: published 2026-05-06T16:16:56.080Z; passes 14-day policy.
- `next-intl@4.13.0`: published 2026-05-28T15:37:28.227Z; blocked by 14-day policy.
- `next-intl@4.12.0`: published 2026-05-13T10:23:37.100Z; selected policy-compliant target.
- `@ducanh2912/next-pwa@10.2.9`: published 2024-09-18T18:46:40.557Z; passes policy.

### Compatibility notes

- `next@16.2.6` requires Node >=20.9.0 and peers React/React DOM ^18.2.0 or ^19.0.0. The baseline branch pins Node 24.16.0, so runtime is compatible.
- `next-intl@4.12.0` peers `next` through `^16.0.0` and React through `^19.0.0`; it is the correct policy-compliant bridge.
- `@ducanh2912/next-pwa@10.2.9` peers `next >=14.0.0` and `webpack >=5.9.0`, but its docs/package notes remain webpack/Workbox-oriented. Use webpack fallback in this PR and leave Turbopack/PWA modernization as follow-up.

## Local Findings

- Next apps: `apps/auth`, `apps/chat`, `apps/frontend-control`, `apps/frontend-manage`, `apps/frontend-pwa`.
- Shared Next config: `packages/next-config/index.js`.
- Real middleware files:
  - `apps/auth/src/middleware.ts`
  - `apps/chat/src/middleware.ts`
- `next lint` scripts exist in all five Next app package manifests.
- ESLint config shape differs:
  - `apps/chat` already has `eslint.config.mjs`.
  - `apps/auth`, `apps/frontend-control`, `apps/frontend-manage`, and `apps/frontend-pwa` still use `.eslintrc.json`.
- PWA apps:
  - `apps/frontend-control`
  - `apps/frontend-manage`
  - `apps/frontend-pwa`
- Existing PWA worker outputs under each app's `public/` folder may be regenerated by builds.

## Slices

### Slice 0: Plan Commit

- Do: add this plan file.
- Check: `git status --short`; commit only this file.
- Commit: `docs(project): add next 16 upgrade plan`.

### Slice 1: Package And Config Migration

- Do:
  - Update Next ecosystem dependencies to policy-compliant targets.
  - Update PWA plugin to `@ducanh2912/next-pwa@10.2.9`.
  - Rename middleware files to proxy files and exported functions to `proxy`.
  - Change Next app build/dev scripts to use webpack fallback.
  - Replace `next lint` with `eslint`.
  - Add flat ESLint config files for apps still using `.eslintrc.json` if direct ESLint CLI requires them.
  - Update shared image config with explicit `qualities` and remote patterns.
- Check:
  - `pnpm install`
  - targeted Next app lint/build checks.
- Commit: `chore(deps): upgrade next apps to next 16`.

### Slice 2: Compatibility Fixes

- Do:
  - Fix compile, lint, config, and generated artifact issues found by Slice 1.
  - Search for Next 16 affected App Router APIs: async request APIs, metadata image params, sitemap params, deprecated cache APIs.
  - Confirm PWA service worker generation still includes custom worker imports.
- Check:
  - `pnpm run check`
  - `pnpm run lint`
  - `pnpm run build`
- Commit: `fix(next): resolve next 16 compatibility issues`.

### Slice 3: Full Validation And PR

- Do:
  - Run required install/check/build/audit/E2E commands.
  - Start local test environment when needed.
  - Capture browser screenshots for auth, manage, pwa, control, and chat.
  - Run final security review.
  - Create/update draft PR using `$mr-description-writer`.
- Check:
  - `pnpm install --frozen-lockfile`
  - `pnpm run check:syncpack`
  - `pnpm run check`
  - `pnpm run lint`
  - `pnpm run build`
  - `pnpm audit --audit-level high`
  - `pnpm --filter @klicker-uzh/cypress test:run`
  - `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 pnpm --filter @klicker-uzh/playwright test:run`
  - Browser screenshots for the five Next apps.
- Commit: validation/report/PR updates as needed.

## Progress

- 2026-06-01: Research complete. Target versions selected under 14-day policy. Current branch is `codex/next-16-upgrade`, stacked on `codex/dependency-refresh-lts`.
- 2026-06-01: Active slice is Slice 0. Next step: commit this plan file by itself.

## Follow-Up PRs

- Turbopack migration after webpack-based Next 16 is green.
- PWA modernization or migration to Serwist/native service workers if needed.
- TypeScript 6 migration after Next 16 is validated.
