# Restore shared translation context across Klicker apps

## Approval summary

Restore reliable account creation and remove the shared translation-context split found in the production PWA, Assessment, and Manage bundles. The observed account-page failure is confirmed; other shared components have the same exposure, but their authenticated runtime states have not all been exercised.

Use one cohesive fix PR targeting v3. First reproduce the failure using the production installation and bundling path. Then select the smallest verified fix that makes app providers and shared hooks use the same context, remove the unused username-field hook, and add a non-publishing PR smoke job that runs the production bundler on relevant changes, including draft PRs. Preserve existing languages, messages, rendering behavior, LTI authentication, cookies, account creation semantics, and App Router server/client boundaries. The mandatory iframe test begins with a locally signed synthetic application token at the post-launch page boundary; a complete LMS/platform launch is outside this source package, and no end-to-end LTI platform claim will be made. This work changes no database schema and introduces no external data flow.

The main risk is a dependency-resolution correction affecting multiple apps or React server conditions. Prefer aligning existing peer resolutions; if that cannot produce a single context, evaluate a narrowly scoped resolver correction. Do not assume adding a transpilePackages entry or pinning versions alone solves it. Approval permits narrowly necessary declarations of already-used dependencies, same-version peer alignment, lockfile changes, and the non-publishing PR smoke workflow. If the installer changes the pruned lockfile, approval also permits freezing the install in the affected Dockerfiles after correcting the lockfile and proving those builds pass. New runtime packages, major upgrades, or staging-image publication require a decision.

Approval authorizes local reproduction with synthetic fixtures, source changes, container builds/tests, host browser verification, configured reviews, commits, ordinary branch pushes, and one draft PR. Estimated effort is 2–3 working days if production builds, the existing CI runners, and local seeded services are available; report capability blockers rather than substituting weaker evidence. Stop at a verified draft PR with required CI settled. Merge, release publishing, staging/production deployment, live writes, and production account creation remain separately authorized.

The approved extension preserves a single string-valued `jwt` query parameter through the existing locale-aware course account redirect. The destination remains the existing account page; its signature and scope verification remain authoritative. Forward no unrelated query parameters or course identifier. The separate account-coverage task owns the real-route cookie-free regression.

## Execution details

### Evidence and uncertainty

Production alpha.74 PWA build 1V_uimYWMj9_nQn_UICem returns 500 on direct German and English createAccount, while its data endpoint and login succeed. Logs point to DebouncedUsernameField's unused useTranslations call. Its compiled module reads a context defined inside a bundled next-intl copy, while \_app provides context from external next-intl.

The same server context split and additional callers exist in PWA, Assessment, and Manage. Across the available 24-hour window, 1,479 matching PWA log lines use the username-field frame; these are not unique requests. Other frontend logs contain no matching exceptions. Loader guards can avoid the affected server render. Source inventory finds 31 shared hook-bearing files. No direct shared-hook consumers were found in Auth, Control, or Chat; this is bounded source evidence, not an unconditional guarantee.

The lockfile resolves next-intl through different peer contexts for apps, shared-components, and i18n. Next externalization logic bundles when importer and app-root resolutions differ. Fable checked the same rule in published 16.2.11 source; inspect the exact installer copy during reproduction. The previous alpha.73.3 lockfile already has peer variants. Do not attribute introduction to alpha.74 without a controlled comparison.

PWA, Manage, and Control production use Webpack; their build:test uses Turbopack. Auth and Chat production use Turbopack. Assessment is a separate PWA-derived image and requires its own configuration/build validation.

### Working context and authority

Repository: /Users/rschlae/Git/klicker/klicker-uzh.
Worktree: /Users/rschlae/Git/klicker/klicker-uzh/trees/rs/translation-context-fix.
Branch: rs/translation-context-fix. Target: v3, repository-declared active branch and remote default. Initial base: cbcede79718e8e60ff04d3f8376ab6a3f4bb64ed; revalidate at execution. Worktree initially tracks origin/v3 and is at that revision; primary v3 is 43 commits behind with unrelated user changes. Preserve those changes.

Artifacts root: project/. Canonical plan: project/2026-09-09-translation-context-fix-plan.md. Reviews and values-free build evidence: project/\_local/reviews/ and project/\_local/translation-context/ (ignored). Keep production receipts and host-specific details out of published source; summarize only necessary sanitized facts in the PR.

Authority: the user approved implementation on 2026-09-09, including preservation of the LTI JWT through the course-specific account redirect. Execute through the verified draft PR without renewed approval for source work. Boundary owner: self. Terminal: reviewed, verified coherent draft PR on the existing public remote with CI settled and remaining release gates explicit. Pause: unavailable production-equivalent reproduction; ambiguous fixture ownership or credentials; a required change to auth, cookies, rendering semantics, external providers, major versions, or production state; unresolved independent review.

Single ordinary PR is appropriate: one shared dependency defect, one corrective policy, and its regression protection must land together. No stacked topology is proposed. Full-path workflow applies because bundling crosses app/package boundaries.

### Delegation map

| Workstream                           | Owner    | Dependency and acceptance                                                                                                                        |
| ------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Reproduction and fix choice          | Main     | Own the cross-app resolution decision; demonstrate failing baseline and successful candidate under identical production conditions.              |
| Source correction and focused guards | Executor | After main selects a candidate; own approved manifest/config/component/test paths, return scoped diff and failing-before/passing-after evidence. |
| Cross-app verification               | Executor | After correction; own synthetic verification fixtures and runnable checks, no production access; main verifies browser and build receipts.       |
| Integration and delivery             | Main     | Own review dispositions, CI, source hygiene, draft PR, and exact runtime shutdown.                                                               |

Main retains initial reproduction because it is coupled to the unresolved build-boundary decision. Use one worker at a time where scopes overlap. Native planner reviews this plan; Fable 5.1 supplies explicit independent advice. Neither substitutes for human approval.

### Slice 1: Establish a production-equivalent failing baseline

Route: main. Read rs-local-runtime-lifecycle before starting any runtime. Use the isolated worktree and existing devrouter/container conventions; do not touch shared runtimes or reset existing databases. Provision only task-owned synthetic fixtures in its isolated database. Host Git/forge commands stay on the host; toolchain runs in containers; Playwright remains host-side.

Inspect the actual alpha.74 Docker prune/install/build sequence and exact Next 16.2.11 externalization code. Record Node/pnpm/Next versions, pruned manifests, relevant peer resolution realpaths, build options and source SHA. Reproduce alpha.74 separately from the target branch without overwriting work; use an additional task-owned detached worktree only if required, recording it for later cleanup. Force fresh relevant Turbo tasks; avoid shared-cache deletion and global Docker pruning.

First seek the smallest synthetic server render using the real production output and app provider, retaining a used shared hook as well as the username field. Require the baseline to throw the observed missing-context error. If the minimal probe does not reproduce, use the actual account page under the production-mode app with synthetic backing services. A passing host-only render or Turbopack test build cannot replace this baseline. Record both the pruned input lockfile and post-install lockfile, plus actual next-intl/use-intl/React realpaths for the app, shared-components, and i18n importers. Treat unexpected install drift as a first-class finding. Run one bounded comparison of turbo prune 2.5.6 versus 2.10.11 on the same input, then compare resulting installer resolution; these are the relevant historical build-tool versions. Reuse the pinned historical versions only in an isolated diagnostic container. Historical attribution is optional and cannot delay the corrective package indefinitely. Capture whether client output shares one use-intl context before and after; do not infer client failure from a server-only exception.

Acceptance: reproducible failure, production-like peer topology and bundler, plus one attributable input/output receipt. If current origin/v3 is already fixed, identify the responsible diff and propose promotion of existing source instead of duplicating a fix; do not assume deployment authority.

### Slice 2: Correct translation resolution and guard the contract

Route: executor after main specifies the chosen patch. Candidate paths: root package.json or pnpm-workspace.yaml only if needed for peer alignment; relevant app/package manifests; pnpm-lock.yaml; affected Dockerfiles only for evidence-triggered frozen installation; packages/next-config/index.js only if resolution correction requires it; packages/shared-components/src/DebouncedUsernameField.tsx; existing test/build utility locations.

Compare resolution of next-intl, use-intl and React from each actual provider and shared importer. Include packages/i18n's runtime IntlErrorCode import and shared-components. Preserve chat's react-server conditions and next-intl subpaths. Prefer the smallest declaration/settings change that aligns existing peer graphs. Regenerate the lockfile using the pinned package manager in the container; restore unrelated churn. Avoid a global assertion requiring every workspace Next version to become identical.

If peer alignment is insufficient, test adding next-intl to transpilePackages as one bounded fallback, checking that both next-intl paths converge on a single bundled use-intl context. This is a hypothesis, not an assumed fix; an earlier advisor warned that bundling multiple copies alone is insufficient. Require actual server/client context identity and Chat react-server proof. Do not attempt serverExternalPackages as a workaround for the mismatched realpath: the resolution failure occurs before that opt-out. If neither peer alignment nor this bounded fallback preserves behavior, return the specific design decision for approval; aliases and condition changes are outside this plan. Do not switch production bundlers or disable SSR to hide the defect.

Remove the unused hook/import in DebouncedUsernameField, preserving form behavior. Keep a regression probe on a shared component that still uses translations, so the test cannot pass merely from removing that one call. Add a fast provider/importer realpath guard for the peer-alignment solution, demonstrated failing on the current installed graph and passing on the corrected graph, including the pruned installer. Scope it per consuming app rather than requiring one workspace-wide Next version. If the validated fallback intentionally bundles multiple next-intl realpaths, replace the equality assertion with a focused bundled context-identity guard that enforces that selected solution; do not leave a knowingly false realpath assertion. Keep the production behavioral provider/consumer render test as independent proof. Do not assert chunk IDs, filenames, counts, prose, hashes, or incidental lockfile layout as functional contracts.

Acceptance: same baseline changes from the observed error to translated server-rendered output; fail again when the corrective resolution change is reverted in a disposable test scope while the unused-hook removal remains. Relevant checks pass before a scoped conventional fix commit. Dedicated simplifier and one slice-reviewer cover maintainability, dependency resolution, conditional exports and cross-app compatibility on the committed range.

### Slice 3: Verify the complete affected boundary

Route: executor for reusable synthetic checks; main owns evidence acceptance. Use actual production builds, not build:test as a proxy. Retain the fast Turbopack suite. Add a dedicated non-publishing PR smoke job using existing runner and security conventions, initially building the affected Webpack consumer contexts (PWA and Manage) and invoking the shared-hook SSR regression probe. Use .github/workflows/test-intl-production.yml, invoking .github/scripts/intl-production-smoke.mjs from the checked-out candidate rather than a v3-pinned reusable action. Trigger pull_request events opened, synchronize, reopened and ready_for_review targeting v3, with no draft exclusion. Relevant path filters cover apps/frontend-pwa/**, apps/frontend-manage/**, apps/frontend-control/**, apps/auth/**, apps/chat/**, packages/**, package.json, pnpm-lock.yaml, pnpm-workspace.yaml, .npmrc, turbo.json, tsconfig.json, .github/workflows/test-intl-production.yml, .github/scripts/intl-production-smoke.mjs and its fixture/guard paths under .github/fixtures/intl-production/\*\* and util/test-intl-resolution.mjs. Use an independent GitHub-hosted ubuntu-24.04 runner, contents: read, actions/checkout with persist-credentials: false, and candidate ref github.event.pull_request.head.sha. Never use pull_request_target or a persistent self-hosted runner for this candidate code. Record the checked-out HEAD and harness file digest in the receipt; verify the receipt HEAD equals the requested PR head. Two matrix jobs, intl-production-smoke (frontend-pwa) and intl-production-smoke (frontend-manage), are mandatory acceptance checks for this package; this does not authorize changing branch-protection settings. Concurrency cancels only superseded runs of this same workflow/PR. No secrets, registry push, staging mutation or production configuration values. Existing staging workflows skip drafts and their filters miss these shared paths; leave those publishing workflows unchanged. Both new matrix jobs must actually succeed on the draft PR candidate; inherited/skipped CI is insufficient. If GitHub requires an authorized human to approve the untrusted workflow, report that exact platform gate and preserve the run rather than bypass it. Full six-build coverage remains local/container verification; CI retains the targeted production-bundler regression lane. This new workflow consumes existing hosted CI build capacity and is explicitly part of the requested approval.

| Surface             | Required acceptance                                                                                                                                                                                                                                                                                          |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| PWA account/profile | DE and EN direct createAccount return 200, expected form is interactive; a locally signed synthetic application-token iframe entry reaches it, account creation and profile continuation work without context errors; direct profile edit and validation retain behavior.                                    |
| PWA learning UI     | Synthetic leaderboard, one live question, practice/group question rendering and evaluation; exercise each distinct shared hook/render family with minimal fixtures, including charts/table hooks where consumed. Account for all 31 inventory files by exercised path or a specific source-proven exclusion. |
| Manage              | Synthetic seeded lecturer: first-login/shortname settings, representative DataTable with translations, leaderboard/chart evaluation and question preview. Verify initial server response and hydrated interactions with browser/server errors captured.                                                      |
| Assessment          | Own production configuration/image; account-creation redirect stays intentional; synthetic assessment question path and shared components render. No real assessment attempts or participant records.                                                                                                        |
| Auth, Control, Chat | Their native production bundlers; provider/shared-boundary render smoke if imports exist and representative synthetic entry routes. Chat checks locale/server-client provider integrity without any paid LLM call or external content.                                                                       |

The two CI jobs use their app Dockerfile prune/install/build sequence and run the shared-hook regression probe against that resulting output; a full-workspace Webpack install cannot substitute. The harness records pruned input and post-install lockfile identities, importer realpaths and NODE_ENV=production. All build/runtime URLs point to local synthetic fixtures, and external telemetry/feature-flag lookups are disabled or served locally. Use a minimal test entry only inside the disposable Docker build context when necessary to reach the real provider and a used shared hook; it must preserve the app/package resolution layout, never ship in application source/output releases, and must not replace the real-page browser matrix. Fixtures live under .github/fixtures/intl-production/; use existing signing and test tools, no new library or service dependency.

Full build set: PWA, Assessment, Manage, Control, Auth, Chat. Produce an app-by-app receipt including exact source/build identity, bundler, configuration, command, test counts, server/client error capture and outcome. Compiled-source inspection corroborates behavior but cannot alone prove absence; account for both development and production conditional branches where the fix touches resolution.

Use host agent-browser for required UI verification with screenshots and host pnpm playwright:host for regression checks. Keep account writes only in the isolated synthetic database. Record the synthetic application-token issuer, fixture ownership and allowed redirects. Add the bounded post-launch fixture under .github/fixtures/intl-production/ with the browser test at playwright/tests/intl-production.spec.ts, using existing signing utilities and a test-owned secret. The mandatory source-package contract is the signed application-token iframe flow, account creation, continuation, redirect boundaries and server/browser error capture. Complete platform-launch verification is outside this package and does not block draft-PR completion; never label this post-launch proof as an end-to-end LMS/LTI launch test. A full platform launch belongs to the separately approved staging/production acceptance using an identified test integration. Unavailability of the mandatory synthetic post-launch fixture does block source acceptance; production tokens cannot substitute. Existing passing unrelated tests may be reused only when content/toolchain/environment boundaries match.

Acceptance: production-mode regression checks pass for all six builds; all inventory entries dispositioned; no uncaught missing-context errors in exercised paths. Unvisited arbitrary routes are not claimed as proven. Run applicable repository-native build/check/format/lint/syncpack and staged gitleaks checks, with container equivalents for host hooks. Run scoped relevant security checks without starting a broad security audit.

### Slice 4: Integrate, review, and deliver source

Route: main. Document the non-obvious bundling cause and production/test divergence in the relevant existing engineering guide or a focused docs/solutions page; no ceremonial ADR or global agent-file expansion. Persist review evidence under ignored project/\_local/reviews. Obtain one integrated final review through configured Claude Opus 5 after checks, covering correctness, dependency safety, conditional exports, coverage and scope. Fix verified findings within the same package, repeating only affected checks/review portions.

Inspect every changed hunk, comment, generated file and staged payload for scope and data hygiene. Commit the approved plan first, then coherent source/testing changes. Push normally to rs/translation-context-fix and open/update a draft PR against v3 using the repository PR-writing workflow. Include the known incident, systemic correction, production-build evidence and remaining release gates. Complete required CI and ordinary feedback; use standing-authorized final AI review per repo policy once applicable conditions hold. No merge or ready-for-review transition without the user's explicit instruction.

After the final runtime-dependent check, stop the exact task-owned runtime and verify it stopped. Keep worktrees and artifacts; deletion requires a separate exact cleanup approval. Update Progress and return the draft PR plus evidence links.

### Conditional export and build-mode constraint

The shared webpack config currently adds the development condition in every environment. next-intl/use-intl publish different development and production exports. Leave that existing condition override unchanged in this package; record any transition from a bundled development copy to an external production copy and verify translation/error behavior under the selected actual production mode. Broader conditional-export cleanup is a follow-up decision, not incidental cleanup. packages/i18n imports IntlErrorCode but does not itself create a hook consumer; include it in the installation contract without claiming another observed page failure.

### Separately approved release and production validation

This section is a proposal for a later approval, not authority under the current implementation approval. Once source is accepted, name the exact commit, images/digests, release action, staging targets and production targets in a separate rollout request. Capture current image and GitOps revisions and a compatible rollback target; do not assume alpha.73.3 is known good. No migrations are planned.

Validate the release on staging with synthetic DE/EN direct and LTI entry plus the representative cross-app matrix before proposing PRD. Production approval must name deployment actions and whether synthetic account/LTI writes are permitted. After an authorized rollout, distinguish image rollout health from acceptance: verify build/digest, anonymous account-page 200, approved synthetic flows, and matching error rates over a named observation window. If acceptance fails, stop and invoke only the specifically approved rollback sequence. Existing source revert alone is not a deployed rollback.

## Progress

Execution approved on 2026-09-09. The plan and source slice are committed on rs/translation-context-fix. Source commit: 11dbb6de92a27eb2f1c6cde68bc9999edd1d183b. The root-cause note is committed separately as 4ae89d0c0e; the branch is four commits ahead of origin/v3 and zero behind at the latest fetch. No PR exists yet.

The production-pruned baseline reproduces account-page 500. Aligning existing Babel and i18n Next/React peer resolutions fixes that page while its original hook remains. The minimized lockfile passes frozen pruned installation. The source also removes the unused username hook and preserves a single JWT through the course redirect. Direct EN/DE final PWA SSR and three actual HTTP redirect cases pass.

All six local production runner builds pass: PWA, Assessment, Manage, Control, Auth and Chat. Anonymous entry probes pass with networking disabled; Assessment retains its intended account-page redirect. Final PWA DE desktop and EN mobile screenshots show the styled form; inputs and disclosures respond without uncaught browser errors. Account registration, profile continuation and the full shared-consumer matrix are not yet proven. Browser and probe runtimes are stopped.

Main owns dependency correction, source checks, six-app receipts and integration. The harness worker was stopped after the narrowed assignment produced no artifacts. Main now owns the translation regression harness and CI files; both production fixture builds and EN/DE translated SSR probes pass. Restoring the original dependency graph with the hook removal retained reproduces the missing-context 500. The account-coverage task retains registration and synthetic post-launch tests. Its isolated services are healthy after the user approved a task-local subnet. That task owns all runtime changes, fixture creation and resets. Its production browser launcher remains in progress; development readiness is not production acceptance. No existing network was deleted.

The native simplifier completed with no recommended reduction. The source slice risk review completed with no findings after its sole Docker COPY concern was verified and retracted. Both clean committed PWA and Manage production smoke runs pass at 575d32444659ec2b10d42139126931f4ab8479a5, with EN/DE translated SSR, aligned imports and unchanged pruned installation locks. These local arm64 runs are not GitHub CI receipts.

The account task reports and its saved logs confirm 35 package checks and 93 host contract tests passing on the integrated source. Remaining container lint, Syncpack and repository-contract checks also pass. The host-only Devrouter contract failed when initially run in the container and passed through the correct host command. These applicable checks are reused; authenticated browser acceptance remains pending.

Remaining gates: synthetic authenticated and iframe flows; complete shared-consumer coverage; integrated final review after full acceptance; and draft PR CI. Evidence is in [implementation receipts](_local/translation-context/implementation-evidence.md). Delivery remains pending, with merge and deployment outside current authority.

### Review provenance

Fable 5.1 high, through the trusted Claude CLI frontier-escalation role, completed a read-only challenge with DONE_WITH_CONCERNS. Its accepted findings cover installer drift, the CI trigger gap, a fast installation guard, a bounded bundling fallback, and export-condition evidence. Native planner round 1 returned REVISE; round 2 approved the revised contract. These are reviewer conclusions, not execution or deployment approval.

- [Fable findings and dispositions](_local/reviews/2026-09-09-translation-context-fable.md)
- [Native plan-hardening transcript](_local/reviews/2026-09-09-translation-context-plan-hardening.md)
- [Investigation and shared-component inventory](_local/translation-context/investigation.md)

The optional AGY structured-review route remains unqualified per the same-day model-routing guidance; no successful AGY rival review is claimed. Fable was explicitly requested and completed. Browser consultation was not used because complete review evidence includes private runtime observations without authorization for that destination. Required native planner review completed successfully.
