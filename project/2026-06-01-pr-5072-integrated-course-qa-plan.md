# PR #5072 Integrated Course Q&A Plan

## Identity

- Plan: `project/2026-06-01-pr-5072-integrated-course-qa-plan.md`
- Branch: `codex/course-qa-takeover` (updates remote `course-qa`)
- Target: `v3`
- PR: [#5072](https://github.com/uzh-bf/klicker-uzh/pull/5072)
- Prior checkpoint: local `course-qa` at `cc0c8cf29a`; remote review at `c389b4ee8d`

## Goal

- Problem: Course Q&A exists as separate page/link. User wants contextual integration beside content on desktop, sheet-style on mobile.
- Do: Reuse existing Course Q&A behavior inside existing learning surfaces.
- Outcome: Q&A visible next to course content where useful; `/qa` route remains fallback/deep link/embed target.

## Non-Goals

- No schema redesign unless required by UI.
- No new dependencies.
- No full Manage redesign in first slice.
- No delete/upvote backend hardening in this UX slice; tracked as prior review finding.

## Constraints

- Repo: Next.js Pages Router, Apollo, Tailwind, UZH design system.
- Verification: run focused TypeScript/build checks where feasible; browser screenshots required for UI changes.
- Context7: unavailable in current MCP tool set after discovery attempt. Use local code patterns and fetched web interface guidelines.
- Dev env: Manage port 3002 occupied locally, use alternate port plus backend origin override if screenshots need Manage.

## Research

- Source: Vercel web interface guidelines fetched 2026-06-01.
- Relevant: contextual state should reflect URL, links for navigation, forms need labels, focus states, long user content needs wrapping, dates should use `Intl.DateTimeFormat`.
- Applicability: Directly relevant to PWA/Manage UI polish; no external product research needed for first integration slice.

## Progress

- 2026-07-23: Takeover started in repo-local worktree `trees/course-qa-takeover`; legacy worktree preserved.
- 2026-07-23: Slice 1 active. Reconcile remote review and seven unpublished UX commits, sync current `v3`, resolve conflicts, regenerate derived GraphQL files, and establish a fresh verification baseline.
- 2026-07-23: Slice 1 implementation done. Preserved remote review `c389b4ee8d`, merged `v3` at `c8de9c8978`, retained the contextual Q&A rail alongside the new embedded-practice behavior, combined participant discussion and credential relations, regenerated GraphQL artifacts, and mirrored the discussion schema into analytics after the pre-commit sync guard exposed the missing generated files. Verification passed with Node `24.16.0` and pnpm `11.5.0`: Prisma check, Prisma sync guard, GraphQL check, PWA check, Manage check, and `git diff --check --cached origin/v3`. Next: commit the sync, run independent review and simplification, then integrate accepted findings before the merge-blocker slice.
- 2026-07-23: Slice 1 review done for merge commit `3b8d18485`. Independent review found no merge-introduced defects and confirmed the combined Prisma, analytics, generated GraphQL, and embedded/contextual UI behavior. Simplification review removed unnecessary memoization for primitive discussion URLs and nullable expiry-clock state. Runtime browser evidence remains a required finish-gate item after the merge blockers and pre-rollout fixes are complete.
- 2026-07-23: Slice 2 active. Fix the three verified merge blockers: provide the GraphQL discussion tests with a non-production `APP_SECRET`, append cursor pages instead of replacing the first page, and localize the remaining English-only Q&A strings.
- 2026-07-23: Slice 2 implementation done. PWA and Manage focused ESLint and type checks, GraphQL type checking, Prettier, and `git diff --check` pass. The PWA has no unit-test harness for the Apollo merge callback; verify pagination with more than 20 threads in the mandatory browser slice. The discussion integration suite still requires an isolated local test database and will run with the runtime verification environment.
- 2026-07-23: Slice 2 review found no CI, translation, ICU, or accessibility defects. Accepted pagination findings: extra pages now stop background polling, overlapping page requests are guarded and deduplicated, and upvote mutations rely on Apollo normalization instead of refetching the first page. Thread and reply creation still refresh intentionally to surface newly created content. Browser coverage will verify pagination, polling stability, and repeated-click behavior.
- 2026-07-23: Slice 3 active. Enforce lecturer-only write access for the aggregated discussion overview and fail both vote mutations closed when either the rollout or runtime course Q&A gate is disabled. Add integration assertions for participant overview denial and both gate combinations.
- 2026-07-23: Slice 3 implementation done. GraphQL type checking, Prettier, and `git diff --check` pass. Added integration assertions but runtime execution is pending: devrouter created an isolated database, then its Postgres bootstrap stalled during temporary-server shutdown and the local OrbStack Docker API stopped responding to inspection calls. Do not use another worktree's database; retry the isolated runtime before the finish gate.
- 2026-07-23: Slice 3 review done for `25fcdeebb`. Independent authorization/security review found no regression. Accepted one simplification: the rollout/runtime gate now has a single predicate reused by all seven discussion entry points. Kept the two integration scenarios self-contained instead of adding a fixture abstraction for two uses. GraphQL type checking and Prettier pass; isolated runtime execution remains pending.
- 2026-07-23: Slice 4 active. Preserve mathematical comparison text, reject content beyond the existing 4,000-character client limit instead of silently truncating it, and atomically cap stored replies at the 50 replies that the API can return.
- 2026-07-23: Slice 4 implementation done. GraphQL type checking and the three new integration scenarios pass against the isolated devrouter PostgreSQL/Redis services, including two concurrent replies racing for slot 50. The full discussion file reaches 13/16 passing; three pre-existing v3-drift failures remain in the microlearning fixture, PostgreSQL catalog type handling, and missing derived-permission recomputation. Devrouter bootstrap itself still fails earlier on the unrelated `@klicker-uzh/hatchet` Rollup parser, so tests run inside the isolated app container with temporary test-only Redis forwards.
- 2026-07-23: Slice 4 review found that reply deletion still recomputed `replyCount` from a stale snapshot, which could undercount during concurrent creation and later permit an invisible reply 51. The adjustment uses guarded atomic create/decrement operations on the same thread row and extends the integration scenario to cover concurrent create/delete plus exact creation/deletion event counts.
- 2026-07-23: Slice 5 active. Repair the three v3-drifted discussion integration scenarios without changing product behavior: explicitly seed an empty microlearning stack, cast PostgreSQL catalog `name` values for Prisma's driver adapter, and recompute owner permissions before creating an embed link.
- 2026-07-23: Slice 5 implementation done. The test now explicitly enables anonymous posting for its anonymous external-block case as well. GraphQL type checking passes and all 16 discussion integration scenarios pass against the isolated worktree PostgreSQL/Redis services.
- 2026-07-23: Slice 5 review done for `1bd89616b`. Correctness review found no issue. Rejected removing the PostgreSQL `::text` casts: the isolated runtime reproduced Prisma's `UnsupportedNativeDataType: name` failure without them, while the casted suite passes 16/16.
- 2026-07-23: Slice 6 active. Keep rejecting every anonymous request after a Redis rate limit is exceeded, but persist only the first rejection event per window to prevent rejected traffic from amplifying database writes.
- 2026-07-23: Slice 6 implementation done. The focused isolated integration scenario accepts the first anonymous post, rejects the next two, and verifies exactly one `ANON_RATE_LIMITED` event with the expected scope-window metadata. GraphQL type checking passes.
- 2026-07-23: Slice 6 review accepted two findings: cover course/IP/TTL-reset behavior and avoid unconditional space/scope upsert writes before rejecting repeated traffic. Existing spaces/scopes are now read and only created or relabeled when needed, with unique-conflict recovery for concurrent first use. The audit event remains best-effort; adding a durable outbox for pilot rate-limit observability would be disproportionate. Both expanded isolated integration scenarios pass, including unchanged parent timestamps on repeated scope-window rejection.
- 2026-07-23: Slice 6 follow-up review done. Unique-conflict recovery now preserves scope relabeling, anonymous posting requires the existing scope minted by the embed generator so stale tokens cannot recreate deleted parents, and course/IP policy tests are isolated. GraphQL type checking and all 20 isolated discussion scenarios pass.
- 2026-07-23: Slice 7 active. Make repeated concurrent thread and reply vote toggles idempotent by deriving counter changes from atomic vote insert/delete results and guarding counters against deleted content and negative values.
- 2026-07-23: Slice 7 implementation done. Concurrent duplicate thread/reply upvotes and removals now use insert/delete result counts to change counters exactly once; updates ignore deleted targets and never decrement below zero. GraphQL type checking and all 20 isolated discussion scenarios pass, including one vote row transition and one audit event per accepted upvote.
- 2026-07-23: Slice 7 review done for `ebd18de9c`. Correctness review found no defect. The only simplification replaces four repeated two-request test blocks with one typed concurrency helper while retaining explicit state and event assertions.
- 2026-07-23: Slice 8 active. Replace the silently clamped lecturer overview request with typed, cursor-based pagination that merges source groups without duplicate threads and exposes a localized continuation control.
- 2026-07-23: Slice 8 implementation done. The Manage overview now requests the supported 20-thread page with the generated sort enum and merges later groups and threads by stable keys. The German and English continuation labels and Manage type check pass; browser verification will cover a course with more than 20 threads.
- 2026-07-23: Slice 8 review found that polling or Refresh could replace Apollo's merged cache while pagination was active. The adjustment keeps polled page one and accumulated cursor pages separate, merges them with a pure deduplicating helper, preserves expanded pages across Refresh, reports initial and refresh errors, and keeps an accessible continuation/status region.
- 2026-07-23: Slice 8 browser verification passed against the isolated seeded Manage app. The overview showed 20 of 27 threads, expanded to all 27 with a disabled localized completion control, and retained all 27 after Refresh. Evidence: `project/_local/course-qa-screenshots/06-manage-overview-first-page.png` and `07-manage-overview-all-threads.png`.
- 2026-07-23: Slice 9 active. Replace the practice stack's mobile route link with one responsive in-page discussion section: a sticky rail beside content on desktop and a collapsed disclosure below content on mobile. Also reset per-post anonymous choices after successful submission and suppress duplicate source/scope labels.
- 2026-07-23: Slice 9 implementation and browser verification done. One discussion panel now renders as an always-open sticky desktop rail and a native collapsed mobile disclosure, with the standalone route retained only as fallback. Successful anonymous choices reset per post, generated sort typing replaces the client cast, and identical source/scope labels render once. PWA type checking and scoped ESLint pass. Evidence: `project/_local/course-qa-screenshots/08-practice-mobile-collapsed.png`, `09-practice-mobile-expanded.png`, and `10-practice-desktop-responsive-rail.png`.
- 2026-07-23: Slice 9 simplification review found no worthwhile implementation reduction, but correctly flagged the Cypress workflow's removed mobile link dependency. The adjustment now opens the native in-page disclosure, asserts that the route remains outside `/qa`, and posts the stack-scoped thread there.
- 2026-07-23: Slice 9 correctness review found that the panel was visible before evaluation and that viewport detection hid the desktop rail during SSR/first paint. The adjustment gates both responsive layouts on completed stack evaluation, keeps desktop visibility entirely in CSS, and uses a labeled mobile toggle with `aria-expanded` and `aria-controls`. Cypress now also asserts that Q&A is absent during answering.
- 2026-07-23: Slice 9 adjustment verification passed. PWA and Cypress type checks, scoped PWA ESLint, formatting, and `git diff --check` pass. Browser verification confirms no discussion rail before evaluation, a collapsed/expandable mobile panel after evaluation, and an immediately visible desktop rail beside the evaluated stack. Screenshots `08` through `10` were replaced with these gated states.
- 2026-07-23: Slice 10 active. Replace the microlearning evaluation's repeated per-result discussion buttons with one contextual stack selector in the Q&A section. Keep the section as a desktop rail and move a collapsed in-page disclosure before the long result list on mobile.
- 2026-07-23: Slice 10 implementation and browser verification done. The 34 repeated action buttons are gone; one labeled selector controls the sticky desktop Q&A rail. On mobile, a collapsed accessible Q&A section now precedes the long result list and expands in place. Switching from stack 64 to stack 79 remounted the panel with input ID `microlearning-evaluation-qa-79-thread-content`. PWA type checking and scoped ESLint pass. Evidence: `project/_local/course-qa-screenshots/11-microlearning-evaluation-desktop.png`, `12-microlearning-evaluation-mobile-collapsed.png`, and `12-microlearning-evaluation-mobile-expanded.png`.
- 2026-07-23: Slice 10 review adjustment done. The mobile visual and DOM reading order now agree while the desktop grid keeps results left and Q&A right; stack options include stable ordinals so duplicate display names remain distinguishable; and the disclosure has a 44px minimum target. The microlearning Cypress workflow now verifies source order, disclosure state, and context switching with Q&A enabled. PWA and Cypress type checks, scoped PWA ESLint, formatting, `git diff --check`, and refreshed authenticated desktop/mobile screenshots pass.
- 2026-07-23: Embed browser verification done against isolated production-mode Manage and PWA builds after `.orb.local` HMR reloads made the dev-server interaction nondeterministic. The lecturer generated a 48-hour external-block link with anonymous posting enabled; a separate unauthenticated session created an anonymous thread and reply, and both successful forms cleared their drafts and reset their anonymous checkboxes. The signed token is redacted in evidence. Screenshots: `project/_local/course-qa-screenshots/14-manage-embed-generated-redacted.png`, `15-anonymous-embed-empty.png`, and `16-anonymous-embed-thread-reply.png`.
- 2026-07-23: Focused E2E reconciliation active. The Course Q&A Cypress workflow now uses the integrated course overview panel for normal student actions, reserves direct `/qa` navigation for fallback and gate checks, opens the secondary Manage embed generator explicitly, and verifies unauthenticated anonymous thread/reply creation plus successful form resets. Manage and Cypress type checks, formatting, and `git diff --check` pass; runtime execution follows on the isolated services.
- 2026-07-23: Focused E2E review adjustment done. The embed workflow now scopes replies to the created thread without parsing test IDs, uses visible enabled checkboxes without forced actions, proves embedded mode suppresses the normal app header and footer, and corrupts a non-terminal JWT signature character so the rejection check cannot preserve a valid signature through Base64URL padding. Redundant Cypress isolation and test-only Manage hooks were removed. Cypress type checking, formatting, and `git diff --check` pass.
- 2026-07-23: Focused E2E runtime reached real browser execution after installing the missing Cypress Linux runner dependencies in the ephemeral app container and mapping loopback Manage/PWA origins for backend cookie routing. TypeScript 6 required the same Cypress `ignoreDeprecations` compatibility setting already present on the pending Next 16 stabilization branch. With that applied, the first 10 integrated scenarios passed before the Electron renderer exhausted the machine's current headroom; split the state-coupled 21-test workflow into independent self-seeding specs before the next run.
- 2026-07-23: Focused E2E isolation done. The state-coupled workflow is now four independently seeded specs for course, practice, embed, and rollout gates. The embed workflow uses two external scopes because the anonymous policy permits one post per fingerprint and scope in the 90-second window, and it waits for the participant session before testing an identified embed post. Generated embed-link fixtures remain ignored. Completed Chromium runs: course `8/8`, practice `7/7`, embed `6/6`, and rollout gates `7/7`; Cypress type checking, formatting, and `git diff --check` pass. A later course rerun passed its first two scenarios before the host-memory-constrained renderer crashed, with no assertion failure. Next: commit this test slice, run exact-commit correctness and simplification reviews, then proceed to the final verification gates.
- 2026-07-23: Focused E2E isolation review done for `c2ed18379`. Separate correctness and simplification reviewers found no issues at the reporting threshold and retained explicit seed setup per spec. Fresh backend verification passes all `20/20` discussion integration scenarios against the isolated PostgreSQL/Redis environment. Next: run final security, whole-branch, and strict maintainability gates, then update the draft PR with whole-branch evidence and screenshots.
- 2026-07-23: Final review hardening slice active. Independent branch and maintainability reviews confirmed that rejected participant writes could create a discussion space before authorization, delete operations bypassed the rollout/runtime gates, and concurrent thread deletes could emit duplicate events. The backend now authorizes identified participants before persistent side effects, applies both course gates to thread/reply deletion, and guards thread deletion inside the transaction. GraphQL type checking and all `22/22` isolated discussion integration scenarios pass, including rejected-space creation, disabled deletes, and concurrent delete idempotency. Next: commit and review this slice, then address the remaining decision-free UI and API findings.
- 2026-07-23: Final review hardening backend review done for `cd4489c17`. The correctness reviewer found no issue. The simplification reviewer removed the one-use actor binding while preserving the explicit pre-side-effect participant authorization. Next: verify and commit the adjustment, then fix the Q&A-only course branch and Manage permission presentation.
- 2026-07-23: Final integration-gap UI slice active. Q&A-only courses now enter the integrated course layout even without description, assessment, or gamification content, and the Manage Q&A tab is hidden unless the current course user has editor rights matching the overview/embed WRITE checks. Cypress adds explicit Q&A-only and READ-only course cases; PWA, Manage, and Cypress type checks pass. Browser execution reached the seeded cleanup but the first pre-existing lecturer test redirected to Auth before the new cases despite three isolated runtime configurations; a separate Chromium attempt crashed at startup. Root causes remain unresolved, so the two new cases do not yet have runtime proof. Next: commit and review the slice with runtime evidence explicitly pending.
- 2026-07-23: Final integration-gap UI review found that a `?tab=discussions` deep link could still mount the Manage Q&A content for a READ-only user even though its trigger was hidden. One editor-aware availability value now gates the controlled tab value, trigger, and content. The READ-only regression asserts both overview and embed controls are absent, and the Q&A-only test captures and restores its course state in `afterEach` even after assertion failure. Next: rerun focused checks and commit the review adjustment.
- 2026-07-23: Embed bearer-transport security slice implementation done. New links move the signed token from the query string into the URL fragment; the PWA captures and immediately removes either fragment or legacy query tokens before loading discussions, applies a no-referrer policy, and forces token-producing or token-bearing persisted GraphQL operations to POST. ADR 0001 records the alpha transport and revocation boundary. PWA, GraphQL, and Cypress type checks, scoped PWA ESLint, formatting, `git diff --check`, and all `22/22` isolated discussion integration scenarios pass. Next: commit and review the exact slice.
- 2026-07-23: Embed bearer-transport review adjustment done. Both exact-commit reviewers found that direct `history.replaceState` cleaned the visible URL but retained Next.js internal history values that could restore a token. Cleanup now uses a shallow router replacement after one-time token capture, and Cypress checks that serialized browser history contains no token. PWA and Cypress type checks, scoped ESLint, formatting, and `git diff --check` pass. Next: commit the adjustment and continue to the stack-evaluation API gate.
- 2026-07-23: Stack-evaluation API gate implementation done. Participant stack-scoped listing, thread/reply creation, voting, and author deletion now require a persisted response tied to an element instance in that stack; course-level and lecturer access remain unchanged. The write gate runs before discussion-space creation. GraphQL type checking and all `22/22` isolated discussion integration scenarios pass, including denial with zero persistent side effects before evaluation and denial across every stack operation when evaluation state is absent. Next: commit and review the exact slice.
- 2026-07-23: Stack-evaluation review adjustment done. Correctness review found that a single response could unlock a multi-element stack because stack submission accepts response subsets. The gate now requires a non-empty stack and a participant response for every element; the two-element fixture proves zero-side-effect denial before and after a partial submission. Simplification review moved the repeated delete scope guard into shared delete authorization and split the mixed test into focused denial and table-driven compatibility scenarios. GraphQL type checking, formatting, `git diff --check`, and all `23/23` isolated integration scenarios pass. Next: commit the adjustment and continue with remaining decision-free UX/API findings.
- 2026-07-23: Course-wide embed slice implementation done. The existing secondary Manage embed generator gains an explicit scope control: external block remains the default, while entire-course mode omits external identifiers and signs the canonical course scope. Generated GraphQL artifacts are current; Manage, GraphQL, and Cypress type checks, scoped Manage ESLint, formatting, and all `23/23` isolated discussion scenarios pass, including course-scope anonymous posting. Two Chromium runs reached seeded cleanup but redirected to Auth before the Manage assertion, including one with the loopback issuer explicitly aligned; no new UI assertion failed, but runtime screenshot proof for this control remains blocked by the known Auth setup issue. Next: commit and review the exact slice.
- 2026-07-23: Course-wide embed review adjustment done. Correctness review found that changing the existing persisted operation would reject requests from already-loaded Manage clients during rollout skew. The external-block operation and its original `ffcbc56d...` hash are preserved, while course-wide generation uses a separate persisted operation and hash; a production-manifest assertion proves both hashes resolve. Simplification review consolidated embed-mode branching and updated the expanded Cypress and integration-test names. GraphQL generation/build, Manage and Cypress type checks, scoped Manage ESLint, formatting, `git diff --check`, and all `23/23` isolated discussion scenarios pass. Next: commit the adjustment, then remove the remaining discussion scope type cast.
- 2026-07-23: Discussion scope typing slice implementation done. The shared parser now returns the generated GraphQL input type and enum, removing the thread-creation `as any` cast while preserving the same serialized scope values. Shared-components and PWA type checks, scoped PWA ESLint, formatting, and `git diff --check` pass. Next: commit and run exact-commit correctness and simplification reviews.
- 2026-07-23: Discussion scope typing review done for `a4ca5d4ba`. Correctness and simplification reviewers found no issues; dependency-aware checks and runtime scope serialization passed.
- 2026-07-23: Localized semantic-label slice implementation done. Student and lecturer views now translate the known course label and generated practice/microlearning stack fallbacks while preserving custom stack names and external identifiers. Shared-components, PWA, and Manage type checks, scoped ESLint, formatting, and `git diff --check` pass. Agent-browser reached the current dev app after a temporary loopback API proxy, but the seeded student login did not establish a session and the saved embed token was invalid after database reuse, so this narrow label change has no new runtime screenshot; the previously verified PWA/Manage screenshot set remains current for layout and interaction. Next: commit and run exact-commit reviews.
- 2026-07-23: Localized semantic-label review found that parsing an English-looking stack label could misclassify a custom name such as `Practice Stack 7` and could not distinguish generated practice from microlearning labels. The adjustment enriches discussion scopes with structured stack type, order, and optional custom display name in one batched lookup. Clients now preserve custom names exactly and localize only generated fallbacks without text heuristics. GraphQL generation/build, dependency-aware type checks, scoped ESLint, and all `23/23` isolated discussion scenarios pass; the integration fixture explicitly covers a custom regex-shaped name and both stack types. Next: commit and review the adjustment before extracting the duplicated responsive rail shell.
- 2026-07-23: Semantic-label adjustment review found that create-thread and thread-upvote responses still used the unenriched single-thread mapper, allowing Apollo normalization to replace list metadata with nulls. The single-thread lookup now uses the same batched enrichment path, and integration assertions cover create, list, and upvote responses for both stack types. The reviewer also flagged changed persisted hashes conditionally; these operations exist only on the unpublished Course Q&A branch, so there is no older production client to preserve. Next: verify and commit this correction, then rerun exact-commit review.
- 2026-07-23: Responsive discussion-rail slice active. Extract the duplicated practice and microlearning disclosure/sticky-rail shell into one PWA component while preserving page-specific placement, DOM order, labels, IDs, and Cypress selectors. The shared mobile toggle standardizes the 44px target and accessible expanded/controlled state.
- 2026-07-23: Responsive discussion-rail implementation done. Practice and microlearning now share one responsive shell while retaining their own content, placement classes, panel IDs, labels, and test selectors. PWA and Cypress type checks, scoped PWA ESLint, formatting, and `git diff --check` pass. Agent-browser reached the real isolated PWA through the temporary loopback API proxy, but seeded login again cleared the form without establishing a participant session; no new authenticated screenshot is claimed. The authenticated practice and microlearning screenshots `08` through `12` remain the visual evidence for the behavior preserved by this extraction. Next: commit and run exact-commit correctness and simplification reviews.
- 2026-07-23: Responsive discussion-rail review done for `c0c2a3f93b`. Correctness review found no regression in gating, DOM order, responsive visibility, sticky placement, state, IDs, selectors, or accessibility. Simplification review retained the narrow Q&A-specific abstraction and removed duplicate `min-w-0` classes from both callers because the shared shell owns that invariant. Next: verify and commit the adjustment.
- 2026-07-23: Testing-plan reconciliation done. `DISCUSSIONS_TESTING_PLAN.md` now describes the integrated page model, Node 24/devrouter environment, proof types, completed focused Chromium and `23/23` backend runs, all 16 screenshots, current Auth limitation, and the two honest remaining scenarios: anonymous rate-limit browser error state and legacy v1 live-feedback smoke. It no longer treats `/qa` as the primary workflow or reports the isolated database and Manage overview as blocked.
- 2026-07-23: Testing-plan review corrected two runtime overclaims: the completed embed Chromium run and screenshots predate fragment-token transport/course-wide embeds, and microlearning answering has compile-time rather than fresh runtime proof. Those scenarios are now `PARTIAL`, historical screenshots are labeled, and the plan includes exact focused Chromium commands plus targeted QA-010/QA-012 runbooks instead of repeated generic prose.
- 2026-07-23: Discussion-service modularization active. Preserve `services/discussions.ts` as the explicit public façade and separate types, model mapping, access/scope resolution, embed/rate-limit policy, queries, posting, and interactions with one-way dependencies. Keep transaction bodies and the public schema import path unchanged; verify all `23/23` DB scenarios before splitting the test registrar.
- 2026-07-23: Discussion-service modularization implementation done. The public façade is 29 lines and explicitly re-exports the unchanged 11 public types and nine operations. Seven internal modules separate types, model mapping, access/scope resolution, embed/rate-limit policy, queries, posting, and interactions; every implementation module is below 600 lines and internal modules never import the façade. GraphQL type checking and all `23/23` isolated DB scenarios pass with transaction bodies unchanged. Next: commit and run exact-commit correctness and simplification reviews before restructuring the test registrar.
- 2026-07-23: Discussion-service modularization review adjustment done. Exact-commit correctness review found no behavioral issue. Simplification review separated scope persistence and event creation from authorization into `scopes.ts` and made file-local constants/helpers private. GraphQL type checking, generation/build, formatting, and all `23/23` isolated DB scenarios pass; build output contains only the repository's existing Pothos/Rollup warnings. Next: commit the adjustment, then split the integration test into one lifecycle-owning aggregator plus non-discovered suite registrars.
- 2026-07-23: Discussion integration-test modularization implementation done. `discussions.test.ts` remains the sole discovered file and lifecycle owner; shared fixtures and five synchronous `.suite.ts` registrars preserve all 23 test names, registration order, one-fork cleanup, and the exact focused command. Every resulting file is below 500 lines. GraphQL type checking, formatting, `git diff --check`, and the isolated DB suite pass `23/23`. Next: commit and run exact-commit correctness and simplification reviews before the fresh final gates.
- 2026-07-24: Discussion integration-test modularization review done for `d4a38ee76`. Separate correctness and simplification reviewers found no issue; AST checks confirmed all 23 bodies, names, order, helpers, and lifecycle hooks are preserved, and the focused DB suite remains `23/23`. Fresh Chromium command validation exposed a stale runbook separator that caused all 30 specs to run; direct `pnpm exec cypress run` now selects Chromium 150 and one named spec. The current course workflow still returns to Auth before its first Q&A assertion, including after real delegated login and a clean shared-secret runtime restart, so fresh browser screenshots remain blocked by local cross-app session validation. Next: commit the runbook correction and proceed with security, whole-branch, and strict maintainability gates using the existing 16-screen evidence set plus fresh backend proof.
- 2026-07-24: Final maintainability gate found that reply deletion could commit its soft-delete when the guarded thread update lost a race. The new concurrent thread/reply deletion scenario then exposed opposite lock ordering and a PostgreSQL deadlock. Reply deletion now locks and decrements the thread before soft-deleting the reply, matching thread deletion order; either failed guarded update raises a rollback sentinel. GraphQL type checking and all `24/24` isolated discussion scenarios pass. Next: commit and review this atomicity adjustment, then address the remaining decision-free typing and Cypress-config findings.
- 2026-07-24: Atomic reply-deletion review confirmed the implementation but found the concurrency scenario alone did not guarantee overlap. A deterministic second regression reproduces the old reply-first partial-delete precondition and proves the current thread-first guard leaves reply content/deletion state unchanged and emits no reply-delete event. The simplification reviewer correctly noted that this scenario validates ordering rather than second-step rollback, so its name and plan evidence now state that narrower guarantee. The focused suite passes `25/25`.
- 2026-07-24: Discussion relation-typing implementation done. Thread, reply, scope, and vote relation shapes now derive from Prisma payload types; unauthenticated queries include an explicitly empty vote relation instead of changing the result shape. The five manual relation casts, including both `as unknown` bypasses, are removed. GraphQL type checking and all `25/25` isolated discussion scenarios pass. Next: commit and review this type-safety slice, then extract Course Q&A Cypress tasks from the root config.
- 2026-07-24: Cypress task extraction implementation done. The three Course Q&A database handlers now live in `cypress/tasks/courseQa.ts` and are spread into the unchanged task registry, removing 94 feature-specific lines from the 2,213-line root config. The redundant catch/rethrow was dropped; task names, arguments, Prisma writes, and return values are unchanged. Cypress type checking and formatting pass. Next: commit and review this extraction before resolving whole-branch findings.
- 2026-07-24: Discussion relation-typing review adjustment done. Correctness review found no issue. Simplification review found that runtime includes and payload declarations repeated the same relation shapes; one focused `relations.ts` module now owns participant-filtered includes and derives both Prisma payload types directly from those builders. GraphQL type checking, formatting, `git diff --check`, and all `25/25` isolated discussion scenarios pass. Next: commit the adjustment and run its exact review alongside the Cypress extraction review.
- 2026-07-24: Embed token transport fix implemented for whole-branch finding QA-R01. Manage now routes both external-block and course-wide token-producing persisted queries through the POST-only link, matching ADR 0001; the focused embed workflow aliases each operation and asserts `POST`. Manage and Cypress type checks and formatting pass. Browser execution remains blocked at the known local Auth redirect before this Manage flow, so the method assertions are committed but not freshly executed. Next: commit and review this transport slice.
- 2026-07-24: Embed token transport review found no implementation issue and one test-quality gap: a regressed persisted GET carries `operationName` in the URL rather than the request body. The intercept now resolves either location so the existing method assertion fails explicitly on GET instead of timing out. Cypress type checking and formatting pass.
- 2026-07-24: Embed token transport adjustment review found that the exact `/graphql` intercept glob still excluded GET URLs with a query string. The matcher now includes `/graphql?...`, ensuring a regressed token-producing GET is aliased and fails at the explicit POST assertion. Cypress type checking, formatting, and `git diff --check` pass.
- 2026-07-24: Manage pagination coherence fix implemented for whole-branch finding QA-R02. Polling stops after cursor pages are loaded, and manual Refresh discards those stale pages before refetching page one. A focused Cypress scenario resets and seeds 21 ordered threads, loads page two, inserts a new first-page thread, refreshes, and then proves all 22 threads plus the shifted boundary item are recoverable. Manage and Cypress type checks, scoped Manage ESLint, formatting, and `git diff --check` pass; browser execution remains blocked by the known local Auth redirect. Next: commit and review this pagination slice.
- 2026-07-24: Manage pagination review adjustment done. Pagination now stops polling before requesting the next cursor page and restarts it if that request cannot establish pagination; Refresh preserves loaded pages until page one refetches successfully. Manage type checking, formatting, the repository pre-commit gate, and `git diff --check` pass.
- 2026-07-24: Embed capability UX fix implemented for whole-branch finding QA-R03. The thread query now exposes identified-post and vote capabilities derived from actual course participation. Anonymous-only embeds force anonymous submission with clear copy, identified-only embeds render read-only for anonymous visitors, and unsupported vote controls become non-interactive counts. API regressions cover anonymous, identified-only, and authenticated embed viewers; Cypress covers forced-anonymous and read-only surfaces. GraphQL, PWA, and Cypress type checks, scoped PWA ESLint, generation/build, formatting, `git diff --check`, and all `25/25` isolated discussion scenarios pass. Browser execution remains blocked by the known local Auth/runtime path; next: commit and run exact-commit correctness and simplification reviews.
- 2026-07-24: Client-address normalization follow-up implemented after exact-commit review. The backend now creates its Express instance through a focused request-address factory, and real HTTP regressions cover a direct peer and a multi-address `X-Forwarded-For` chain through that configured app. The HAProxy option is now attached to the production and staging Ingress resources, and a rendered-manifest regression checks both placement and absence from the backend Service. This closes the gap where the discussion fingerprint suite only received an already-normalized `req.ip`.
- 2026-07-24: Client-address CI integration follow-up implemented. The backend request tests now use the repository-standard `test:run` entry point, and path-filtered backend and Helm workflows execute the HTTP regression, both environment lints, and the rendered-manifest placement check on pull requests and `v3` pushes.
- 2026-07-24: Account-deletion consistency fix implemented for whole-branch finding QA-R06. Participant deletion now decrements every thread and reply total represented by the participant's vote rows in the same transaction that removes the account and cascades those rows. The regression keeps one independent vote on each target and proves both totals and persisted vote counts remain at one after deletion. GraphQL type checking, formatting, `git diff --check`, and all `27/27` isolated discussion scenarios pass. Next: commit and run exact-commit correctness and simplification reviews.
- 2026-07-24: Account-deletion review adjustment done. Thread votes, reply votes, and account deletion now serialize with a participant `FOR NO KEY UPDATE` row lock before changing vote rows or counters, so concurrent add/remove operations cannot be missed or double-counted without blocking normal foreign-key key-share locks. Vote reconciliation moved into the discussion domain; account deletion uses group member counts and one batched empty-group delete. Separate deletion-first add/delete and vote-first remove/delete regressions observe PostgreSQL's active lock waiters before releasing the barrier, preserve one independent vote on each target, and passed `5/5` focused repetitions. GraphQL type checking, formatting, `git diff --check`, and all `29/29` isolated discussion scenarios pass.
- 2026-07-24: External-scope limit maintainability adjustment done. The shared 100/200-character source/reference contract now lives in `@klicker-uzh/types`, and both backend rejection and Manage input constraints import it while hardcoded boundary regressions remain independent. Types, GraphQL, and Manage type checks, scoped Manage ESLint, formatting, and `git diff --check` pass.
- 2026-07-24: Account-lock regression cleanup done. Both database-observed barrier tests now release and settle the holder transaction plus every queued operation in `finally`, including timeout failures, and allow five seconds for loaded CI to expose the expected waiters.
- 2026-07-24: Proxy-trust hardening implemented for whole-branch finding QA-R05. Both deployed value sets expose the ClusterIP backend through one HAProxy ingress; the Ingress explicitly enables `forwarded-for`, Express trusts exactly one proxy hop, and anonymous fingerprints consume only normalized `req.ip` instead of parsing the client-controlled first header value. A focused fingerprint regression, an installed-Express loopback probe with a spoofed-first two-address header, backend and GraphQL type checks, formatting, `git diff --check`, and all `30/30` isolated discussion scenarios pass. Next: commit and run exact correctness/security and simplification reviews.
- 2026-07-24: Scope-encoding adjustment review found that direct identified posting could create an empty discussion space before malformed UTF-16 was rejected. External identifiers are now validated before any parent lookup or creation, and the regression proves a stale valid token plus malformed posting input leaves zero discussion spaces.
- 2026-07-24: External scope collision fix implemented for whole-branch finding QA-R04. External source and reference identifiers are now trimmed and rejected above their persisted 100/200-character limits before any discussion space is created, instead of being silently truncated into a potentially shared scope key. Manage enforces matching input limits. The focused integration regression accepts exact-limit identifiers, rejects both colliding over-limit variants without side effects, and leaves one exact scope; all `26/26` discussion scenarios plus GraphQL and Manage type checks, scoped Manage ESLint, formatting, and `git diff --check` pass. Next: commit and run exact-commit correctness and simplification reviews.
- 2026-07-24: External scope review adjustment validates URI encodeability during the same preflight used before space creation. Malformed UTF-16 identifiers now return no embed and create no discussion parent records instead of throwing while constructing the scope key. The regression includes an unpaired surrogate alongside both over-limit collision variants. GraphQL type checking, formatting, `git diff --check`, and all `26/26` discussion scenarios pass.
- 2026-07-24: Malformed external scope handling implemented for whole-branch finding QA-R08. The shared scope parser now treats invalid percent escapes as an invalid scope instead of throwing during render. A focused Cypress regression visits the participant fallback route with a malformed encoded external scope and expects the normal access-denied state with no composer. Shared-components and Cypress type checks, formatting, and `git diff --check` pass; browser execution remains blocked by the known local Auth redirect. Next: commit and run exact-commit correctness and simplification reviews.
- 2026-07-24: Embed capability review adjustment accepted three behavior-preserving reductions and two correctness findings. The API now exposes one participant capability instead of duplicate posting/voting flags, the panel derives voting and anonymity states once, and embed participant resolution is direct. Token capability queries now require the exact persisted space and scope, including after a same-key space recreation, and read-only embeds retain an independent heading. The ordered Cypress journey remains because the existing Course Q&A workflows intentionally share seeded setup and generated URLs across focused user-role transitions. GraphQL generation/build, GraphQL/PWA/Cypress type checks, scoped PWA ESLint, formatting, `git diff --check`, and all `26/26` discussion scenarios pass.
- 2026-07-24: Final client-address regression follow-up done. The HAProxy option is asserted on both rendered Ingresses and absent from the backend Service; the configured Express app is covered by direct-peer and multi-address `X-Forwarded-For` HTTP tests. Path-filtered workflows run these checks in CI, Helm is pinned to the locally verified `v4.2.3`, and the repository pre-commit gate passes.
- 2026-07-24: Final backend verification passes all `30/30` DB-backed discussion scenarios. Fresh agent-browser capture remains blocked because the local `dev` helper reports `devrouter is not built yet`; manual fallback uses non-workspace API origins and reproduces the known Auth redirect or loading state. Existing screenshots `01` to `13` remain prior real-runtime layout and interaction evidence, while `14` to `16` are historical embed evidence.
- 2026-07-24: One product gate remains before rollout: choose minimum deletion controls for student-owned posts and lecturer moderation (recommended) or pull answered/pinned triage into the pilot scope. Decision-free implementation and final branch review can continue without broadening that product scope.
- 2026-07-24: Final security and maintainability review slice implemented. All rate-limit counters now increment and receive their fixed-window TTL in one Redis script, preventing a failed second command from leaving a permanent bucket. Reader and embed GraphQL responses no longer expose raw discussion author participant UUIDs; deletion UI can later use a server-derived capability after the moderation decision. GraphQL generation, GraphQL and PWA type checks, formatting, `git diff --check`, and all `30/30` DB-backed discussion scenarios pass.
- 2026-07-29: Exact review of the rate-limit/privacy slice found one valid migration edge: a TTL-less counter left by the old two-command implementation would not self-heal. The script now assigns expiry to new counters and existing counters with no TTL, and the integration scenario seeds that legacy state. GraphQL type checking, formatting, and `git diff --check` pass; the isolated app container used for the prior `30/30` run no longer exists, so DB execution is deferred until the final backend runtime is re-established. The persisted-query compatibility concern was rejected because Course Q&A has not shipped and therefore has no production clients using the removed author fields.
- 2026-07-29: Course overview mobile integration now uses the shared responsive discussion rail: Q&A remains visible beside course contents on desktop and starts as an accessible collapsed disclosure at `390x844`. Added a Cypress contract for collapsed and expanded states. Next route generation, PWA and Cypress TypeScript checks, focused PWA lint, formatting, and `git diff --check` pass via installed package binaries. The Volta pnpm wrapper could not verify its registry signature while offline, and the real Cypress/agent-browser journey remains deferred until the local app runtime is restored.
- 2026-07-29: Exact review of the mobile integration found that Cypress's 1000px default sits below the rail's `lg` desktop breakpoint, which would hide controls in later workflow scenarios. The suite now establishes a `1280x900` desktop baseline per test, while the dedicated mobile scenario overrides it to `390x844`. Simplification review found no unnecessary complexity or accessibility regression.
- 2026-07-29: Backend read authorization is centralized in `resolveCourseDiscussionReadContext`, which owns the course gate, actor resolution, supported-scope validation, evaluated-stack access, external-embed restriction, and exact embed binding. `courseDiscussionThreads` now owns only pagination and thread retrieval, with one shared inaccessible response. GraphQL TypeScript, formatting, and `git diff --check` pass; the existing DB-backed gate/embed/scope scenarios remain deferred until the discussion runtime is restored. Standalone focused ESLint is unavailable because the monorepo config is exposed through the root Turbo task, which remains enforced by the commit hook.
- 2026-07-29: Exact correctness/security review found no authorization or query-semantics drift in the backend extraction. Simplification review noted that `DiscussionSpace.courseId` is unique, so the read context now returns one nullable space from `findUnique` instead of modeling the relation as an array; embed binding and thread filtering use that space directly. The remaining risk is the deferred DB-backed discussion suite.
- 2026-07-29: Manage Q&A decomposition extracted the course/external embed-link workflow into `CourseDiscussionEmbedGenerator`; `CourseDiscussionOverview` now owns only polling, pagination, refresh, and lecturer thread rendering. Existing GraphQL operations, labels, controls, native disclosure, and Cypress selectors are unchanged. Manage route generation, TypeScript, focused lint, formatting, and `git diff --check` pass; browser verification is deferred to runtime restoration.
- 2026-07-29: Exact correctness and simplification reviews of the Manage extraction found no issues. The extracted state/JSX matches the previous inline workflow, preserves every selector and disclosure behavior, and resets stale generated-link state only when the child genuinely unmounts. Reviewers found the two-prop boundary cohesive and recommended no further split.
- 2026-07-29: PWA thread rendering extracted to `CourseDiscussionThreadCard`. Each card now owns reply drafts, reply submission state, anonymity choice, and thread/reply vote mutations; `CourseDiscussionPanel` retains the shared query, thread composer, access states, polling, and pagination. Existing content, controls, selectors, and always-visible reply behavior are unchanged, including the prior anonymity reset when anonymous posting is disabled. PWA route generation, TypeScript, focused lint, formatting, and `git diff --check` pass; browser verification remains deferred.
- 2026-07-29: Exact review of the PWA thread-card extraction found one valid state-lifecycle issue: Apollo mutation loading ended before the awaited thread refetch, allowing a second submission sooner than the previous implementation. Each card now holds an explicit submission flag through mutation and refetch using `try/finally`. Review found no other cache, anonymity, keyed-state, selector, or rendering regressions; simplification review recommended retaining the cohesive per-card mutations.
- 2026-07-29: PWA query and composer orchestration extracted to `useCourseDiscussion`; the hook owns scope normalization, the shared query, polling, localized presentation data, posting capabilities, thread creation, and cursor pagination. `CourseDiscussionPanel` is now a 238-line access-state and layout renderer, while selectors and rendered behavior remain unchanged. PWA route generation, TypeScript, focused lint, formatting, and `git diff --check` pass; browser verification remains deferred.
- 2026-07-29: Exact hook review found that the extracted scope helper used nullish fallback, changing `?scopeKey=` from the course default to an invalid empty scope. The original truthy fallback is restored. Simplification review also removed ineffective memoization around cheap strings, parsed scope input, and handlers whose dependencies already changed each render. No other polling, pagination, mutation, capability, localization, or rendering regressions were found.
- 2026-07-29: Reply composers now start collapsed behind a compact per-thread Reply disclosure, reducing repeated empty textareas in desktop rails and mobile feeds. The toggle retains focus and exposes `aria-expanded`/`aria-controls`; Cancel closes the composer and clears its draft/anonymity choice. Course and embed Cypress workflows cover collapsed, open, cancel/reopen, identified and anonymous reply submission, and read-only absence. PWA and Cypress TypeScript, route generation, focused lint, formatting, and `git diff --check` pass; browser execution remains deferred.
- 2026-06-01: Plan committed as `a1b2df084`. Pre-commit `check:all` passed with existing Node engine warnings.
- 2026-06-01: Slice 1 done. Extracted `CourseDiscussionPanel`; `/qa` route is wrapper. Checks: `pnpm --filter @klicker-uzh/frontend-pwa check` passed, `git diff --check` clean, browser screenshot `/private/tmp/course-qa-integrated-screenshots/01-slice1-qa-route-refactored.png`.
- 2026-06-01: Slice 2 done. Mounted stack-scoped `CourseDiscussionPanel` as sticky desktop rail in `ElementStack`; kept stack action controls in the content column and mobile discussion link fallback. Checks: `pnpm --filter @klicker-uzh/frontend-pwa check` passed, `git diff --check` clean, screenshots `/private/tmp/course-qa-integrated-screenshots/02-slice2-practice-stack-desktop-rail.png` and `/private/tmp/course-qa-integrated-screenshots/04-slice2-practice-stack-mobile-link.png`.
- 2026-06-01: Slice 3 done. Replaced course overview Q&A button with integrated course-scoped `CourseDiscussionPanel`; desktop uses right rail, mobile stacks below selected tab content. Checks: `pnpm --filter @klicker-uzh/frontend-pwa check` passed, screenshots `/private/tmp/course-qa-integrated-screenshots/05-slice3-course-overview-desktop-panel.png` and `/private/tmp/course-qa-integrated-screenshots/06-slice3-course-overview-mobile-panel.png`.
- 2026-06-01: Slice 4 done. Replaced microlearning evaluation stack discussion links with in-page stack selector and contextual `CourseDiscussionPanel`; desktop uses right rail, mobile stacks below evaluation. Checks: `pnpm --filter @klicker-uzh/frontend-pwa check` passed, screenshots `/private/tmp/course-qa-integrated-screenshots/07-slice4-microlearning-evaluation-desktop-panel.png`, `/private/tmp/course-qa-integrated-screenshots/08-slice4-microlearning-evaluation-selected-stack.png`, and `/private/tmp/course-qa-integrated-screenshots/09-slice4-microlearning-evaluation-mobile-panel.png`.
- 2026-06-01: Slice 5 done. Reordered Manage course Q&A tab so discussion triage is primary, moved the embed generator into a collapsible secondary section, and added an affordance/description for the collapsed state. Checks: `pnpm --filter @klicker-uzh/frontend-manage check` passed, screenshots `/private/tmp/course-qa-integrated-screenshots/10-slice5-manage-discussions-overview.png` and `/private/tmp/course-qa-integrated-screenshots/11-slice5-manage-embed-expanded.png`.
- 2026-06-01: Slice 6 done. Hardened integrated Q&A UI with locale-aware dates, long-text wrapping, form names/autocomplete, decorative icon hiding, reduced-motion-safe hover transforms, and clearer collapsible focus affordance. Checks: `pnpm --filter @klicker-uzh/frontend-pwa check` passed, `pnpm --filter @klicker-uzh/frontend-manage check` passed. Final screenshots saved under `/private/tmp/course-qa-integrated-screenshots/final-*.png`.

## Slices

### Slice 1: Reusable Discussion Panel

- Do: Extract Q&A UI/query/mutation logic from `apps/frontend-pwa/src/pages/course/[courseId]/qa.tsx` into reusable component.
- Files: new component under `apps/frontend-pwa/src/components/course/`, existing `/qa` page wrapper.
- Behavior: Existing `/qa` route unchanged visually enough; component accepts `courseId`, `scopeKey`, `embedded`, optional `compact`.
- Check: focused `pnpm --filter @klicker-uzh/frontend-pwa check` if feasible; browser screenshot of `/qa`.
- Commit: `refactor(course-qa): extract reusable discussion panel`.

### Slice 2: Practice Stack Desktop Rail

- Do: Mount discussion panel beside `ElementStack` on desktop for stack scope.
- Files: `apps/frontend-pwa/src/components/practiceQuiz/ElementStack.tsx`, extracted panel props if needed.
- Behavior: Desktop shows content + sticky Q&A rail; mobile uses compact button/link fallback first if sheet would be too broad.
- Check: browser screenshots desktop and mobile practice quiz stack.
- Commit: `feat(course-qa): integrate stack discussions beside practice content`.

### Slice 3: Course Overview Integration

- Do: Replace top separate Course Q&A button with integrated compact panel/summary in course overview.
- Files: `apps/frontend-pwa/src/pages/course/[courseId]/index.tsx`, panel component.
- Behavior: Latest course-level threads and composer visible in existing course page.
- Check: browser screenshot course overview desktop/mobile.
- Commit: `feat(course-qa): surface course discussions in overview`.

### Slice 4: Microlearning Evaluation Context

- Do: Replace repeated navigation links with scope-selecting discussion panel/sheet.
- Files: `apps/frontend-pwa/src/pages/course/[courseId]/microLearnings/[id]/evaluation.tsx`.
- Behavior: Stack rows can open/update discussion panel without leaving evaluation.
- Check: browser screenshot evaluation with selected stack discussion.
- Commit: `feat(course-qa): integrate microlearning stack discussions`.

### Slice 5: Manage Triage Polish

- Do: Make discussion overview primary; move embed generator into secondary/collapsible area; add clearer filters if low-risk.
- Files: `apps/frontend-manage/src/components/courses/CourseDiscussionOverview.tsx`.
- Behavior: Lecturer sees discussion triage first; embed link generator no longer dominates.
- Check: browser screenshot Manage Course Q&A tab.
- Commit: `feat(course-qa): improve manage discussion overview`.

### Slice 6: Final Hardening

- Do: Address low-risk UX/accessibility issues found during slices: long text wrapping, date formatting, reduced-motion, focus-visible if missing.
- Files: touched components only.
- Check: focused checks, browser screenshots, final review.
- Commit: `fix(course-qa): polish integrated discussion ux`.

## Finish

- Run final focused checks.
- Capture final screenshots for all changed surfaces.
- Run final security/best-practices review.
- Update PR/MR body with screenshots if requested.
