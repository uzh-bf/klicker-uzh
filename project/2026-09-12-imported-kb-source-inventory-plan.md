# Superseded: imported KB source inventory

This draft was replaced before merge by
`project/2026-09-12-doc-query-source-inventory-plan.md`, which implements the
read-only source inventory through doc-query without Klicker schema or migration changes.

The implementation branch contains no live registration, deployment, corpus mutation,
or retained video files from this superseded design.
