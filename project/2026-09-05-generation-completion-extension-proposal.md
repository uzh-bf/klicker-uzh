# Proposed extension: one Element-generation completion flow

Status: approved by the user on 2026-09-05 and integrated into the existing
execution plan at `trees/rs/generation-lifecycle-contracts/project/2026-09-04-pr-5777-generation-lifecycle-contracts-plan.md`,
committed as `ea717d68601151998e7d7f7405c63a0a70cafad7`.
This proposal is approval history; the execution plan owns current authority,
acceptance checks and Progress. The proposed/awaiting-approval language below
records the reviewed proposal, not the current execution state.

## Research
Problem: existing v3-ai generation has real completion transactions in questionGenerationDrafts.ts:155-250 and flashcardGenerationDrafts.ts:96-193, separate from their editing responsibilities. Both create initial GeneratedElementDraft rows and conditionally mark ElementGenerationBuild terminal. Direct tests for these two helpers are absent (rg across packages/graphql/test). The 74-file hosted GraphQL pass and current PR5777 tests prove lease claims and later Element keeps, not this earlier completion transaction.
Baseline: existing clean task branch rs/generation-lifecycle-contracts HEAD 5e984643c6a63f800e4402736fa5d3370a980ae8, origin/v3-ai 208e97d38e6abfd13d997d48200077febc8c1445; origin/v3 468f05b91503b133670dda235be9a4b38bba2155. No upstream integration.
Source mapper returned a four-service write inventory with test gaps, not a proven defect. Main checked the two completion implementations and absence of direct test calls. Its wider test-first recommendation is narrowed to this one consequential transaction seam; do not add method-by-method predicate tests or duplicate existing keep/accounting tests.
## Goal and topology
Extend existing draft PR5777, not a new PR/stack/worktree: one generation lifecycle package, folding the adjacent completion boundary into the draft before merge. This is a proposed material scope extension requiring user approval after planner hardening, not already-authorized implementation. Keep the existing lease/persistence work unchanged. The canonical execution plan remains project/2026-09-04-pr-5777-generation-lifecycle-contracts-plan.md in trees/rs/generation-lifecycle-contracts. After approval integrate these rows into that plan's existing Delegation map, Test portfolio and Slices and amend its authority/Progress; preserve prior approval as applying only to completed scope and record extension approval separately. Do not append duplicate execution structures. Proposal custody is the existing roadmap worktree project directory. No duplicate authoritative execution plan.
Goal: one explicit server-side owner for initial generated-draft completion with real rollback/fencing evidence and no changed product semantics. W4 lifecycle normalization remains partial after this; KB owners, failure/review/retry transitions, graph settlement/maintenance and later schema normalization remain their existing obligations.
## Primitive impact

User ruling, 2026-09-05: flashcards and questions are element types, not separate product primitives. The roadmap must converge on one Element-generation approach, with shared ownership, lifecycle, review and persistence rules. The immediate completion change is the first bounded application, not permission to redesign other owners or public contracts.

| Product primitive | Disposition | Contract and consumers |
| --- | --- | --- |
| Element | Reuse | One owned content item; SC, MC, KPRIM and FLASHCARD are type variants. Existing validation and ordinary persistence remain authoritative. |
| ElementGenerationBuild | Reuse | One generation attempt and lifecycle owner. Existing provider phase differences remain compatibility inputs, not new domain models. |
| GeneratedElementDraft | Reuse | One generated-content review record and saved-Element relationship. Type-specific original/current payloads retain their validation and provenance. |

Evidence: docs/domain-model.md content hierarchy; packages/prisma/src/prisma/schema/element.prisma ElementType; knowledge.prisma ElementGenerationBuild and GeneratedElementDraft; existing shared keepGeneratedElementDraft path. No schema or public behavior change is proposed.

## Frozen contract
- Expose one Element completion operation with typed inputs for currently supported generated element types. Existing question-provider input accepts SC/MC/KPRIM in FINALIZING, verifies question item types, keeps provenance/defaults and counts only duplicationIndex=0; commits COMPLETED. Existing flashcard-provider input accepts QUEUED/RUNNING/PUBLISHING_INCOMPLETE, counts all build drafts, preserves warning/unresolved/checkpoint fields and maps incomplete to INCOMPLETE, completed and completed_with_review to COMPLETED. Each of the three accepted flashcard source states accepts each of these three result statuses independently; preserve that existing compatibility matrix.
- Preserve initial read and final conditional-write predicates including build, type, status and lease token, error codes/messages, createMany skipDuplicates, and final row-count failure. Preserve current lack of direct owner predicate: entrypoint ownership and unguessable lease token remain the current composition; do not turn a hypothesis into an authorization change.
- Preserve re-entry: createMany skipDuplicates leaves matching existing drafts unchanged, and matching cardinality may satisfy completion. Do not overwrite original/current/provenance/defaults of existing rows or strengthen this into a content-equality/idempotency policy change.
- Draft writes and build completion remain in one existing Prisma transaction. Failure at draft cardinality, item type or final conditional transition must roll back every draft and artifact/counter/status write from that call. No success on a stale token or incompatible state.
- Completion creates review drafts, NOT ordinary Elements, activity snapshots, new dispatch/spend or release operations. Accounting/dispatch and the short lease stay separate.
## Implementation boundary
Use packages/graphql/src/services/elementGenerationCompletion.ts for one shared Element completion operation, called directly by both current provider workflows. One implementation owns the transaction, build/type/status/lease checks, draft insertion, cardinality assertion, final conditional update and rollback. Merely moving two transaction bodies into one file or hiding them behind a dispatcher does not satisfy the user ruling.

Use a closed, typed input for the currently supported provider result variants. Type-specific conversion validates and maps choices or front/back content into the existing GeneratedElementDraft shape. Keep conversion within the same transaction and after the current build check where required to preserve validation and error precedence. Retain original/current/provenance and the existing default/null behavior exactly.

The shared operation derives expected statuses, counting scope, terminal outcome and optional artifact/counter fields internally from validated result kind and the current provider contract. Callers cannot supply arbitrary Prisma predicates/update data, transaction callbacks or ad-hoc behavior flags. Distinguish content shape from provider phase: present source-state/count differences are compatibility constraints, not separate permanent Element lifecycles. Do not alter persisted enums, public operations or retry policy to make completion look uniform.

Keep one shared database path with small local conversion/policy branches only where the contracts differ; no plugin registry, generic workflow engine or new package. Remove the two old completion implementations and switch both callers to the same operation; no compatibility re-exports. Retain existing editable normalization and one-way imports. Adding a future supported Element type should extend its typed result conversion/validation, not copy the transaction or introduce a new completion service. This does not enable generation for currently unsupported Element types.

The wider roadmap carries this same convergence requirement for preparation, review, retry and saving: share common lifecycle and business services, isolate justified content/provider differences, and require evidence before changing incompatible historical behavior. This slice proves completion only, not the entire generation lifecycle.
Owned production paths: new elementGenerationCompletion.ts, questionGenerationDrafts.ts, flashcardGenerationDrafts.ts, and completion-callsite and import hunks in questionGeneration.ts/flashcardGeneration.ts. Tests: one new elementGenerationCompletion.integration.test.ts, existing questionGenerationLifecycle.test.ts only if a consequential wiring assertion is absent. docs/async-and-workers.md records the owner and same transaction boundary. No changes to editable draft operations, general Elements, auth/permissions, schema/Analytics/migrations, SDL/public operations, dependencies, workers, knowledge.ts, knowledgeGraphAccounting.ts, provider configuration, UI, or other worktrees.
Parallel ownership: Klicker KB KG actively owns processed-document schemas, graph serving state, webhook/polling/ingestion, graph accounting and associated UI. Recheck exact path overlap before editing; stop for actual overlap, not unrelated upstream movement.
## Test portfolio
1. Add a direct real PostgreSQL completion suite at the existing completion seams for baseline, then the single Element completion operation. Use one detailed stored-shape success per provider-result variant for original/current/provenance and counters/artifacts. Parameterize question SC/MC/KPRIM acceptance and flashcard QUEUED/RUNNING/PUBLISHING_INCOMPLETE source-state acceptance with distinct COMPLETED/INCOMPLETE terminal outcomes. Do not multiply parser payload matrices.
2. Same suite: stale token and incompatible from-state reject for both supported provider-result variants through the same operation with no new drafts or build mutation. Cover question item-type mismatch and each variant's different draft-cardinality rule. Cross a question-result variant with a FLASHCARD build and the flashcard-result variant with a question build; assert the existing exact error code/message and no draft/build mutation. Add one matching pre-existing-row re-entry case per payload family, proving existing original/current/provenance/defaults are unchanged. Use compact service-level parameterization for all nine accepted flashcard source-state/result-status combinations. Existing lease acquisition tests remain unchanged; do not duplicate them.
3. Same suite, one takeover case for EACH existing provider-result variant: lose the lease AFTER the initial read but BEFORE the terminal update. Use the existing Prisma query-extension barrier pattern from elementGenerationPersistence.integration.test.ts:82-105, narrowed to the target build's real initial findFirst; execute query(args), retain its actual result, signal arrival and await explicit release. A second real database client takes the expired lease through acquireElementGenerationLease with the existing clock seam. Require distinct backend identities and one successful successor claim; then release the suspended completion call. Assert CONCURRENT_MODIFICATION, zero leaked inserted drafts, unchanged winner token/state/artifacts and no spend/quota mutation. Bound the barrier and release it in finally. No mocked Prisma transaction or database result and no production test hook. The test-only query extension controls scheduling, not returned data. Use bounded waits and finally cleanup of both clients and barrier. Broad table locks and polling database lock views are unnecessary. If a deterministic real barrier cannot be implemented safely, stop with the exact gap rather than weaken the assertion.
4. Reuse existing accounting/dispatch, lease, generated keep and lifecycle tests plus existing Element/permission/activity sentinel suites from the current plan. No new save-entrypoint, preparation-failure or generic status matrix in this extension. A baseline defect becomes a preserved reproducer and user decision; never silently patch behavior or assert incorrect behavior as desired.
## Delegation and slices
Owner: main; boundary owner: self. Main retains design, integration, external actions and acceptance because these require cross-slice judgment. No peer execution-orchestrator handoff is proposed.

| Slice | Owner | Dependency | Acceptance |
| --- | --- | --- | --- |
| A — characterize current completion behavior | executor | Frozen contract and clean head | PostgreSQL suite passes against existing helpers, including type/status/token fences, stored shape, cardinality/re-entry and rollback/takeover. |
| B — install one Element completion transaction | executor | Characterization accepted and reviewed | Both callers use one operation; old bodies removed; suite passes; schema, SDL, provider and accounting surfaces unchanged. |
| C — reconcile and qualify the existing draft | main | Shared completion checks/reviews settled | Existing plan/docs reconciled, branch checks/final review complete, runtime stopped; push/draft update only after extension approval. |
A. executor: one new real database test file against existing helper imports, following frozen portfolio. Main controls runtime and checks. Run focused baseline tests. Commit test(graphql), dedicated simplifier and one data-integrity slice reviewer on immutable range, disposition before next slice.
B. executor, after test slice accepted: replace both completion implementations with one common Element transaction within owned paths, preserving semantics and import direction; migrate both callers and the new test suite to the common operation. Main verifies diff and tests. Commit refactor(graphql), simplifier plus one architecture/data-integrity slice reviewer; correct only material findings.
C. main: engineering-doc and plan reconciliation, affected package checks plus root checks/build, integrated final review for the whole branch (new source invalidates prior final gate for this extension only). Verify exact runtime stopped, compute full branch size, update same draft PR only under approval below. No new PR or stack.
## Verification and runtime
Reuse the exact task worktree and its stopped task-created Devsy runtime, not a second worktree or shared services. Read rs-local-runtime-lifecycle and repo runtime instructions before starting. Confirm all data remains disposable/synthetic and no concurrent consumer; otherwise stop before destructive hooks. Use existing dedicated database and supported KB_GRAPH_BLOB_HOST_PORT=10043 override if still free. No Infisical or model calls.
Run commands in container via devrouter exec exact path. New suite directly using pnpm --filter @klicker-uzh/graphql test test/elementGenerationCompletion.integration.test.ts (no extra --). Rerun named lifecycle/lease/dispatch/accounting/keep files from current plan, scoped formatter/linter, GraphQL generate/check and schema mirror. Root check:all/build with inherited limitations separately reported; known safe serial isolated-cache alternative remains available.
No UI or provider contract changes; existing Y-question-generation-review checks completed-build editing rather than initial completion and cannot substitute for new DB proof. Reuse that unchanged journey's evidence unless imports/wiring materially change its tested behavior; exact-head hosted Playwright remains required for publication qualification. No browser runtime merely for documentation. Public SDL and Prisma/migration diffs must remain empty.
## Authority, terminal and pause
Now: read-only research and local proposal/roadmap documentation only. One user approval would authorize the described extension on existing branch: test-only runtime resume/use and synthetic cleanup, edits, native verification/reviews, local commits, push to origin/rs/generation-lifecycle-contracts and update existing draft PR5777. No new branch, upstream integration, merge, ready-state change, provider spend, secrets/shared workflow repair, real data, staging/production/migration/deployment or deletion.
Publication is an intermediate boundary after local verification/review. Terminal after approval: all extension slices published to the same draft with matching remote readback, roadmap evidence recorded and runtime stopped, then exact-head hosted checks, ordinary feedback and final AI review settled, or a named material blocker requiring new authority. Preserve draft status; no ready-state mutation. Continue CI/feedback around parked blockers via rs-babysit-pr within its four-hour watch budget, using one watcher/heartbeat; pending checks alone are not a stop. A material blocker ends implementation authority, not automatic handling of independent CI/feedback. Never claim pr_ready with failed or pending gates.
Pause only for actual owner collision, unsafe fixture/runtime, baseline defect or altered required behavior, necessary schema/dependency/auth/provider change, failed independent gate after configured correction budget, or external action outside authority.
## Decisions/docs
The user confirms one Element domain, reused by existing generation records. This implementation changes internal ownership, not public lifecycle policy. No new ADR: reversible consolidation follows the existing canonical model and does not change ADR0013 accounting or ADR0017 canonical graph/build record. If contracts change, stop and re-arm domain/ADR decision rather than add policy.
Update only docs/async-and-workers.md owner reference and current plan Progress after implementation. The roadmap's schema normalization, business regression and RC gates stay binding. First AI release is 3.4.0 RC, not 3.5.0.
## Planning review

The prior approved proposal permitted separate completion bodies and is superseded for that architecture and its test/callsite implications by the 2026-09-05 user ruling. Unchanged verification, topology and authority evidence remains reusable. Fresh native planner round 1 returned REVISE; main verified and accepted crossed Element-type fences, existing-row re-entry protection, the full accepted flashcard state/result matrix and explicit delegation ownership. Round 2 returned APPROVED with no blocking findings. Evidence: ../../rs/generation-lifecycle-contracts/project/_local/reviews/2026-09-05-element-completion-architecture-plan-hardening.md. No implementation or extension publication is authorized by planning review alone. The Claude advisor and previous optional challenge failed OAuth before work; no result is claimed.

## Current Progress
Proposed only. No source/test edit or runtime restart performed. Current draft PR head checks: GraphQL74files and codebase passed; Playwright failed three UI assertions on shard 8; the parent roadmap records the same three failures on the exact base; OpenCodeReview HTTP403 parked pending external auth repair; final AI review not started. This does not weaken existing gates or authorize their bypass.
