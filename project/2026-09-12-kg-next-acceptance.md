# KG compatibility and downstream acceptance

## Approval summary

Continue the approved KG quality roadmap under one executable batch. Establish real synthetic publication where the local integration is available, compare legacy Finance with explicit Finance on current source, and evaluate multi-document graphs and downstream evidence use. The existing USD20 program ceiling includes historical USD2.17359406 and every new request, reservation, retry and validator. Content experiments additionally stop at USD2 incremental cost.

Retain the six-policy contract, current drafts and synthetic-data boundary. Legacy compatibility means supported behavior, not identical stochastic graphs. The content comparison changes graph descriptions supplied as evidence; both arms retain graph-based selection. It cannot establish graph-free superiority. Small repeated diagnostic runs support defect discovery, not broad quality claims.

User approval authorizes bounded local tests, in-scope fixes, ordinary task-branch commits/pushes and draft updates. Merge, deployment, worker image publication, real course data and broader infrastructure qualification remain outside this batch. Each acceptance seam ends with producing evidence or an exact blocker; independent work continues. Completion requires reconciled costs, reviewed corrections, updated drafts and stopped task runtimes. An unavailable publication or chat seam remains incomplete acceptance.

## Execution details

### Context and ownership

Klicker worktree: `trees/rs/kg-domain-selection-plan`, branch `rs/kg-domain-selection-ui`, initial head `96acc9719d48d5f19dc0e96b73381b6ab574d4a2`, draft PR5906 above PR5904. Generator task branch `rs/kg-quality-checkpoint-a` starts at `d88256d78c096d263fde9471c8d246eb378425a8`, draft MR17. Its post-benchmark correction is included. Both remotes were fetched before execution; no integration is implied.

| Slice | Owner and route | Acceptance |
| --- | --- | --- |
| Publication | Main; coupled runtime and data boundaries | Correlated real UI, API, worker, published artifact and reload, or exact failing seam |
| Graph matrix preparation | Trusted native worker | New isolated harness, omission semantics, frozen source, twelve unique runs, offline protocol checks |
| Graph execution and audit | Main; shared provider budget | Four-cell pilot then eight remaining runs, raw/cleaned and actual chunk/source evidence |
| Content and retrieval | Main; experimental validity decisions | Matched effective evidence modes and observable fixed-budget retrieval, or exact blockers |
| Corrections and delivery | Main plus required review roles | Focused verified fixes, draft evidence and reconciled roadmap |

### Experiment protocol

Use a fresh ignored `project/_local/kg-quality-next-acceptance/` root. Preserve prior evaluation trees. Freeze actual tracked generator hashes, mounted identity, dependency versions, fixture inventory, schedule and parameters before calls. Use the existing restricted gateway and model allowlist, carrying the cumulative ledger forward. Unknown billing or outstanding reservations stop further spending.

Six runs compare the frozen Finance source with omitted policy versus explicit Finance v1, three repeats each. Six runs compare a new multi-document Finance collection under Finance v1 and General Academic v1. At least one document must exceed the effective chunk threshold; verify persisted chunks and source mappings. The first repetition of each cell is the pilot, counted within twelve. A separate UI publication build is additional and separately accounted. Schema-v2 graph diagnostics do not substitute for schema-v1 application dispatch proof.

Keep legacy `domain_policy` absent from construction and serialization; inspect `model_fields_set`. Score supported relations, false merges, duplication, attribution, failed builds and raw-to-cleaned retention. Missing co-extraction cannot establish preservation. Keep failures in denominators without selective replacement.

For content, preselect a usable graph and three synthetic tasks covering concepts, relations and cross-document reasoning. Compare raw chunks with description context twice per task, at most twelve outputs, starting with one matched pair. Freeze selection and blueprint. Inspect actual generating requests: if both paths suppress descriptions, mark the comparison ineffective. Manually assess claim support, correctness, distractor rationale, citations, completeness and refusal against raw sources.

For retrieval, first verify the installed implementation exposes source-linked evidence and a shared token budget. Use six synthetic queries covering lookup, terminology, comparison, cross-document reasoning, conflict and unanswerability. Compare naive and mixed retrieval only if those controls hold. Participant-chat E2E needs an existing scoped corpus/runtime binding; a transport canary does not qualify answer quality.

### Verification and delivery

Extend local budget checks for cumulative/phase reservation limits, restart and uncertain billing. Add protocol checks for omission, source drift and duplicate attempts. Extend production tests only for reproduced uncovered defects at the existing generation, merge, provenance, publication or consumer seams. Do not test fixture wording or generated prose.

Record per-run logs, raw/cleaned graphs, chunks, recipes, request ranges and semantic scores. Public artifacts contain sanitized findings only. Apply required review gates to substantive corrections and integrated evidence; reuse unchanged passing source checks. Preserve draft status and stack topology. Stop exact task runtimes and verify provider state and routes before closeout.

## Progress

- Approval mode: executable batch, latest user request to proceed with a goal.
- Planner: native independent construction pass completed; omission semantics, phase ceiling, effective ablation and extra publication-build accounting incorporated.
- Main retains runtime/budget execution; settled harness preparation is delegated despite the planner's conservative all-main suggestion.
- Local Manage startup initially hit another worktree's Blob port10003. Existing `KB_GRAPH_BLOB_HOST_PORT=18440` override clears the collision without stopping its owner. Managed startup completed; the runtime was subsequently stopped.
- Four pilot builds and eight remaining repetitions passed on frozen generator source. Publication remains blocked by missing isolated bindings.
- Verified provenance correction: record nullable legacy course profile in generator recipe parameters and omit its null default from canonical bytes. Preserve historical and strict recipe validation; old missing metadata does not establish historical strict execution. The fix was prepared outside the evaluated tree and applied after all graph and retrieval calls completed. Relevant source: generator generation_recipe.py and tasks.py; existing recipe and workflow tests. Graph-bound artifact hashes continue to separate outputs.
- Staged plan delivery is blocked: ordinary hooks repeatedly lose Git worktree context while shared core.bare changes. Secret scan and formatting pass. No hook bypass or shared config mutation occurred.


## Completed experiment evidence

The twelve scheduled builds all succeeded on frozen generator `d88256d`. Three repeats each compared omitted-policy Finance, explicit Finance, multi-document Finance and multi-document General Academic. Every multi-document build persisted two documents and three chunks, including two chunks from the larger document. Cleaned graphs had 30–34, 32–34, 71–96 and 87–134 nodes respectively. All strict builds passed their type allowlists. These are small diagnostic samples, not evidence that larger graphs or specialist policies improve downstream outcomes.

All seven executed merge proposals in the multi-document runs were singular/plural variants (Derivat/Derivate and Absicherungsinstrument/Absicherungsinstrumente). The initial four-run semantic spot review retained the systematic-risk qualification and risk-tolerance/capacity distinction. Complete educator-calibrated semantic scoring remains outstanding; absence of a known bad merge in these proposals does not establish general merge precision.

The six-query retrieval comparison completed in naive and mixed modes using copied graph storage and a common 5,000-token ceiling. Naive returned all three source chunks for every query. Mixed returned three chunks for lookup, two for comparison/cross-document reasoning and one for terminology/conflict/unanswerability. In the conflict case, the selected raw chunks omitted the other document. Graph descriptions may retain some information, so this is an evidence-selection limitation rather than proof of an incorrect answer. No answers or participant-chat turns were generated or scored.

The content comparison stopped before paid calls: existing new-question generation paths both omit graph descriptions, making the proposed evidence-mode comparison ineffective. Three offline tests confirmed that behavior. Publication stopped after seeded UI login and KB access because isolated catalog, generator and FalkorDB bindings were unavailable; no publication build was submitted.

Cumulative ledger spend is USD2.59520654, including conservative historical charges, with zero holds under the USD20 cap. This batch added USD0.42161248: USD0.41761074 for graphs and USD0.00400174 for retrieval. The ledger, not the graph-only summary, is the final total.

A provenance defect was corrected after all paid runs: future legacy recipes record their course profile while strict/default and historical canonical digests remain compatible. Old recipes without the marker remain ambiguous; this does not implement replay reconstruction. The fix changes metadata, not the evaluated graph algorithm. At generator9876293, 1,118 tests passed with one skip excluding pre-existing DOCX tooling failures. After the behavior-preserving simplification committed asfd75e6b, all13 recipe tests and Ruff passed. Independent slice review confirmed compatibility. The source is pushed to the existing draft MR; exact-head CI663570 passed all gating jobs with the existing allowed legacy audit failure. Integrated final review confirmed source correctness; its stale-progress documentation finding has been corrected locally.

## Remaining acceptance and order

1. Supply the existing isolated catalog/generator/FalkorDB integration and prove a real schema-v1 UI build through publication and reload. Infrastructure changes remain outside this batch.
2. Define and implement an effective content evidence treatment, inspect paired generating requests, then run the bounded matched comparison. Do not infer effectiveness from the option name.
3. Preserve raw-source coverage when graph context consumes the retrieval budget; evaluate claims, citations, conflicts and abstention through a bound participant-chat corpus.
4. Expand to reviewed multi-document scenarios and blinded educator scoring before policy/model tuning or adoption claims.

The runtime used for publication was stopped with zero task routes and the browser closed. The evaluation container is also stopped. Both draft descriptions are updated and read back. The generator source is pushed, but these Klicker documents remain uncommitted because the normal hook fails. This document does not claim full acceptance or deployment readiness.


## Commit-hook correction

A disposable linked-worktree reproduction established the hook root cause: the local-KB test inherited GIT_DIR and ran git init against its parent metadata, changing shared core.bare from false to true. The fixture now removes inherited Git variables, matching the existing identity-test convention. All70 local-KB tests pass with a linked-worktree GIT_DIR supplied, and core.bare remains false. The shared setting was restored to its verified original value. This is a test-fixture isolation fix; application behavior is unchanged.
