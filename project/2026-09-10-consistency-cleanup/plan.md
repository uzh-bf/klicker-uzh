# B0 consistency cleanup

## Approval summary

Executable batch; full path. The user authorizes local implementation, tests, normal merge ancestry in task worktrees, and ordinary draft publication. Production maintenance is v3-ai; next-release integration is v3-audit; accepted stable application and trusted CI stay v3. Source convergence must preserve deployed application and migrator artifacts and all existing SQL. Later roadmap packages are outside this batch.

Protected/shared ref advances, PR retargeting, repository promotion controls, authoritative infrastructure application changes, and deployment activation await one exact CUTOVER-APPROVAL packet. No new release, migration application, feature activation, paid model call, entitlement change, destructive retained-data operation, default-branch change, or shared-branch deletion is authorized.

The terminal authorized result is reviewed local source integration and safe draft publication, an evidence-backed cutover packet, separate source/PRD/STG/core-health/containment/isolation verdicts, and an updated continuation ledger. Operational success requires actual approved effects and readback; prepared work is not deployed work.

## Execution details

Pinned inputs after origin fetch: stable 492e5f1e4302167557336488d4a2ee009cbb67de; maintenance 5cb1e445df5b41496b0051850c94031dbdc956df; audit 352f47fd7443d93aa5720e863d6285b801cceebe; staging release 039e7e17535e28ef4eb487737d8743a2566e3116. Incident tag is an unverified runtime lead until workload inventory proves identities.

Main session owns Git integration, private operational evidence, conflict resolution, runtime lifecycle, release authority and final proof. Planner owns the independent frozen-plan challenge. A bounded worker may own CI selector/release guard changes after contracts are settled; private infrastructure remains trusted-only. Required independent reviews cover integration/control risks. No specialist may mutate shared refs, access secrets, deploy, or dispatch paid requests.

1. C0: refresh source/tag ancestry, existing PR routing, actual Application ownership, effective Helm inputs, live image and worker state, migration metadata availability, controls and isolation. Sanitize all records; never commit operational dumps.
2. C2: normal local merge of pinned stable into pinned maintenance; inspect each conflict, reconcile dependencies using pinned tooling, hash every migration for both parents/result, prove ancestry. No authored application SQL.
3. C3/C5/C6: minimally align canonical branch guidance and operational guards. Require explicit valid STG selection; preserve trusted v3 checkouts and qualified stg-release ancestry/digest rules. Bind PRD promotion to an exact approved maintenance receipt, not a tag pattern. Render current and proposed PRD inputs with unchanged artifacts; classify every effective delta and split any incident repair from mechanical handover. Prepare authoritative df-cloud change and preview via existing CI only.
4. C4: separate audit task branch/PR forward-merges the prepared maintenance candidate; mark acceptance conditional on protected maintenance merge, preserve audit-only history and staging ancestry. Do not change shared branches.
5. Verification/publication: inspect push/PR automation first; frozen install, relevant checks/types/schema mirrors, production build and real isolated Playwright on merged tree. Run focused control negative tests and Helm comparisons. Required reviews before completion; drafts disclose pending evidence. Avoid duplicate environments; stop exact task runtime and verify shutdown. No paid final-review dispatch.
6. C7/C8: prepare exact ordered cutover effects, holds, identities, diffs, recovery, invalidation conditions and missing operator evidence. Record separate verdicts, routing ledger and next roadmap owners. Block only dependent work when approval/access is missing.

## Acceptance and risk

Ancestry includes both pinned parents and audit includes staging ancestor. Migration paths and hashes are preserved; source checksum proof remains distinct from live ledgers. PRD effective resource inventory, pod templates, hook, configuration precedence and capability state require zero unreviewed delta. Current and proposed source can differ substantially despite equal image pins. Production safe smoke requires its separately approved synthetic effects; ordinary Playwright is isolated only. No application runtime is started just for declarative checks, but the dependency merge needs real integration verification.

## Progress

Local preparation complete; final independent risk review pending. Normal maintenance and audit merges published as drafts #5881 and #5883; narrow trusted-CI draft #5882. Private infrastructure draft !557 remains unmerged. Exact controlled source/infra identities and operational blockers are in completion.md and CUTOVER-APPROVAL.md. Shared refs and live controls remain untouched by this task. Synthetic verification runtime stopped with source-matched provider status and zero routes; data retained.

### Delegation map and operational gates

| Slice                           | Owner                                                                                             | Dependencies / bounded paths                                      | Acceptance / stop                                                                                      |
| ------------------------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| C0, C1, C5 private live/IaC     | main                                                                                              | df-cloud klicker source, exact app-klicker and namespace metadata | Actual identities and three-way render; no live changes                                                |
| C2, C4 source integration       | main                                                                                              | pinned parents, manifests, lockfile, generated mirrors            | Parent ancestry and byte-identical SQL; stop at shared refs                                            |
| C3, C6 control implementation   | main                                                                                              | deployment workflows/scripts and canonical docs                   | Selector fails closed, exact release receipt; paid worker route excluded by user prohibition           |
| C2/C3 risk review               | native planner for pre-implementation plan; trusted independent reviewer for final complete range | This plan / later immutable diff                                  | Full applicable contract, no omitted risk lens; required reviews may not use paid external model calls |
| C7, C8 evidence and publication | main                                                                                              | plan, completion and routing ledgers                              | Safe drafts, exact cutover packet, independent verdicts; no protected merge                            |

Main retains tightly coupled source/deployment seams and private runtime work. Paid external executor/slice-review routes are prohibited by this task; no peer task is authorized. Native plan review is active; implementation review route must preserve the same boundary.

C1 holds must precede approved shared-ref or watched-source changes and account for manual dispatch, in-flight workflows/syncs, parent reconcilers and Reloader. No active migration may be cancelled. The safe end state keeps PRD automatic activation held until an exact approved candidate/configuration receipt exists; STG re-enablement is a separately named choice. Recovery requires proven rendered-workload and database compatibility. Any unintended SQL stops cutover and returns to incident handling.

C5 requires live resources versus current approved desired manifests versus proposed manifests. Any runnable hook requires proof that the pinned migrator has no unapproved pending SQL on every applicable database. Missing ledgers block handover qualification, not source preparation.

C6 sets the intended selector to v3-audit, retaining Argo stg-release and full-SHA/digest validation. Exact candidate workflow/image inventory and digest receipt are required; unreviewed audit SQL blocks staging activation. A distinct maintenance target must isolate DB, Redis, Hatchet namespace/queues, storage, and outbound credentials from audit and PRD. If absent, prepare a specification; provisioning and isolation proof remain explicit gates.

Planner round 1: accepted all four findings and made the obligations above explicit.

### Named independent gates

- Planner: native planner, plan only before implementation; acceptance APPROVED after bounded correction; unavailable route blocks gate.
- Simplifier: native simplifier, immutable committed implementation range after checks; acceptance behavior-preserving simplification disposition; unavailable route blocks that gate.
- Risk and final review: native final-reviewer explicitly selected for this task to preserve the no-paid-external-call boundary. It reviews the complete committed integration/control range and verification with security/data/architecture lenses; missing route or incomplete scope blocks qualification. No external slice-reviewer or CLI paid provider is used.

Maintenance includes the later published 1d85533 build correction through normal ancestry. Maintenance candidate 5b63ea881 and audit merge 0fadf15c12 have identical trees. Both contain pinned stable 492e5f1e and maintenance 1d85533; audit also contains current stg-release 1d85533. SQL preservation and two live ledgers verified; independent Analytics ledger remains unavailable. Simplifier found no justified reduction; final risk review pending.
