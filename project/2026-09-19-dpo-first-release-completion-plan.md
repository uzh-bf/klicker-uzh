# DPO first-release completion and follow-up execution plan

## Current outcome — verified 21 September 2026

The core notice and persistence layer is **merged, deployed to PRD, and accompanied by the published website**. Full DPO completion is still open. Registered/guest chat account-level completion, KB attestations, purpose-aware exports and the LA product remain later packages. Authenticated assessment acceptance, direct response admission and live optional-processing evidence need explicit closure.

The [DPO integration roadmap](./2026-09-06-dpo-integration-roadmap.md) is the current cross-package status and order. This file owns the remaining acceptance work and executable package contracts. These sections supersede the earlier preparation-only and September 19–20 checkpoints; their detailed history remains in Git. The September 8 DPO documents and later explicit user decisions remain the wording authority.

**Working branch:** `rs/dpo-prd-prep`, existing draft [PR #6227](https://github.com/uzh-bf/klicker-uzh/pull/6227), target `v3`. Execution mode: solo, explicitly requested by the user during this reconciliation. Main owns each package and its verification when execution is resumed. The completed read-only source and planning reviews are inputs; main owns corrections and final self-review. No further delegation is planned. The current task authorizes verification and project-plan updates, not implementation of all remaining packages, a new rollout, retained-data deletion, or target merges. Existing narrow delivery permissions persist. Required terminal for this update: evidence-backed current state, complete remaining package contracts, reviewed documentation and updated draft.

## Verified release and acceptance evidence

| Boundary | Evidence on 21 September | What it establishes / limits |
| --- | --- | --- |
| Core source | [#5970](https://github.com/uzh-bf/klicker-uzh/pull/5970) merged as `d3ab5f1c04`; shared foundation present in `origin/v3` `abbf785082` and `origin/v3-ai` `171c3e96cb` | Normal/assessment creation, PWA renewal, profile settings, canonical writer, audit history, published copy and source LA containment exist. Does not establish every runtime journey. |
| Release | Tag `v3.4.0-alpha.81` resolves to `fd81ae52047953c474ee6b25476a44e2b5136ada`; [#6243](https://github.com/uzh-bf/klicker-uzh/pull/6243) merged production pins as `099c726a8f` | Release source and deployment configuration are distinct revisions. Subsequent source commits are not automatically in alpha.81 images. |
| PRD | Argo `app-klicker`: Synced, Healthy, Succeeded at config revision `abbf785082`; all 17 deployments ready and pinned to alpha.81 | Fresh read-only cluster check. No deployment has an unready desired replica. Health is not authenticated consent E2E. |
| Migration and assessment DB | Alpha.81 migrator Job succeeded; it uses the normal backend Secret. In-memory comparison of both backend database targets returned equal, including host, port, database and schema; no values emitted | Closes the separate assessment database-target uncertainty. Does not independently prove every applied migration row or account journey. |
| STG | Argo Synced, Healthy, Succeeded at `7ce67a8aa921b0d6e5d06c563c0ccd47a27ce226`, matching `origin/stg-release` | `origin/v3-audit` is now `4309b63b89`; that later source is not yet the observed STG deployment. |
| Public website | `/datenschutz/`, `/privacy_policy/`, `/lernanalyse/`, `/learning_analytics/` all HTTP 200 on `www.klicker.uzh.ch`; policy dates are 14 September 2026 | Publication gap closed. Previous same-task normalized page comparison and asset-hash comparison matched the FTP build; this pass refreshes availability and policy date. |
| Public application surfaces | PRD PWA `/de/createAccount` and assessment `/de/account/data-use` return 200 | Confirms reachability; assessment's unauthenticated entry is not proof of authenticated expanded notice or persistence. |
| Prior interactive proof | STG `7cc37fce83`: normal signup disclosure interactions, existing-user completion, profile and fresh-load refusal persistence; later checked source-equivalent notice surfaces | Reused historical interaction evidence, not a new PRD persistence test. Retain exact revision attribution. |
| Approved documents | Accepted-change XML paragraph comparison: 59/60 substantive German policy paragraphs normalized-identical; remaining difference is the recorded typo `verwendete` → `verwendet`. Normal and assessment notice paragraphs are present in German i18n | Formatting normalization is review evidence, not a wording-pinning test. Export paragraphs are intentionally deferred; the DOCX's synthetic Edu-ID identity is illustrative. English equivalence relies on the earlier review. |
| Optional-processing containment | Source: `isLearningAnalyticsEnabled()` returns false, four GraphQL optional analytics reads short-circuit, Python package import raises `LEARNING_ANALYTICS_DISABLED`. PRD namespace has no CronJobs and only the completed migration Job | Partial evidence only. External notebooks, schedulers, in-flight processes, retained derivative rows and pending withdrawals have not been inventoried live. |

No new application test suite or authenticated browser session ran in this documentation pass. It does not establish legal approval beyond the supplied DPO-reviewed documents. The existing mandatory completion route satisfies the requested PWA “popup” outcome; changing it into a modal adds no required behavior.

## Immediate acceptance closure — R0

**Owner:** main/release owner. **Dependencies:** already deployed foundation. **Target:** verification first; any demonstrated correction gets a focused task branch against its owning target. **Priority:** before optional processing activation and before claiming full DPO acceptance.

1. **Assessment and PRD account acceptance.** Use a designated synthetic actor, with authority for the exact environment. Verify first assessment entry, returning incomplete account, stale-policy renewal, both purposes refused, profile update, new-session read-back, return routing and assessment self-deletion denial. Normal and assessment accounts are intentionally separate: no linking or choice synchronization is introduced. Local OIDC mock #5976 proves local callback/form behavior only; an authorized real provider session is needed for hosted Edu-ID proof. Record any unavailable credential as a precise evidence gap.
2. **Direct-response boundary — confirmed source gap; priority correction.** Alpha.81 `apps/response-api/src/index.ts` queues normal and assessment responses without checking persisted account-data-use completion; the response worker also has no such check. Normal ingress classifies cookie presence and defers validation to the worker; do not claim it authenticates at ingress today. No production bypass request was sent. Port only the required admission guard from the preserved draft, after existing identity validation and before queueing; use the canonical predicate, fail closed for incomplete registered/assessment identities, and preserve legitimate anonymous/temporary participation. Cover cookie/bearer transports actually supported by each current endpoint, expired tokens, missing participant rows and cookie-less/LTI continuation. Add `apps/response-api/src/participantAdmission.ts` and `apps/response-api/test/participantAdmission.test.ts` only for this shared normal/assessment seam. Integrate in the existing handler, preserving CORS and protocol behavior. Synthetic requests must prove denied submissions enqueue no event and create no response, while completed accounts and permitted temporary flows still work. This is a focused `v3` corrective package, not an auth refactor; carry it through the integration chain for the next release.
3. **Live optional processing.** Inventory the named PRD/STG analytics writers, scheduled jobs, notebook/operator entry points, read/export routes and relevant optional chat telemetry without reading participant content. Record image/source provenance and values-free enabled/stopped status. A namespace without CronJobs does not prove external jobs are stopped. Classify operational answers/scoring/audit logs separately from LA derivatives. Any confirmed disallowed processing is an immediate remediation finding.
4. **Retained derivatives and withdrawals.** Through the authorized read-only operator boundary, collect aggregate counts only for the derivative allowlist below and pending withdrawal records. First-false legacy accounts need explicit coverage: the shipped service queues only true-to-false transitions. Zero retained data plus effective whole-path containment can close reconciliation; nonzero data requires a concrete dry-run and separately approved, idempotent derivative-only cleanup. Never imply that a queued withdrawal is completed deletion.
5. **Capture and record evidence.** For normal creation, Edu-ID continuation, assessment completion, existing-account renewal and profile settings, capture default and all-expanded states in DE/EN desktop and mobile, plus the actual interaction/persistence result. Reuse unchanged earlier captures with provenance. Record hosted-provider limitations separately from local mock evidence. Update this table with exact image/source revisions and remaining failures.

Derivative inventory: `ParticipantAnalytics`/`CompetencyAnalytics`, `AggregatedAnalytics`/`AggregatedCompetencyAnalytics`, `ParticipantCourseAnalytics`, `AggregatedCourseAnalytics`, `ParticipantPerformance`, `InstancePerformance`, `ActivityPerformance`, `ParticipantActivityPerformance`, `ActivityProgress`. Verify current model names before any operator query. Preserve operational `QuestionResponse`, `QuestionResponseDetail`, scores, assessment records and `ChatMessage`. This list authorizes classification, not deletion.

**Investigation complete:** every row has verified evidence or a precise missing capability. **Acceptance passed:** all required runtime rows actually pass, the direct-response correction is verified on the served revision, and any confirmed collection/retention mismatch is resolved. A named blocker never counts as acceptance. Independent chat source work can continue while a hosted actor or operator check is unavailable. No new export or analytics activation is included.

## Migration and integration invariants

Four DPO migration directories are included in the release under `packages/prisma/src/prisma/schema/migrations/`: `20260912141124_participant_policy_onboarding`, `20260913150006_participant_research_consent_index`, `20260913150007_participant_learning_analytics_consent_index`, and `20260914090000_participant_analytics_withdrawal`. The succeeded migrator is runtime evidence; per-migration database application was not separately queried in this pass. Preserve their identities and SQL. The audit trigger blocks UPDATE, independent DELETE and TRUNCATE; it **allows** deletion when the participant is already absent, preserving account-deletion cascade. Do not disable audit protection as a routine rollback or cleanup step.

Application rollback retains the additive schema and saved decisions. A rollback to older behavior can weaken notice/consent enforcement even though rows survive, so it requires explicit containment and post-rollback acceptance checks. Do not describe alpha.80 as a consent-equivalent fallback. Restoring a pre-migration database is outside ordinary rollback.

Before reusing #5819/#5825 or the old consent/analytics stack, produce a per-operation schema mapping against the deployed four migrations. Generate only the missing schema delta with Prisma; retain unavoidable custom SQL with its reason. Prove upgrade from the deployed schema and a clean install in a disposable database, without duplicate columns, tables, indexes or rewritten applied migrations. Regenerate GraphQL output and synchronize analytics schema through repository tooling.

Use the chain `v3 → v3-ai → v3-audit`. Preserve merge commits. Current missing `v3` commits on `v3-ai` are `2520f69416`, `099c726a8f`, and `abbf785082`. [#6220](https://github.com/uzh-bf/klicker-uzh/pull/6220) is conflicting; determine the actual conflict before selecting the vehicle. A clean ordinary sync uses a receiving-branch merge/push; substantive conflict resolution uses a task branch and integration PR with exact-head checks. [#6239](https://github.com/uzh-bf/klicker-uzh/pull/6239) owns the `v3-ai → v3-audit` PR hop and must be refreshed after the first hop. Every new staging promotion revalidates the precise candidate. These are planned actions, not synchronization or deployment performed by this update.

## Binding behavior

### Account and consent primitives

The existing `Participant` is the subject of the choices. Reuse its research/LA values, choice metadata, current disclosure acknowledgement, revision and append-only `ParticipantDataUseEvent`. Database Boolean defaults alone are not evidence of a recorded decision. An explicit `false` with complete metadata is a completed choice.

Research initially displays allowed for a new or unanswered account, with a visible state and an accessible opt-out. LA starts unanswered and requires an explicit yes/no. Submission requires an unchecked mandatory acknowledgement. Never silently persist the research default on page load, expand/collapse, authentication, or guest creation. Save acknowledgement and both choices atomically through the shared writer.

On policy renewal, retain recorded choices and choice timestamps unless the participant changes a value. Never silently turn a refusal into permission. Profile changes to a purpose do not implicitly renew policy acknowledgement. A stale revision or uncertain write must reload canonical state and require renewed acknowledgement where appropriate rather than overwrite another tab's changes. A retry after a successful write must remain safe.

Select one coordinated disclosure revision for the release. The currently implemented identifier is `2026-09-08`; it identifies disclosure semantics, not the website publication date. During copy reconciliation, decide whether the newly introduced guest presentation adds a material disclosure change requiring a bump. PWA, chat and server must agree on the accepted revision and completion predicate. Avoid different contracts under one identifier and avoid an unnecessary second renewal during the same rollout.

### Surface contracts

| Surface                                      | Required presentation                                                                                                                                   | Required behavior                                                                                                               |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| Normal signup, including Edu-ID continuation | Existing concise approved normal disclosure; data categories retained; independent choices                                                              | Required acknowledgement; both refusals still create a usable account.                                                          |
| Assessment account creation/entry            | Same collapsible structure, with assessment identity, responses/results, audit logs, authorized lecturer access and account/data retention restrictions | Correct assessment variant from trusted context; refusal does not block assessment; self-deletion restrictions remain enforced. |
| Existing PWA account                         | Mandatory completion/renewal route; saved choices retained                                                                                              | Return safely to the intended route after success; no redirect loop through privacy/help/logout.                                |
| Registered chat account                      | Mandatory account-policy completion in the chat entry flow                                                                                              | Preserve resolved identity and requested bot/thread; completion must not trigger guest fallback.                                |
| First-time chat guest                        | Short guest-account notice, privacy link, independent choices, unchecked acknowledgement, start/cancel actions                                          | Persist choices on the resolved guest participant before chat use; cancel causes no chat message/model call.                    |
| Returning chat guest                         | Renew only when version or choice metadata is incomplete                                                                                                | Reuse existing persona and choices; do not recreate a guest to evade the gate.                                                  |
| Settings                                     | Saved research and LA values, relevant consequences and links                                                                                           | PWA profile and guest-accessible chat settings write the same canonical record with revision protection.                        |

PWA's current completion page satisfies the blocking renewal requirement; do not replace it with a modal only to match the conversational word “popup.” Chat may use an in-app dialog/screen, with accessible focus handling and no dismiss-to-chat bypass. A forced completion surface must retain a meaningful cancel/exit path.

### Guest copy and identity

Use a short German introduction, translated equivalently into English:

> **Chat als Gast nutzen**
>
> Für diesen Chat wird ein Gastkonto angelegt. Du musst keinen Namen und keine E-Mail-Adresse angeben. Deine Chatnachrichten und die für den Betrieb erforderlichen Nutzungsdaten werden gespeichert und deinem Gastkonto zugeordnet.
>
> Du kannst unabhängig entscheiden, ob deine Daten für Forschung und Learning Analytics verwendet werden dürfen.

Follow this with the shared purpose choices, a linked privacy-policy acknowledgement and **Chat starten** / **Abbrechen**. Reuse approved research and LA wording; this introduction supplements the supplied account text where the guest workflow differs. Do not describe guests as anonymous or promise that messages cannot contain personal information. Do not add a new terms-acceptance requirement accidentally while adapting the guest form.

The current LTI guest key is derived from the launch subject and course. The same subject/course reuses a persona; another course can have another participant record. Choices therefore apply to that guest persona. Do not promise cross-course synchronization or link guests to registered accounts in this package.

A minimal guest identity/participation record may already exist when LTI launch completes. Explain this honestly; do not rewrite authentication to defer identity creation. Before acknowledgement, allow only operations needed to resolve identity, present/save choices, view policy/help, or exit. Block new messages, attachments, content processing, model calls and optional-purpose processing. Token expiry is not a retention policy and does not prove deletion of a guest account or chat history.

### Authentication and chatbot disclaimer composition

Keep authentication, account completeness, bot/course authorization, and bot-specific disclaimer as separate checks. In particular, do not fold consent completeness into `isActiveAccountParticipant` or another predicate that resolves missing/invalid identity and can fall back to a guest. Preserve the current identity resolution. On `v3-ai` the runtime transport order is guest cookie → scoped PWA embed cookie → scoped fallback token → account cookie, with the existing target-bot scope-miss handling. LTI launch account-versus-guest selection is a separate contract; retain it unchanged. Never change either order to add consent.

Registered and guest chat requests use a structured completion-required response after identity validation. The client preserves the intended destination and opens the appropriate completion surface. Unknown/expired identity follows existing authentication failure handling; it must not become a consent bypass or silently switch participant.

The existing `ChatUsageCredits.acceptedDisclaimerId` and `acceptedDisclaimerAt` remain the per-chatbot disclosure record. Completing account policy does not accept the bot disclaimer, and accepting the bot disclaimer does not complete account policy. Compose the sequence as identity/access resolution → account completion → bot-specific disclaimer → chat use. A changed bot disclaimer independently prompts again.

Ordinary anonymous live-quiz participation and its temporary participants are not the persisted chat-guest use case. Preserve their existing behavior. `Participation.isActive` remains a leaderboard flag, never a substitute for consent or access authorization.

### Processing and deletion truthfulness

This section records the unresolved post-release acceptance obligation and the prerequisite for future optional activation. The reviewed policy describes LA as separately enabled and automatically deleted when deactivated. Current source blocks Python analytics imports and the four optional GraphQL analytics reads, but `ParticipantAnalyticsWithdrawal` has no verified consumer and live containment is unproven. A first recorded false for a legacy account may create no withdrawal if the old Boolean was already false.

The deployed release must continue to keep deferred LA processing inactive and prevent access to disallowed derivatives. Inventory collectors, scheduled/pending jobs, recomputation, read/export paths and any optional chat telemetry. The first-release default is whole-path LA containment, not selective activation for opted-in participants. Stop or effectively block scheduled, queued, running and directly invoked derivation, and contain derivative reads/exports. Use an existing suitable switch if one exists; otherwise implement the smallest explicit containment. Findings requiring selective activation or a broader analytics implementation require a plan amendment before that work. `Course.areAnalyticsValid` is cache validity, not course LA activation. Do not repurpose it.

Preserve operational responses, scoring, assessment audit logs and chat history needed to provide the requested service. Do not equate them automatically with optional LA derivatives. Do not activate new research processing or telemetry by saving preferences. Any already active research-use path must respect recorded eligibility or remain contained until its later implementation.

As immediate post-release verification, determine whether retained derived LA data or pending withdrawals exist using authorized, values-free evidence. If they exist, prepare an idempotent, scoped reconciliation/cleanup with a dry-run and clear source/derived boundaries. Legacy first refusals must be covered. Actual retained-data deletion requires a separate named approval with target, backup/recovery limits and affected data classes. Disabling reads alone is not proof of deletion; a queued request is not completed deletion. Closing this acceptance gap requires proof that writers are blocked, in-flight work cannot recreate derivatives, required scoped reconciliation/deletion is completed, and reads remain contained. The choices and policy are already exposed. Missing evidence is an open post-release acceptance gap, not a fictional pre-release stop. Escalate a confirmed mismatch immediately, prepare the smallest correction, and keep optional activation blocked until it is resolved.

## Shared implementation seam

The canonical writer, server-only entry and shared choice presentation below are already merged on `v3` and `v3-ai`. Reuse them. The chat transport, gate and conditional reconciliation tool remain planned; their path names are proposed additions, not shipped files.

| Seam                            | Concrete implementation boundary                                                                                                                                                                                                       | Contract                                                                                                                                                                                                                                           |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Canonical writer                | Reuse `packages/graphql/src/services/participantAccountDataUse.ts` and its already narrowed Prisma/verified-identity context                                 | GraphQL passes its existing context; chat constructs that small context only after validating its participant identity. Keep role validation, transaction, revisions, timestamps and errors in one implementation.                                 |
| Server-only package entry       | Reuse `packages/graphql/src/participant-data-use.ts` and the already configured normal/instrumented Rollup entries; verify emitted `dist/participant-data-use.js` and declarations when the package is rebuilt | Chat imports this server entry only; do not pull the GraphQL schema or Prisma into browser code and do not add a new package/dependency. Verify emitted path and transitive imports.                                                               |
| Shared choice presentation      | Reuse the choice view at `packages/shared-components/src/participant/ParticipantDataUseChoices.tsx`; keep PWA `ParticipantDataDisclosure.tsx` as its normal/assessment composition                                             | The existing view remains browser-only and uses existing design-system controls and shared translations. PWA and chat are the two concrete callers. Avoid a generic consent-form framework.                                                           |
| Chat transport                  | New `apps/chat/src/app/api/chatbots/[chatbotId]/data-use/route.ts`                                                                                                                                                                     | GET reads canonical state; POST completes acknowledgement and choices; PATCH changes one recorded purpose. Existing verified participant/bot access and origin protections apply even before completion. No request accepts a participant ID.      |
| Chat composition                | New `apps/chat/src/components/ParticipantDataUseGate.tsx`, integrated into existing `assistant.tsx`                                                                                                                                    | One normal/guest presentation and settings mode; use the shared choice view and existing request helper. Keep bot-specific disclosure separate. Extend existing `apiGuards.ts`, without altering identity resolution, to apply account completion. |
| Conditional reconciliation tool | Only if inventory proves no existing tool can satisfy retained-data cleanup: `packages/prisma-data/src/scripts/reconcile-participant-analytics-withdrawals.ts`                                                                         | Dry-run by default; explicit scoped execution, idempotency, derivative-only allowlist, no output containing participant content. Exact table inventory and live execution authority must be resolved before implementing destructive mode.         |

The transport returns the canonical purpose values and metadata, acknowledgement version/time, revision, current required version and completion status. POST accepts the existing completion input (`expectedRevision`, `disclosureVersion`, two Booleans, `acknowledged: true`). PATCH accepts one purpose, its Boolean value, disclosure version and expected revision using the existing choice-update semantics; it never renews acknowledgement.

Map existing domain error codes without changing their meaning: invalid input → HTTP 400, stale revision → 409, completion required → 403, lock timeout → 503 with recoverable UI, forbidden → 403; expired/missing authentication retains the current 401 behavior. Do not convert database/transport errors into success or default consent values. GET and successful writes return current canonical state; on conflict or uncertain response the client rereads it. Version mismatch uses the existing invalid-input response and refreshes required-version metadata before resubmission.

The new completion route bypasses only the account-completion check, never identity or bot/course authorization. Existing content routes remain protected: `chat`, `practice/submit`, `knowledge-graph`, thread/message/attachment/feedback/title operations and bootstrap reads must be classified explicitly. Do not gate unrelated lecturer preview/manage routes through participant logic. Public policy/help are accessible without loading protected history.


## Verification portfolio and release discipline

| Consequential boundary | Existing protection / reuse | Test obligation and remaining evidence |
| --- | --- | --- |
| Atomic persistence, false choices, stale revision, retry, policy renewal, immutable audit/cascade | `participantAccountDataUse.integration.test.ts`, `participantDataUse.integration.test.ts`, `participantDataUse.test.ts`; earlier synthetic DB/browser evidence | **None (new tests):** run the unchanged existing suite; no duplicate canonical service suite for chat. |
| Assessment identity, deletion and response admission | Existing accounts/LTI/deletion and ingress tests; local OIDC mock | **Add new:** response-admission contract tests at the shared normal/assessment seam; **none** for unchanged hosted account behavior (browser proof). |
| Chat account gate, persona identity and bot disclaimer | Existing chat guard/LTI/disclaimer suites | **Extend existing + add new transport suite:** R1 route classification plus direct bypass, settings race and browser/embed integration. |
| KB pre-transfer evidence and audience/resource binding | Existing KB lifecycle tests; preserved #5825 tests | **Replace/consolidate draft tests:** R2 adapt to current seams; real synthetic ingestion/retrieval and renewal races. |
| Purpose-bound export, permission/withdrawal races and audit | Preserved #5819 export contracts/tests | **Replace/consolidate draft tests:** R3 reconcile source and extend actual release tests, with no consent filter on legitimate assessment use. |
| LA withdrawal, empty scope and protected group output | Preserved analytics/withdrawal/provenance tests | **Replace/consolidate draft tests:** R4 consumer/writer integration, legacy false, re-enable and small/empty cohort cases. |
| Private points and all-member averages | Existing scoring/group tests and retained draft cases | **Replace/consolidate:** port retained-score cases into current scoring/group seams. |
| Wording, layout and publication | Accepted-change document comparison, public website checks, prior screenshots | **None (automated content tests):** no prose-pinning tests. Human-readable comparison plus real default/all-expanded browser captures for changed surfaces. |

Before each package's release, verify the complete delta against its immediate target, preserve the four applied migrations, run focused container checks and required exact-head CI, simplify/review at the workflow's applicable gates, and maintain the draft description with screenshot provenance. Promote through merge commits and independently validated STG candidate; acquire named PRD activation authority. Rollback preserves data and contains processing rather than restoring an old schema. No new provider or broad security audit is implied.


### Concrete verification and stop conditions

Main owns R0 and every R1–R5 slice in solo mode. Each named slice is a coherent commit/check boundary within its package, not an automatic new PR. Schema changes are generated only after operation-by-operation reconciliation; the generated migration directory name is determined by Prisma at execution, not fabricated here. Reuse existing schema files and create no extra model layer merely to match the old draft.

Run source checks in the provisioned disposable container, with the current package's supported test environment; run browser E2E from the host. Representative repository-native commands, narrowed to the owning package:

- Shared/R0: `pnpm --filter @klicker-uzh/graphql test test/participantAccountDataUse.integration.test.ts test/participantDataUse.integration.test.ts test/assessmentAccountDeletion.test.ts`; `pnpm --filter @klicker-uzh/response-api build` after adding the guard. Wire its new admission suite into the existing Vitest runner; verify that the runner actually selects and executes it.
- R1: `pnpm --filter @klicker-uzh/chat test:run test/participant-data-use-route.test.ts test/chatbot-layout-access.test.tsx test/chatbot-bootstrap-route.test.ts test/lti-guest.test.ts test/pwa-embed.test.ts`, then the shared package build and `pnpm --filter @klicker-uzh/chat check`.
- R2/R3/R5: `pnpm --filter @klicker-uzh/graphql test` with the named KB, export or leaderboard test paths as arguments; `pnpm --filter @klicker-uzh/graphql check` regenerates and verifies SDL. Check/build `@klicker-uzh/kb-management` or `@klicker-uzh/frontend-manage` when changed.
- R4: port `packages/graphql/test/participantAnalyticsWithdrawal.integration.test.ts`, `apps/analytics/tests/test_analytics_eligibility.py` and `test_research_contribution.py`; run them using each package's existing runner. Keep optional runtime processing disabled while unit/integration tests use isolated synthetic fixtures. Do not add a Python test framework for this plan.
- Every changed browser contract: `pnpm playwright:host -- <focused-spec>` against the owned managed runtime; default/all-expanded screenshot gallery with source, locale, viewport and interaction provenance. Stop that exact runtime after verification unless the user explicitly retains it.

Unavailable actors/providers block their runtime acceptance only. Failed source tests block publication claims for that package. Unresolved schema overlap blocks dependent ports. Missing release/cleanup authority blocks only those live effects. An existing draft does not permit bypassing any of these conditions.

## Remaining delivery packages

The following packages preserve the agreed outcomes. They are planning commitments, not claims that draft code is ready or authorization for target merges and production effects. Start with R0 evidence and R1 chat notices; KB remains the next feature after the outstanding notices. Later exports, LA and points can progress independently where dependencies permit. Create or reuse focused task branches only when execution begins; do not bulk-merge preserved broad drafts.

### R1 — Registered and persisted-guest chat notices

**Owner/target:** main, `v3-ai`. **Dependency:** shipped shared participant contract, available on the current AI branch. A general branch sync is release hygiene, not a reason to duplicate that contract or block independent source work.

**Slice R1a — Canonical transport and request admission.** Reuse `packages/graphql/src/participant-data-use.ts` and the shared completion predicate. Add the narrowly authorized status/completion/settings route described above. Extend `apps/chat/src/lib/server/apiGuards.ts` after identity and bot/course authorization. Do not modify token precedence or account liveness to encode consent. Classify every participant route: chat/model calls, practice submission, knowledge graph, bot/bootstrap reads, credits, threads/history, messages, title, feedback and attachments. Only identity, minimum gate metadata, policy/help, completion/settings and exit operations may bypass completion. Preserve lecturer preview/manage access separately. Recheck persisted state for direct requests, not a JWT snapshot.

**Slice R1b — Guest/registered UI and settings.** Add `ParticipantDataUseGate.tsx` through `assistant.tsx` and the ordinary chat entry/bootstrap flow; verify the actual mounting point on the current branch. Use shared design-system choices, the short guest introduction above, locale-correct links, unchecked acknowledgement, research collapsed, independent collapsibles, and explicit LA choice. Resolve a guest identity as today; explain that it persists without collecting name/email. Cancel sends no message or model request. Renewal preserves stored refusals. Chat settings remain reachable for guests and update the same persona through `apps/chat/src/components/settings-panel.tsx`. Keep a safe bot/thread destination and do not automatically replay queued messages after acceptance. Compose the account gate with the existing bot disclaimer without accepting either on behalf of the other.

**Slice R1c — Proof and release.** Extend `apps/chat/test/chatbot-layout-access.test.tsx`, `chatbot-bootstrap-route.test.ts`, `lti-guest.test.ts` and `pwa-embed.test.ts`; add `apps/chat/test/participant-data-use-route.test.ts` for the new canonical transport; reuse shared PostgreSQL persistence coverage. Test account-only, bot-only, both-missing and both-current states; missing choices, stale revision/version, duplicate retry, database failure, returning guest, distinct course personas, scoped embed, invalid identity and cross-bot denial. Browser proof covers direct, LMS/embed and reload journeys with both purposes refused. Capture DE/EN mobile/desktop default and expanded forms and settings using the screenshot-gallery workflow. Update `docs/auth-model.md` and the student LA pages only where guest availability changes. Keep revised wording semantically aligned; bump the common disclosure revision only for an actual contract change.

**Terminal:** focused draft, exact-head checks, reviewed screenshot table, server bypass proof and persisted choices for registered and guest personas. After named merge/promotion authority, repeat synthetic STG acceptance, then prepare the PRD vehicle. No new consent schema, identity linking or auth refactor.

### R2 — KB rights and personal-data confirmations

**Owner/target:** main, `v3-ai`. **Dependency:** R1 notices delivered as the first outstanding user-facing milestone; current KB lifecycle inventory before porting any old draft. Preserve #5825 as reference, especially its binding and audience-renewal work.

**Slice R2a — Bind confirmation before transfer.** Reconcile `packages/graphql/src/services/knowledge.ts`, `packages/kb-management/src/components/KnowledgeBase{AddResource,ReplaceFile}Modal.tsx`, `KnowledgeBaseChatbotBindings.tsx` and the existing KB mutation operations. Proposed ports from #5825 are `packages/graphql/src/lib/kbMaterialConfirmation.ts`, `packages/graphql/src/services/kbMaterialConfirmations.ts` and `packages/kb-management/src/components/KnowledgeBaseMaterialConfirmation.tsx`. Map current upload-ticket issuance, initial/additional upload, replacement, import, finalization and chatbot attachment/audience changes in `apps/frontend-manage`, `packages/graphql`, and current KB services. Add separate unchecked rights and personal-data confirmations to the existing dialogs. Ordinary author names and bibliographic credits are permitted under the settled copy; do not impose a blanket ban. Bind server evidence to the authenticated actor, resource/version, purpose/audience and disclosure revision. Fail closed before issuing a transfer capability or enqueueing ingestion when required evidence is absent or stale. Reuse current lifecycle/idempotency mechanisms.

**Slice R2b — Existing resources and renewal.** Explicitly classify `apps/backend-docker/src/kbHttpRoutes.ts` and external-import flows: the preserved draft is not complete coverage for these routes. Distinguish read-only source listing from transfer capabilities and trusted ingestion callbacks, applying attestations at the actual transfer/admission boundary. Require fresh confirmation when previously uploaded material is newly used without matching evidence or its applicable audience/use changes; cover the previously identified narrower-audience case too. Compare the current binding, not only an “accepted once” Boolean. Decide migration/backfill from concrete existing schema; do not invent historical acceptance. Preserve graph build/retrieval ownership and the merged ingestion fixes; confirmation must not revive deleted/replaced material or release a stale transfer.

**Slice R2c — Real ingestion proof.** Use a synthetic document through the ordinary upload/import/replacement paths and confirm ingestion plus retrieval for the authorized bot. Direct ticket/finalization/API bypass, stale evidence, concurrent replace/audience change, cancellation, retry and rejected transfer leave no falsely confirmed state. Extend `packages/graphql/test/knowledge.test.ts` and `knowledgeIngestion.test.ts`; port and adapt `kbMaterialConfirmation.test.ts` and `kbMaterialConfirmations.test.ts` from #5825. Capture default/expanded forms and keyboard interactions; document the durable confirmation contract in the existing KB area documentation when the source seam changes. Verify deployed STG provider path separately from local deterministic tests.

**Terminal:** focused draft containing UI plus server enforcement, durable binding, real synthetic ingestion and review evidence. No new graph architecture or replay of production ingestion. Existing resources are not bulk reprocessed by this package.

### R3 — Assessment and research exports

**Owner/targets:** main on `v3`; chat/provenance adapters on `v3-ai` after shared contracts arrive through normal synchronization. **Dependencies:** shared participant state; R4 for the LA-derived research class only. Other selected export classes must not wait for LA activation.

**Slice R3a — Assessment download boundary.** Port `packages/graphql/src/services/assessmentExports.ts`, `src/lib/assessmentExportArtifact.ts`, `src/lib/assessmentExportRequest.ts` and `apps/frontend-manage/src/components/courses/modals/AssessmentExportModal.tsx` from #5819 only after migration reconciliation. Reconcile existing `AssessmentStudentResultsTable.tsx`, course-results GraphQL and direct CSV/download paths with #5819. Put current ADMIN authorization, required attestation, selected scope and durable release audit at the actual server release boundary. Assessment outputs may contain the approved identifying assessment fields. Refusal of optional research/LA does not suppress legitimate operational assessment results. Remove client/direct-endpoint bypasses without creating a second results calculation. Verify identities, numeric results, field/scope integrity, unauthorized course access, audit failure and retry.

**Slice R3b — Research release.** Reuse `packages/graphql/src/services/dataExports.ts`, `src/lib/researchExportArtifact.ts`, `src/lib/researchExportRequest.ts`, `apps/frontend-manage/src/components/courses/modals/ResearchExportModal.tsx` and the `/api/data-exports/{research,assessment}` handlers mounted in `apps/backend-docker/src/app.ts`. Follow [ADR-0024](../docs/adr/0024-research-export-release-boundary.md); export attestation revision is a separate protocol from participant disclosure `2026-09-08`. Reuse the preserved bounded JSON v1/schema and receipt design only after current-source review. The user already settled direct ADMIN downloads after attestation: **no human review/approval queue**. Project/purpose and selected supported classes are required. Use export-local identifiers, remove direct account identifiers, preserve scope/size limits, and recheck research eligibility at release against concurrent withdrawal. Serialize or otherwise prove the release/withdrawal race, retain request/released/failed receipts, and fail closed on audit failure or disconnect. Exporting operational response data for research needs research eligibility; an LA-derived class additionally needs its LA provenance/eligibility contract.

**Slice R3c — AI adapters and content decision.** Build chat transcript adapters on the current authoritative history/resource model, not a stale wire message shape. Free text and transcripts are authorized in principle. The remaining content-envelope decision is whether transcript exports include image/media bytes, references without bytes, or text only. Recommended initial envelope: text plus approved structured metadata, excluding media bytes and signed URLs. Obtain this ruling before enabling that class; proceed with independent response/assessment classes meanwhile. LA exports wait for verified R4 contribution provenance, group disclosure controls and empty-scope retirement.

**Terminal:** actual synthetic artifacts validate against the selected schema, no direct-download or eligibility-race bypass, approved document wording used in the dialogs, and reviewed core/AI drafts with their dependencies explicit. Port and adapt `assessmentExports.integration.test.ts`, `dataExports.integration.test.ts`, `assessmentExportArtifact.test.ts` and `assessmentExportRequest.test.ts` under `packages/graphql/test/`. Download tests assert contracts and data boundaries, not exact prose. Update the existing export engineering documentation identified during implementation.

### R4 — Consent-aware LA activation, withdrawal and group reports

**Owner/targets:** main on `v3`, AI-specific provenance/research adapters on `v3-ai`. **Dependencies:** R0 live inventory/disposition and the shipped choices; current old-stack schema reconciliation. Do not activate the hard-disabled path just because choices exist.

**Slice R4a — Withdrawal before activation.** Port `packages/graphql/src/services/participantAnalyticsWithdrawal.ts`, its Hatchet handler registration and `apps/analytics/src/modules/analytics_eligibility.py` from #5819 after the schema mapping. Reconcile the existing `ParticipantAnalyticsWithdrawal` queue with the preserved worker/consumer. Establish idempotent deletion of all applicable participant derivatives, invalidation of future publication and exclusion from future aggregates, retries and durable completion evidence. Cover first-false legacy state, true-to-false changes, withdrawal/re-enable races, in-flight writers and previously emptied scopes. Follow [ADR-0023](../docs/adr/0023-global-learning-analytics-choice-and-course-gate.md): individual derivatives are deleted and future aggregates exclude the participant; do not promise retroactive removal/recomputation of already published de-identified aggregates. Re-enablement is prospective: neither old derivatives nor operational events from excluded intervals become eligible merely because the current Boolean is true. Record an effective boundary for the new eligible interval. Preserve operational records and assessment retention. Use the same proven lock/fencing contract across choices and writers; current source comments explicitly state course writers do not yet acquire it.

**Slice R4b — Course activation and computation.** Reconcile the existing LA stack (#5413, #5611, #5629) rather than introducing competing models. Add the explicit course control (default enabled under ADR-0023 only when the global release containment is lifted), participant eligibility and effective disclosure metadata at every maintained writer/read path; `Course.areAnalyticsValid` remains cache validity. Validate each of the nine historical analytics families against the actual current model inventory. A scope key used for aggregate replacement/retirement must remain stable when participant membership changes; delete stale contribution rows when a recomputation produces zero eligible participants. Reconcile `apps/analytics/src/modules/research_contribution.py` and its existing draft test with the nine families. Preserve only supported, versioned provenance for later research release. Course disable immediately stops optional reads/writes and initiates course-scoped derivative deletion; in-flight publication must fail its fence. Re-enable starts a new eligible interval, without historical backfill of excluded periods. Preserve operational data and do not promise recall of already released de-identified aggregates.

**Slice R4c — Protected reports and rollout.** Student insights are self-scoped; lecturers receive de-identified group reports without rows per individual. The preserved `packages/graphql/src/lib/learningAnalyticsOutputV2.ts` sets minimum cell size 5 and suppresses a nonempty complement below 5. Retain those existing safeguards while removing its `studentReport`/`LearningAnalyticsStudentSummaryV2` individual-row output from lecturer API, UI and downloads. A pseudonymized individual row is still prohibited by the later group-only ruling. Verify aggregate drill-down and export cannot reconstruct that removed view; any proposed relaxation needs a new product decision before activation. Verify eligible-cohort selection, aggregate provenance, small/empty group behavior, course disable and deletion completion. Once separately authorized, exercise controlled STG activation and rollback. Source flags, UI hiding and queue insertion alone are insufficient. Update student availability text only after operational acceptance; keep the privacy policy's deletion claim matched to effective behavior.

**Source terminal:** focused drafts, current migration/consumer/writer tests, and group-report contract proof. **Activation acceptance:** after named authority, withdrawal completion/non-recreation and controlled STG activation pass before PRD preparation. PRD activation and retained-data cleanup require exact named authority, counts/dry-run, recovery limits and monitoring criteria.

#### Eligibility matrix for R3/R4

| Participant and course state | Optional LA computation/self-read/group contribution | Research release | Operational assessment and gamification |
| --- | --- | --- | --- |
| LA refused or course disabled | Denied; delete applicable individual/course derivatives and fence future publication | Operational response/transcript classes depend independently on research choice; no unavailable LA-derived class | Preserved under existing authorization |
| LA allowed, course enabled, research refused | Allowed only after controlled LA activation and within eligible time intervals | Denied, including previously staged artifacts | Preserved |
| LA allowed, course enabled, research allowed | Allowed after activation and disclosure checks | Allowed only through ADMIN attestation, release-time recheck, supported provenance and data minimization | Preserved |
| Missing purpose metadata or stale account acknowledgement | Account completion remains required; no optional LA processing | No inferred eligibility from default Booleans | Legitimate temporary anonymous flows remain separate |

Research refusal never withdraws otherwise permitted LA. LA refusal never suppresses operational assessment results. Research withdrawal affects future release under ADR-0024; no automatic recall of demonstrably de-identified released exports is promised.

### R5 — Retained points and leaderboard boundaries

**Owner/target:** main, `v3`. **Dependency:** current scoring/leaderboard baseline, including merged changes since #5819. Independent of LA activation and optional consent.

**Slice R5a — retain private accrual.** Reconcile `packages/graphql/src/services/{courses,participants,groups}.ts` and port `src/lib/groupScores.ts` plus `test/leaderboardRetention.test.ts` from #5819. Current group reduction already counts all members, treating a missing leaderboard score as zero; its missing behavior is retaining/accruing those scores while unpublished. Preserve that denominator. Reconcile #5819's private-point accrual, leave/rejoin and deletion-confirmation work with the current course scoring and leaderboard. Preserve points while publication is off, show retained totals on join/rejoin, and do not replay historical rank awards. Gamification group averages include **all members**, regardless of individual leaderboard publication or LA/research choice. Keep XP, historical awards, reset windows and assessment corrections distinct. No reconstruction of already-erased points is promised. Verify every actual writer/reader rather than replacing current leaderboard UI wholesale.

**Slice R5b — UI and integrated proof.** Reuse the existing join/leave/account-deletion controls and their confirmation text; port only missing retention consequences. **Acceptance:** synthetic accrual while unpublished, leave/rejoin, group denominators, concurrency/idempotency, ranking awards and account-deletion consequences; existing scoring tests extended only for missing behaviors; relevant browser captures. Any missing deletion/retention confirmation from the older draft is explicitly dispositioned. Deliver a focused draft and a compatibility check against current scoring; no research/LA activation dependency.

## Progress and next action

Completed: core source and PRD alpha.81 rollout, public German/English policy and LA pages, shared persistence/settings contract, source LA containment, and same-database target confirmation. Historical provider/Devrouter blockers are not current blockers for this planning task.

Open: R0 authenticated assessment/PRD interaction evidence, direct-response admission correction and synthetic proof, live optional-processing/retained-data evidence; R1 chat completion; R2 KB confirmation; R3 exports; R4 LA activation/withdrawal/reporting; R5 retained points reconciliation. Old broad drafts are preserved and must not be counted as deployed.

This planning task ends at updated documentation delivery. The next proposed execution is R0's narrow response-admission correction and remaining evidence, plus R1, followed by R2. Earlier explicit implementation authority remains available within its recorded scope when execution resumes; this roadmap itself grants none. Hosted actor writes, STG/PRD activation, target merges and retained-data deletion retain their named authority boundaries. Review the exact branch-sync conflicts alongside release preparation. This plan update does not reopen settled guest-choice requirements, separate normal/assessment accounts, research ADMIN authority, or all-member group averaging. The unresolved transcript-media envelope blocks only that export class; no new cohort-threshold decision is required to preserve the existing safeguards. Work through independent items while those rulings remain open.

## Review disposition for this reconciliation

The pre-solo source review identified missing chat account completion, KB/import coverage, export and withdrawal consumers, and direct-response admission. Main verified the deployed tag's ingress before promoting that item to a corrective priority; no production bypass was executed. Main rejected the review's claim that group denominators omit members: current `groups.ts` counts every member, but missing/cleared leaderboard scores contribute zero. R5 now preserves the existing denominator and fixes retained accrual.

The planning review requested clearer acceptance, concrete paths/tests, eligibility, ownership and migration identity. This revision separates investigation from passed acceptance, records exact migration names, moves the test portfolio before slices, names the old source/test ports and resolves ownership to main under the user's explicit solo mode. It also applies accepted ADR-0023/0024 rather than inventing retroactive aggregate erasure or research approval. Final review is main-session self-review; the earlier review's REVISE result is not represented as an independent approval.
