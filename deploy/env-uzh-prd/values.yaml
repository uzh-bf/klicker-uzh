dfDomain: df-app.ch

heartbeats:
  dailyGroupScores: https://betteruptime.com/api/v1/heartbeat/xN8ytcTixyVXsWnNjk9ndimD

global:
  appOrigins:
    api: 'https://backend-sls.klicker.uzh.ch'
    auth: 'https://auth.klicker.uzh.ch'
    lti: 'https://lti.klicker.uzh.ch'
    pwa: 'https://pwa.klicker.uzh.ch'
    manage: 'https://manage.klicker.uzh.ch'
    control: 'https://control.klicker.uzh.ch'
    assessmentApi: 'https://assessment-api.klicker.uzh.ch'
    assessmentPwa: 'https://assessment.klicker.uzh.ch'

podAnnotations:
  reloader.stakater.com/auto: 'true'

hatchet:
  client:
    tlsStrategy: 'none'
    apiUrl: 'http://app-klicker-uzh-hatchet-api.prd-hatchet.svc.cluster.local:8080'
  workers:
    general:
      replicaCount: 2
      priorityClassName: production-workload
      image:
        repository: ghcr.io/uzh-bf/klicker-uzh/hatchet-worker-general-arm
        pullPolicy: Always
        tag: v3.4.0-alpha.62
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/component
                      operator: In
                      values:
                        - hatchet-worker-general
                topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: hatchet-worker-general
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: hatchet-worker-general

    responseProcessor:
      replicaCount: 4
      priorityClassName: production-workload
      image:
        repository: ghcr.io/uzh-bf/klicker-uzh/hatchet-worker-response-processor-arm
        pullPolicy: Always
        tag: v3.4.0-alpha.62
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/component
                      operator: In
                      values:
                        - hatchet-worker-response-processor
                topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: hatchet-worker-response-processor
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: hatchet-worker-response-processor

    responseProcessorAssessment:
      replicaCount: 4
      priorityClassName: production-workload
      image:
        repository: ghcr.io/uzh-bf/klicker-uzh/hatchet-worker-response-processor-arm
        pullPolicy: Always
        tag: v3.4.0-alpha.62
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/component
                      operator: In
                      values:
                        - hatchet-worker-response-processor-assessment
                topologyKey: kubernetes.io/hostname
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: klickerasm
                    operator: In
                    values:
                      - reserved
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: hatchet-worker-response-processor-assessment
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: hatchet-worker-response-processor-assessment
      tolerations:
        - key: klickerasm
          operator: Equal
          value: reserved
          effect: NoSchedule

auth:
  priorityClassName: production-workload

  replicaCount: 3

  autoscaling:
    enabled: false

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - auth
            topologyKey: kubernetes.io/hostname

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/auth-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.62

  ingress:
    annotations:
      cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
    className: haproxy
    hosts:
      - host: auth.klicker.uzh.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-frontend-auth-tls
        hosts:
          - auth.klicker.uzh.ch

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      memory: 200Mi

chat:
  allowedFrameAncestors: 'https://*.klicker.uzh.ch https://lms.uzh.ch https://*.olat.uzh.ch https://elearning.klicker.uzh.ch https://*.df-app.ch'
  priorityClassName: production-workload

  openai:
    baseUrl: 'http://litellm.prd-litellm.svc.cluster.local:4000/v1'
    storeResponses: true

  telemetry:
    enabled: false

  replicaCount: 1

  autoscaling:
    enabled: false

  automaticModels:
    primaryId: gpt-5.5
    fallbackId: gpt-4.1-mini

  modelRegistry:
    # "Auto" routes through the LiteLLM complexity-router, which self-selects the
    # tier per request (SIMPLE=gpt-4.1, MEDIUM=gpt-5.4-low, COMPLEX=gpt-5.4-medium,
    # REASONING=gpt-5.5-low). It is a normal selectable model (fallback: false), so
    # it does NOT change automatic selection for existing chatbots — they still
    # default to gpt-5.5. A chatbot can be pinned to Auto-only via its DB columns
    # (modelSelection=false + allowedModelIds=["auto"]).
    # cost mirrors the gpt-5.4 tier (the typical COMPLEX route); REASONING (gpt-5.5)
    # is rare. Tune if the routed mix shifts.
    - id: auto
      deploymentId: klickeruzh/azure/complexity-router
      name: Auto
      description: Automatic model selection (routes to the best model per request)
      supportsImageAttachments: true
      fallback: false
      cost:
        input: 1.25
        output: 10.0
    - id: gpt-4.1
      deploymentId: klickeruzh/azure/gpt-4.1
      name: GPT-4.1
      description: OpenAI model
      supportsImageAttachments: true
      fallback: false
      cost:
        input: 2.0
        output: 8.0
    - id: gpt-5.1
      deploymentId: klickeruzh/azure/gpt-5.1
      name: GPT-5.1
      description: OpenAI reasoning model
      supportsImageAttachments: true
      supportsReasoning: true
      supportedReasoningEfforts:
        - none
        - minimal
        - low
        - medium
        - high
      fallback: false
      cost:
        input: 1.25
        output: 10.0
    - id: gpt-5.4
      deploymentId: klickeruzh/azure/gpt-5.4
      name: GPT-5.4
      description: OpenAI reasoning model
      supportsImageAttachments: true
      supportsReasoning: true
      supportedReasoningEfforts:
        - minimal
        - low
        - medium
        - high
        - xhigh
      fallback: false
      cost:
        input: 1.25
        output: 10.0
    - id: gpt-5.5
      deploymentId: klickeruzh/azure/gpt-5.5
      name: GPT-5.5
      description: OpenAI reasoning model
      supportsImageAttachments: true
      supportsReasoning: true
      supportedReasoningEfforts:
        - none
        - minimal
        - low
        - medium
        - high
        - xhigh
      fallback: false
      cost:
        input: 5.0
        output: 30.0
    - id: gpt-4.1-mini
      deploymentId: klickeruzh/azure/gpt-4.1-mini
      name: GPT-4.1 Mini
      description: Small OpenAI model
      supportsImageAttachments: true
      fallback: true
      cost:
        input: 0.4
        output: 1.6

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - chat
            topologyKey: kubernetes.io/hostname
  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/chat-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.64

  ingress:
    annotations:
      cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
    className: haproxy
    hosts:
      - host: chat.klicker.uzh.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-frontend-chat-tls
        hosts:
          - chat.klicker.uzh.ch

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1

  resources:
    requests:
      cpu: 50m
      memory: 200Mi
    limits:
      memory: 400Mi

mcpStudent:
  priorityClassName: production-workload

  replicaCount: 2

  autoscaling:
    enabled: false

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - mcp-student
            topologyKey: kubernetes.io/hostname

  topologySpreadConstraints:
    - maxSkew: 1
      topologyKey: topology.kubernetes.io/zone
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          app.kubernetes.io/component: mcp-student
    - maxSkew: 1
      topologyKey: kubernetes.io/hostname
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          app.kubernetes.io/component: mcp-student

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/mcp-student-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.61

  strategy:
    type: RollingUpdate

  resources:
    requests:
      cpu: 25m
      memory: 50Mi
    limits:
      memory: 200Mi

mcpLecturer:
  priorityClassName: production-workload

  replicaCount: 2

  autoscaling:
    enabled: false

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - mcp-lecturer
            topologyKey: kubernetes.io/hostname

  topologySpreadConstraints:
    - maxSkew: 1
      topologyKey: topology.kubernetes.io/zone
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          app.kubernetes.io/component: mcp-lecturer
    - maxSkew: 1
      topologyKey: kubernetes.io/hostname
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          app.kubernetes.io/component: mcp-lecturer

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/mcp-lecturer-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.61

  strategy:
    type: RollingUpdate

  resources:
    requests:
      cpu: 25m
      memory: 50Mi
    limits:
      memory: 200Mi

frontendPWA:
  allowedFrameAncestors: 'https://*.klicker.uzh.ch https://lms.uzh.ch https://*.olat.uzh.ch https://elearning.klicker.uzh.ch https://ius.zone https://*.df-app.ch'
  priorityClassName: production-workload

  replicaCount: 4

  autoscaling:
    enabled: false

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - frontend-pwa
            topologyKey: kubernetes.io/hostname

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/frontend-pwa-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.62

  ingress:
    className: haproxy
    annotations:
      cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
    hosts:
      - host: pwa.klicker.uzh.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-frontend-pwa-tls
        hosts:
          - pwa.klicker.uzh.ch

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  resources:
    requests:
      cpu: 100m
      memory: 200Mi
    limits:
      memory: 600Mi

frontendManage:
  priorityClassName: production-workload

  replicaCount: 3

  autoscaling:
    enabled: false

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - frontend-manage
            topologyKey: kubernetes.io/hostname

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/frontend-manage-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.62

  ingress:
    className: haproxy
    annotations:
      cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
    hosts:
      - host: manage.klicker.uzh.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-frontend-manage-tls
        hosts:
          - manage.klicker.uzh.ch

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      memory: 200Mi

frontendControl:
  allowedFrameAncestors: 'https://*.klicker.uzh.ch https://lms.uzh.ch https://*.olat.uzh.ch https://elearning.klicker.uzh.ch https://*.df-app.ch'
  priorityClassName: production-workload

  replicaCount: 1

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/frontend-control-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.62

  autoscaling:
    enabled: false

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - frontend-control
            topologyKey: kubernetes.io/hostname

  ingress:
    className: haproxy
    annotations:
      cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
    hosts:
      - host: control.klicker.uzh.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-frontend-control-tls
        hosts:
          - control.klicker.uzh.ch

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      memory: 200Mi

backendGraphql:
  priorityClassName: production-workload

  replicaCount: 4

  autoscaling:
    enabled: false

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - backend-graphql
            topologyKey: kubernetes.io/hostname

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/backend-docker-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.62

  appManageSubdomain: manage
  appStudentSubdomain: pwa
  appControlSubdomain: control

  debug: true
  apiDomain: backend-sls.klicker.uzh.ch
  appManageDomain: manage.klicker.uzh.ch
  appStudentDomain: pwa.klicker.uzh.ch
  appControlDomain: control.klicker.uzh.ch
  cookieDomain: .klicker.uzh.ch

  ingress:
    className: haproxy
    annotations:
      cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
    hosts:
      - host: backend-sls.klicker.uzh.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
      # - host: backend-sls.klicker.uzh.ch
      #   paths:
      #     - path: /
      #       pathType: ImplementationSpecific
    tls:
      - secretName: klicker-backend-graphql-tls
        hosts:
          - backend-sls.klicker.uzh.ch
          # - backend-sls.klicker.uzh.ch

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  resources:
    requests:
      cpu: 100m
      memory: 200Mi
    limits:
      memory: 600Mi

olatApi:
  priorityClassName: production-workload

  replicaCount: 1

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/olat-api-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.62

  autoscaling:
    enabled: false

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - olat-api
            topologyKey: kubernetes.io/hostname

  ingress:
    className: haproxy
    annotations:
      cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
    hosts:
      - host: olat-api.klicker.uzh.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-olat-api-tls
        hosts:
          - olat-api.klicker.uzh.ch

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      memory: 200Mi

lti:
  priorityClassName: production-workload
  replicaCount: 2

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - lti
            topologyKey: kubernetes.io/hostname

  redirectURL: https://lti.klicker.uzh.ch
  url: https://exp.olat.uzh.ch
  name: OLAT UZH
  authEndpoint: https://exp.olat.uzh.ch/lti/auth
  tokenEndpoint: https://exp.olat.uzh.ch/lti/token
  keysEndpoint: https://exp.olat.uzh.ch/lti/keys
  cookieDomain: .klicker.uzh.ch

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/lti-arm
    pullPolicy: Always
    tag: v3.4.0-alpha.62

  ingress:
    className: haproxy
    annotations:
      cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
    hosts:
      - host: lti.klicker.uzh.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-lti-tls
        hosts:
          - lti.klicker.uzh.ch

responseApi:
  corsAllowedOrigins: 'https://pwa.klicker.uzh.ch'

  priorityClassName: production-workload

  replicaCount: 4

  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/response-api-arm
    tag: v3.4.0-alpha.62
    pullPolicy: Always

  autoscaling:
    enabled: false

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - response-api
            topologyKey: kubernetes.io/hostname

  ingress:
    className: haproxy
    annotations:
      cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
    hosts:
      - host: response-api.klicker.uzh.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-response-api-tls
        hosts:
          - response-api.klicker.uzh.ch

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      memory: 200Mi

assessment:
  frontendPWA:
    priorityClassName: production-workload
    replicaCount: 4
    autoscaling:
      enabled: false

    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                  - key: app.kubernetes.io/component
                    operator: In
                    values:
                      - frontend-pwa-assessment
              topologyKey: kubernetes.io/hostname
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: klickerasm
                  operator: In
                  values:
                    - reserved
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: frontend-pwa-assessment
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: frontend-pwa-assessment
    tolerations:
      - key: klickerasm
        operator: Equal
        value: reserved
        effect: NoSchedule

    image:
      repository: ghcr.io/uzh-bf/klicker-uzh/frontend-assessment-arm
      pullPolicy: Always
      tag: v3.4.0-alpha.62

    ingress:
      className: haproxy
      annotations:
        cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
      hosts:
        - host: assessment.klicker.uzh.ch
          paths:
            - path: /
              pathType: ImplementationSpecific
      tls:
        - secretName: klicker-frontend-assessment-tls
          hosts:
            - assessment.klicker.uzh.ch

    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxUnavailable: 1
        maxSurge: 1

    resources:
      requests:
        cpu: 100m
        memory: 200Mi
      limits:
        memory: 600Mi

  backendGraphql:
    priorityClassName: production-workload
    replicaCount: 4
    autoscaling:
      enabled: false

    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                  - key: app.kubernetes.io/component
                    operator: In
                    values:
                      - backend-assessment
              topologyKey: kubernetes.io/hostname
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: klickerasm
                  operator: In
                  values:
                    - reserved
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: backend-assessment
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: backend-assessment
    tolerations:
      - key: klickerasm
        operator: Equal
        value: reserved
        effect: NoSchedule

    image:
      repository: ghcr.io/uzh-bf/klicker-uzh/backend-docker-arm
      pullPolicy: Always
      tag: v3.4.0-alpha.62

    appManageSubdomain: manage
    appStudentSubdomain: assessment
    appControlSubdomain: control

    debug: true
    apiDomain: backend-sls.klicker.uzh.ch
    appManageDomain: manage.klicker.uzh.ch
    appStudentDomain: assessment.klicker.uzh.ch
    appControlDomain: control.klicker.uzh.ch
    cookieDomain: .klicker.uzh.ch

    ingress:
      className: haproxy
      annotations:
        cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
      hosts:
        - host: assessment-api.klicker.uzh.ch
          paths:
            - path: /
              pathType: ImplementationSpecific
      tls:
        - secretName: klicker-backend-assessment-tls
          hosts:
            - assessment-api.klicker.uzh.ch

    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxUnavailable: 1
        maxSurge: 1

    resources:
      requests:
        cpu: 100m
        memory: 200Mi
      limits:
        memory: 600Mi

  responseApi:
    corsAllowedOrigins: 'https://assessment.klicker.uzh.ch'

    priorityClassName: production-workload

    replicaCount: 3

    autoscaling:
      enabled: false

    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                  - key: app.kubernetes.io/component
                    operator: In
                    values:
                      - response-api-assessment
              topologyKey: kubernetes.io/hostname
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: klickerasm
                  operator: In
                  values:
                    - reserved
    tolerations:
      - key: klickerasm
        operator: Equal
        value: reserved
        effect: NoSchedule

    image:
      repository: ghcr.io/uzh-bf/klicker-uzh/response-api-arm
      pullPolicy: Always
      tag: v3.4.0-alpha.62

    ingress:
      className: haproxy
      annotations:
        cert-manager.io/cluster-issuer: k8s-crt-mgr-iss-prd-apps-default
      hosts:
        - host: assessment-response-api.klicker.uzh.ch
          paths:
            - path: /
              pathType: ImplementationSpecific
      tls:
        - secretName: klicker-response-api-assessment-tls
          hosts:
            - assessment-response-api.klicker.uzh.ch

    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxUnavailable: 1
        maxSurge: 1

    resources:
      requests:
        cpu: 50m
        memory: 50Mi
      limits:
        memory: 200Mi

# Migration hook image: only the repo is pinned; the tag auto-tracks the
# backendGraphql image tag above (see chart job-migrate.yaml).
#
# DISABLED UNTIL THE FIRST POST-HOOK RELEASE. No migrator image exists for any
# release tag cut before this feature landed, so while backendGraphql.image.tag
# still points at such a tag the hook would fail on ImagePullBackOff and abort
# every prd sync — including a rollback to an older tag. Flip this to true in the
# same commit that bumps the prd image tags to the first release whose CI built
# `backend-docker-migrator-arm`. Until then prd migrations stay manual
# (`prisma:deploy:prod`). Rolling back to a pre-hook tag requires setting this
# back to false. See docs/data-and-migrations.md → Recovering a failed hook.
migrator:
  enabled: false
  priorityClassName: production-workload
  image:
    repository: ghcr.io/uzh-bf/klicker-uzh/backend-docker-migrator-arm
