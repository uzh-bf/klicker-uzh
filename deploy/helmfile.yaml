releases:
  # - name: klicker-stage
  #   namespace: klicker-stage
  #   createNamespace: true
  #   chart: ./chart
  #   version: '{{ env "CHART_VERSION" | default "0.1.0" }}'
  #   installed: {{ env "INSTALL" | default "true" }}
  #   values:
  #     - chart/values.yaml
  #     - chart/values.stage.yaml
  #     - chart/secrets.stage.yaml

  - name: klicker-qa
    namespace: klicker-qa
    createNamespace: true
    chart: ./chart
    version: '{{ env "CHART_VERSION" | default "0.1.0" }}'
    installed: true
    kubeContext: klicker-k8s26bebe63-admin
    values:
      - chart/values.yaml
      - chart/values.qa.yaml
      - chart/secrets.qa.yaml

  # - name: klicker-prod
  #   namespace: klicker-prod
  #   createNamespace: true
  #   chart: ./chart
  #   version: '{{ env "CHART_VERSION" | default "0.1.0" }}'
  #   installed: true
  #   kubeContext: klicker-k8s26bebe63-admin
  #   values:
  #     - chart/values.yaml
  #     - chart/values.prod.yaml
  #     - chart/secrets.prod.yaml

  # - name: klicker-faculties
  #   namespace: klicker-faculties
  #   createNamespace: true
  #   chart: ./chart
  #   version: '{{ env "CHART_VERSION" | default "0.1.0" }}'
  #   installed: true
  #   kubeContext: klicker-k8s26bebe63-admin
  #   values:
  #     - chart/values.yaml
  #     - chart/values.faculties.yaml
  #     - chart/secrets.faculties.yaml
  #     - frontend:
  #         image:
  #           tag: v1.7.0-alpha.5
  #       backend:
  #         image:
  #           tag: v1.7.0-alpha.5
