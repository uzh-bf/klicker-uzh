heartbeats:
  dailyGroupScores: https://betteruptime.com/api/v1/heartbeat/xN8ytcTixyVXsWnNjk9ndimD

auth:
  priorityClassName: staging-workload

  autoscaling:
    enabled: false

  image:
    tag: argocd

  ingress:
    className: haproxy
    hosts:
      - host: auth.klicker-qa.stg.example.df-app.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-prod-frontend-auth-tls
        hosts:
          - auth.klicker-qa.stg.example.df-app.ch

  strategy:
    type: RollingUpdate
    # type: Recreate

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      cpu: 200m
      memory: 200Mi

frontendPWA:
  priorityClassName: staging-workload

  autoscaling:
    enabled: false

  image:
    tag: argocd

  ingress:
    className: haproxy
    hosts:
      - host: pwa.klicker-qa.stg.example.df-app.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-qa-frontend-pwa-tls
        hosts:
          - pwa.klicker-qa.stg.example.df-app.ch

  strategy:
    type: Recreate

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      cpu: 200m
      memory: 200Mi

frontendManage:
  priorityClassName: staging-workload

  autoscaling:
    enabled: false

  image:
    tag: argocd

  ingress:
    className: haproxy
    hosts:
      - host: manage.klicker-qa.stg.example.df-app.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-qa-frontend-manage-tls
        hosts:
          - manage.klicker-qa.stg.example.df-app.ch

  strategy:
    type: Recreate

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      cpu: 200m
      memory: 200Mi

frontendControl:
  priorityClassName: staging-workload

  autoscaling:
    enabled: false

  image:
    tag: argocd

  ingress:
    className: haproxy
    hosts:
      - host: control.klicker-qa.stg.example.df-app.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-qa-frontend-control-tls
        hosts:
          - control.klicker-qa.stg.example.df-app.ch

  strategy:
    type: Recreate

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      cpu: 200m
      memory: 200Mi

backendGraphql:
  priorityClassName: staging-workload

  autoscaling:
    enabled: false

  image:
    tag: argocd

  appManageSubdomain: manage
  appStudentSubdomain: pwa
  appControlSubdomain: control

  ingress:
    className: haproxy
    hosts:
      - host: backend-sls.klicker-qa.stg.example.df-app.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-qa-backend-graphql-tls
        hosts:
          - backend-sls.klicker-qa.stg.example.df-app.ch

  strategy:
    type: Recreate

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      cpu: 200m
      memory: 200Mi

olatApi:
  priorityClassName: staging-workload

  autoscaling:
    enabled: false

  image:
    tag: argocd

  ingress:
    className: haproxy
    hosts:
      - host: olat-api.klicker-qa.stg.example.df-app.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-qa-olat-api-tls
        hosts:
          - olat-api.klicker-qa.stg.example.df-app.ch

  strategy:
    type: Recreate

  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      cpu: 200m
      memory: 200Mi

lti:
  priorityClassName: staging-workload

  image:
    tag: argocd

  ingress:
    className: haproxy
    hosts:
      - host: lti.klicker-qa.stg.example.df-app.ch
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - secretName: klicker-qa-lti-tls
        hosts:
          - lti.klicker-qa.stg.example.df-app.ch

redis:
  priorityClassName: staging-workload
  architecture: standalone
  auth:
    enabled: false
