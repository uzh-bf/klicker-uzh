{{- if .Values.olat-api.ingress.enabled -}}
{{- $fullName := include "chart.fullname" . -}}
{{- $svcPort := .Values.olat-api.service.port -}}
{{- if and .Values.olat-api.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.olat-api.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.olat-api.ingress.annotations "kubernetes.io/ingress.class" .Values.olat-api.ingress.className}}
  {{- end }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-olat-api
  labels:
    {{- include "chart.ingressLabels" . | nindent 4 }}
  {{- with .Values.olat-api.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.olat-api.ingress.className }}
  {{- if .Values.olat-api.ingress.tls }}
  tls:
    {{- range .Values.olat-api.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.olat-api.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}-olat-api
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
