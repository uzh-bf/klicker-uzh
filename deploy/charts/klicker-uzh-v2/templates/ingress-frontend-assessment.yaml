{{- if .Values.assessment.frontendPWA.ingress.enabled -}}
{{- $fullName := include "chart.fullname" . -}}
{{- $svcPort := .Values.assessment.frontendPWA.service.port -}}
{{- if and .Values.assessment.frontendPWA.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.assessment.frontendPWA.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.assessment.frontendPWA.ingress.annotations "kubernetes.io/ingress.class" .Values.assessment.frontendPWA.ingress.className}}
  {{- end }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-frontend-assessment
  labels:
    {{- include "chart.ingressLabels" . | nindent 4 }}
  {{- with .Values.assessment.frontendPWA.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.assessment.frontendPWA.ingress.className }}
  {{- if .Values.assessment.frontendPWA.ingress.tls }}
  tls:
    {{- range .Values.assessment.frontendPWA.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.assessment.frontendPWA.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}-frontend-assessment
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
