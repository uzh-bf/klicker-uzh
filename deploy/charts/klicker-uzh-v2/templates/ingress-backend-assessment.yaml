{{- if .Values.assessment.backendGraphql.ingress.enabled -}}
{{- $fullName := include "chart.fullname" . -}}
{{- $svcPort := .Values.assessment.backendGraphql.service.port -}}
{{- if and .Values.assessment.backendGraphql.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.assessment.backendGraphql.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.assessment.backendGraphql.ingress.annotations "kubernetes.io/ingress.class" .Values.assessment.backendGraphql.ingress.className}}
  {{- end }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-backend-assessment
  labels:
    {{- include "chart.ingressLabels" . | nindent 4 }}
  {{- with .Values.assessment.backendGraphql.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.assessment.backendGraphql.ingress.className }}
  {{- if .Values.assessment.backendGraphql.ingress.tls }}
  tls:
    {{- range .Values.assessment.backendGraphql.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.assessment.backendGraphql.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}-backend-assessment
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
