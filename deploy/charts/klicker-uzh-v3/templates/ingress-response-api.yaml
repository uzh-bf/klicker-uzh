{{- if .Values.responseApi.ingress.enabled -}}
{{- $fullName := include "chart.fullname" . -}}
{{- $svcPort := .Values.responseApi.service.port -}}
{{- if and .Values.responseApi.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.responseApi.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.responseApi.ingress.annotations "kubernetes.io/ingress.class" .Values.responseApi.ingress.className}}
  {{- end }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-response-api
  labels:
    {{- include "chart.ingressLabels" . | nindent 4 }}
  {{- with .Values.responseApi.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.responseApi.ingress.className }}
  {{- if .Values.responseApi.ingress.tls }}
  tls:
    {{- range .Values.responseApi.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.responseApi.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}-response-api
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
