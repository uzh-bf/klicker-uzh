{{- if and .Values.mcpLecturer.enabled .Values.mcpLecturer.autoscaling.enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "chart.fullname" . }}-mcp-lecturer
  labels:
    {{- include "chart.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "chart.fullname" . }}-mcp-lecturer
  minReplicas: {{ .Values.mcpLecturer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.mcpLecturer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.mcpLecturer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.mcpLecturer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.mcpLecturer.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.mcpLecturer.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
