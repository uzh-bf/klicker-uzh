{{- if .Values.frontendKb.ingress.enabled -}}
{{- $fullName := include "chart.fullname" . -}}
{{- $svcPort := .Values.frontendKb.service.port -}}
{{- if and .Values.frontendKb.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.frontendKb.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.frontendKb.ingress.annotations "kubernetes.io/ingress.class" .Values.frontendKb.ingress.className}}
  {{- end }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-frontend-kb
  labels:
    {{- include "chart.ingressLabels" . | nindent 4 }}
  {{- with .Values.frontendKb.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.frontendKb.ingress.className }}
  {{- if .Values.frontendKb.ingress.tls }}
  tls:
    {{- range .Values.frontendKb.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.frontendKb.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}-frontend-kb
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
