{{- if .Values.assessmentAudit.enabled }}
{{- if ne (int .Values.assessmentAudit.mediaPolicyWorker.replicaCount) 1 }}
{{- fail "assessmentAudit.mediaPolicyWorker.replicaCount must remain 1" }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chart.fullname" . }}-assessment-audit-media-policy-worker
  labels:
    {{- include "chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: assessment-audit-media-policy-worker
  annotations:
    reloader.stakater.com/auto: 'true'
spec:
  revisionHistoryLimit: 5
  replicas: {{ .Values.assessmentAudit.mediaPolicyWorker.replicaCount }}
  selector:
    matchLabels:
      {{- include "chart.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: assessment-audit-media-policy-worker
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/cm-audit-workers.yaml") . | sha256sum }}
        release: {{ .Chart.AppVersion }}
        {{- with .Values.assessmentAudit.mediaPolicyWorker.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "chart.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: assessment-audit-media-policy-worker
        azure.workload.identity/use: 'true'
    spec:
      serviceAccountName: {{ required "assessmentAudit.mediaPolicyWorker.serviceAccountName is required when assessment audit is enabled" .Values.assessmentAudit.mediaPolicyWorker.serviceAccountName }}
      priorityClassName: {{ include "chart.fullname" . }}-{{ .Values.assessmentAudit.mediaPolicyWorker.priorityClassName }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: assessment-audit-media-policy-worker
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.assessmentAudit.mediaPolicyWorker.image.repository }}:{{ required "assessmentAudit.mediaPolicyWorker.image.tag is required when assessment audit is enabled" .Values.assessmentAudit.mediaPolicyWorker.image.tag }}"
          imagePullPolicy: {{ .Values.assessmentAudit.mediaPolicyWorker.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "chart.fullname" . }}-config-global
            - configMapRef:
                name: {{ include "chart.fullname" . }}-config-assessment-audit-media-policy-worker
            - secretRef:
                name: {{ include "chart.fullname" . }}-secret-hatchet-worker-general
          env:
            - name: HATCHET_WORKER_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          ports:
            - name: metrics
              containerPort: 9464
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.assessmentAudit.mediaPolicyWorker.resources | nindent 12 }}
      {{- with .Values.assessmentAudit.mediaPolicyWorker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.assessmentAudit.mediaPolicyWorker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.assessmentAudit.mediaPolicyWorker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
